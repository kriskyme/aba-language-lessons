# Passage Reading Changelog

| Date | Version | Summary |
| --- | --- | --- |
| 2026-09-08 | v3.2 (Lesson) / v2.1 (Module Lesson-Plan) / v2.2 (Homework) | **Fixed-output Levels rotate task shape** (`shared/Changelog.md`'s Quality Standards v1.6 entry). Lesson prompt: new 0.11 shape bank and slot assignment for Levels 1-2 with the once-per-day cap; Day 1 Phase 3, Day 2 Phase 1 and Phase 2 lines cite it; self-check item 12. Module Lesson-Plan: the Task-Level basis names the shape per slot for a fixed-output Level; self-check adds no-shape-in-the-same-slot. Homework: the lowest Level's Part 1 is one frame plus one other-shape item, Part 2 one frame. |
| 2026-09-08 | v3.1 (Lesson) / v2.1 (Homework) / v5.1 (Assessment) | Self-contained rule applied - see `shared/Changelog.md`'s Quality Standards v1.1 entry. Foundation Support in the Lesson, Homework, and Assessment prompts no longer relies on teacher-made picture cards (printed words in the packet, real objects, student-written words instead); the Beginner genre bank's "picture-supported label sets" now embed their images in the packet. Renamed `_v3.0.md` -> `_v3.1.md`, `_v2.md` -> `_v2.1.md`, `_v5.0.md` -> `_v5.1.md`; `Index.md` updated. |
| 2026-09-08 | v2.0 (Module Lesson-Plan) / v2 (Homework) / v1.1 (TOEFL Extension) | Converted to the shared-layer structure - see `shared/Changelog.md`'s 2026-09-08 entry. Module Lesson-Plan: Section 0's Rotation Log mechanics, cross-Set and cross-Module adjacency rules, and the "why lighter touch" rationale (which Listening/Speaking's and Writing's plan prompts each cross-referenced by prose) now live in Conventions §F; the prompt keeps its column list, log template, band-conditioned real-referent topic rule, and plan-table definitions; the v2.5/v2.7 sync narrative and "Master Generation Document" reference removed; heading unified to `## 0. Rotation Log`. 242 lines -> 96. Homework: Respectful Tiers and the task-Level-count check point to Quality Standards; the "companions still on Tier A/B/C" preamble removed; Section 3 is "run Quality Standards §F, then 7 Reading items." 199 -> 111. TOEFL Extension: Markdown heading syntax added (it had none), a paste bundle, distractor quality pointed to Quality Standards §C, stale "v2.5"/"Assessment (v4)" references removed; task content unchanged. 198 -> 140. Renamed `_v1.3.md` -> `_v2.0.md`, `_v1.md` -> `_v2.md`, `_v1.md` -> `_v1.1.md`; `Index.md` updated (file table, workflow Steps 1, 3, 4). |
| 2026-09-08 | v5.0 (Assessment) / v2.0 (Assessment print) | Both converted to the shared-layer structure - see `shared/Changelog.md`'s 2026-09-08 entry. Assessment: Respectful Tiers (1.3) and Module alignment (1.4) replaced by pointers to Quality Standards §B/§A4; the Foundation Support check kept as 1.3 with only its Reading-specific format; the answerable-only-from-the-new-passage rule now stated once in 0.2 and inherited by every modality via Quality Standards §C3; item quality points to §C, so Reading assessments now pick up the distractor-plausibility and padded-word-bank rules Listening/Speaking introduced (this prompt had none); preamble's stale "synced to v2.5" and "Current version: v4.1" markers fixed; every `(New)` stamp removed; Section 3 is now "run Quality Standards §F, then these 11 Reading items." 449 lines -> 222. Assessment print: Section 1's shared rows, the masthead and answer-space rules, Section 4's regeneration text, and half of Section 5 now point to Style Guide §E/§F/§G/§I; the per-section page-break and no-`.masthead-meta` conventions recorded once in Style Guide §H.4 for all three modalities. 200 lines -> 104. Renamed `_v4.2.md` -> `_v5.0.md`, `_v1.2.md` -> `_v2.0.md`; `Index.md` updated (file table, workflow Steps 5 and 5a). |
| 2026-09-08 | v2.0 (print) | Converted to the shared-layer structure - see `shared/Changelog.md`'s 2026-09-08 entry. Section 1's six modality-neutral translation rows, Section 2.4's masthead rule, 2.6's star-rating and share-instruction rules, 2.9's no-coaching-note stem rule, 2.12's word-bank placement, 2.13's answer-space rule, 2.14's decorative-rule rule, Section 4's preview-first and when-guidance-runs-out text, and eleven of Section 5's twenty-one bullets now live in `shared/Student_Packet_Style_Guide.md` v2.0 (§E, §F, §G, §I) and are pointed to, not restated. What remains is Reading-specific: Unit A/B labels, the objective wording, Fact Finder / Cause & Effect merging, the worked-model box, the discussion-prompt fallback for pre-v2.6 lessons, Phrase Spotlight idiom rendering, the standing annotation key, and refresher blocks - renumbered 2.1-2.10. Preamble cut to lesson-type description, paste bundle, Changelog pointer, input, and the does-not-change rule; the stale "Companion to ... (v2.8)" reference and the in-table "As of v1.1" narration removed. Section 5 is now "run Style Guide §I, then these 9 Reading items." 339 lines -> 150. Renamed `_v1.12.md` -> `_v2.0.md`; `Index.md` updated (file table, workflow Step 6 paste bundle). |
| 2026-09-08 | v3.0 (Lesson) | Converted to the shared-layer structure - see `shared/Changelog.md`'s 2026-09-08 Quality Standards entry for the cross-modality picture. Every modality-neutral rule this prompt used to carry (Respectful Tiers and Foundation Support, the board-dependent-moment three-property test, the Skill Spotlight / demonstration-only Closing Transfer Check and its per-Set variety rule, item quality, adjacency-only rotation, no em-dashes, the metadata line) now lives in `shared/Generation_Quality_Standards.md` and is pointed to, not restated; Sections 0.7, 0.8, and the tiered-framework section keep only the Reading-specific placements and shapes (board slots by phase and organizer-by-Module, Closing Transfer Check sentence patterns, Fishbowl-based participation modes, Reading's Foundation Support response modes). Preamble cut from 56 lines to a lesson-type description, paste bundle, Changelog pointer, inputs, output, and scope; all inline version stamps (`(new in v2.2, reworded in v2.3...)`), past-failure narratives (the 55-word Level 3 text, the "something in this room" repeat, the v2.6/v2.7 Fishbowl reversal), the 0.2 band annotations re-deriving Conventions §B, and the stale "companions still assume Level A/B/C" and `assessment-generation-prompt-v4.md` references removed. Section 0.3's self-check is now "run Quality Standards §F, then these 11 Reading items" (was 19 items, 12 of them restating their source section). Genre formatting conventions converted from a 20-line paragraph to a table; Reading Execution Rules one line per strategy; the Closing Transfer Check variety rule stated once (was four places). Section numbering 0.1-0.10 deliberately unchanged so the Homework, Assessment, TOEFL, and Student Packet prompts' cross-references still resolve. 888 lines -> 490. No rule was dropped; anything that looks missing is in the shared file. Renamed `_v2.10.md` -> `_v3.0.md`; `Index.md` updated (file table, workflow Step 2 paste bundle, and Step 6's stale `_v1.11.md` reference corrected to the real `_v1.12.md`). |
| 2026-09-08 | v1.2 (Assessment print) | Section 2.6 rewritten: it had drifted into calling `.ans-line-sm` "a single short inline line" and saying a paragraph-letter or fill-in-the-blank item "gets only the inline `.blank`/`.ans-line-sm` space," conflating the two classes even though this same prompt's Section 3 correctly points at the shared stylesheet for everything else. Corrected to match `shared/Student_Packet_Style_Guide.md`'s now-explicit distinction (v1.12): `.blank` for a blank inside a sentence, `.ans-line-sm` only for a standalone full-width line below a question, never mid-sentence. Surfaced while fixing the same conflation as a live bug in several Writing packets - see `shared/Changelog.md`'s v1.12 (Style Guide) entry for the full cross-modality picture. Renamed `_v1.1.md` -> `_v1.2.md`; `Index.md` updated to match. |
| 2026-09-08 | — (retroactive, Advanced Set 1) | Retroactive cleanup of the 4 Advanced Module 1 Set 1 lessons, closing out the two Pending-work items the v2.10 entry above and an earlier pass had left open: (1) **Closing Transfer Check variety** - each lesson's "Closing Transfer Check" (both the `.md` and its packet's "Wrap It Up" wrapup) rewritten from the shared "something/one thing in the room" framing to a distinct concrete scenario per lesson: Lesson 1 (SagradaFamilia) a piece of clothing someone is wearing, Lesson 2 (ForgeAtDawn) a piece of furniture in the room, Lesson 3 (VinylComeback) a sound you can hear right now, Lesson 4 (PortlandHeadLight) something visible out the window - satisfying the new Closing Transfer Check Variety rule retroactively. (2) **`.masthead-meta` tag stack backfilled** onto Lessons 1-2's packets (`SagradaFamilia_Advanced_L1_Packet.html`, `ForgeAtDawn_Advanced_L2_Packet.html`), which predated it; each gets `Reading` / `Advanced` (Band only, no version code, since Lessons 1-2 are unversioned by design and are not retrofitted with one). (3) **Lesson 1's Fishbowl patched for Section 0.10 compliance**: `Lesson1_SagradaFamilia.md`'s single static inner-circle prompt expanded to 3 rotating prompts (reusing the two the packet's print formatting had already invented, so `.md` and packet now agree), and a general outer-circle tally/vocabulary-tracking task added for every outer-circle student, layered underneath the existing Level 4 and Foundation Support roles rather than replacing them. No packet change needed for this item since print already renders Fishbowl as simultaneous small groups (Section 2.9). (4) **Sagrada Família hook image recompressed**: `Lesson1_SagradaFamilia_Img_Hook.jpg` (750x750, 294 KB) was embedded as-is with no size limit ever documented anywhere in the repo; matched to the practice found in Listening/Speaking's `Backpack_Intermediate_L3_Packet.html` (source kept full-size, embedded copy recompressed) by re-embedding a quality-reduced JPEG at the same 750x750 dimensions, ~98 KB, visually unchanged at the `.photo{width:100%}` display size - the source `.jpg` file on disk is left at its original 294 KB. `Index.md`'s two now-resolved Pending-work bullets removed and its Module 1 Advanced Band section updated to match. |
| 2026-09-08 | v1.12 (print) | Section 2.12's v1.6 "table when there's a list, spotlight box when there's one" idiom rule retired: "Idioms to Know" always renders as the unnumbered `.spotlight-box`/`.idiom-item` "Phrase Spotlight" treatment, regardless of how many idioms it glosses. New rule: a transparent-chunk phrase (per the Lesson prompt's §0.4 transparent/opaque classification, already recorded in the lesson doc's "Idioms" item) gets a visible `.idiom-tag` "Slang" label appended after `.idiom-phrase`; an opaque idiom stays unlabeled, the default. Prompted by user feedback that led to discovering the count-based table rule wasn't being applied consistently, and a decision to simplify rather than sweep every packet onto it. Retroactive: all 8 Reading packets with an idiom section were hand-corrected in the same pass - `SagradaFamilia_Advanced_L1`/`ForgeAtDawn_Advanced_L2` (previously flagged "not yet swept" to the now-retired v1.6 rule), `VinylComeback_Advanced_L3` (found reusing `.vocab-list`/`.vrow` for its idioms, a third format never documented anywhere), and the 4 single-idiom packets (Kitchen, Yoyogi Park, Running Shoes, Wynwood Walls) plus Portland Head Light, all just gained the new `.idiom-tag` CSS and label where applicable. `shared/Student_Packet_Style_Guide.md` bumped to match (v1.10 -> v1.11); Listening/Speaking inherits via its existing "same rules as Passage Reading 2.12-2.14" pointer and its own 6 table-format packets were hand-corrected too - see that modality's own `Changelog.md`. Renamed `_v1.11.md` -> `_v1.12.md`. |
| 2026-09-08 | v2.10 (Lesson) | Section 0.8 Part A's Closing Transfer Check reworded: the per-Module illustrative sentence patterns (e.g. Describing's "describe something in this room by comparing it to something else") were being followed near-verbatim lesson after lesson instead of treated as one illustrative instance - confirmed across all 4 already-generated Advanced Module 1 Set 1 lessons (`Lesson1_SagradaFamilia.md`, `Lesson2_ForgeAtDawn.md`, `Lesson3_VinylComeback.md`, `Lesson4_PortlandHeadLight.md` and their packets all reuse the same "something in this room" setup, with Lessons 3-4 converging on near-identical final wording) and one Intermediate lesson one band down (`YoyogiPark_Intermediate_L2_Packet.html`). The patterns are now explicit `[pick a fresh object/scenario]` placeholders, with an instruction not to default to "something in this room." New "Closing Transfer Check Variety" rotation category added to the Lesson Variety & Structural Rotation Rule, requiring the object/scenario to differ across every lesson in a Set, checked against the Set's other lessons at generation time (all 4 lessons of a Set are typically drafted together, so this is checkable without the Rotation Log; a cross-Set/cross-Module fix via a new Rotation Log column was considered and deferred - see `Index.md`'s Pending work). Self-check item 19 added; Detailed Flow's restated Closing Transfer Check instruction cross-references the new rule. Also corrected a stale "Current version: v2.8" self-reference in the preamble, never updated since the v2.9 rename. Renamed `_v2.9.md` -> `_v2.10.md`. Going-forward only: the already-generated Advanced Set 1 lessons/packets that repeat "something in this room" are not retroactively rewritten - flagged as a known, pre-existing issue in `Index.md`'s Pending work instead. |
| 2026-09-08 | v1.11 (print) | Section 2.9 now explicitly forbids adding a coaching note under a sentence-starter stem (e.g. "practice it quietly with a partner first," a tally/tracking task to fill time before speaking, advice on when to use the line) - list only the stems themselves. User flagged a recurring instance of this pattern (a `class="tip"` list item directly under the lowest-star stem) in the Advanced "Forge at Dawn" packet and said they'd asked for it to stop before, but the prompt never actually said not to do it. Same boilerplate tip line, found identical or near-identical across 4 already-generated packets (Advanced: Forge at Dawn, Portland Head Light; Intermediate: Yoyogi Park, Wynwood Walls), was hand-removed from all 4 in the same pass, including each packet's now-unused `.stems .tip` CSS rule. Also fixed a stale "Current version: v1.9" self-reference left over from the same-day v1.10 rename below (never updated when that rename happened). Renamed `_v1.10.md` -> `_v1.11.md`. |
| 2026-09-08 | v2.9 (Lesson) / v1.10 (print) / v1.1 (Assessment print) | Lesson version-code shape changed program-wide from `S<Set>.<Lesson>.<Iteration>` to `<Module>.<Set>.<Lesson>.<Version>` (`shared/Program_Conventions.md` §G, v1.5), since Set numbering restarts per Module and the code needs the Module to stay unambiguous. All three prompts' version-code placeholder mentions updated to match (Lesson's metadata-line spec; Student Packet's masthead construction, two spots; Assessment Student Packet's "why no masthead-meta tag" aside). Renamed `_v2.8.md` -> `_v2.9.md`, `_v1.9.md` -> `_v1.10.md`, `_v1.md` -> `_v1.1.md`. Every already-assigned version code for this modality (lesson `.md`/packet `.html` mastheads, `Rotation_Log_Advanced.md`) rewritten to match in the same pass - see `shared/Changelog.md` for the full cross-modality note; `Index.md` updated throughout. |
| 2026-09-08 | v4.2 (Assessment) | `Module_<N>/` folder level inserted between `lessons/<band>/` and `Set_<N>/` program-wide (`shared/Program_Conventions.md` §D, v1.4), since Set numbering restarts at 1 for each new Module and would otherwise collide across Modules in the same Band. This prompt's two `lessons/<band>/Set_<N>/` mentions (Section 0's Set-number bullet, Section 2.2's save-location note) updated to `lessons/<band>/Module_<N>/Set_<N>/`. Renamed `_v4.1.md` -> `_v4.2.md`. Existing Module 1 content physically migrated into `Module_1/` across all three modalities in the same pass (Intermediate and Advanced Sets, both Lesson Plans); `Index.md` updated to match - see `shared/Changelog.md` for the full cross-modality note. |
| 2026-09-08 | v1.9 (print) | Section 2.13's `.ans-line-sm` description updated to match the shared CSS fix (`shared/Student_Packet_Style_Guide.md` v1.7 -> v1.8, see `shared/Changelog.md`): the class drops its `max-width: 320px` cap and is now full-width like `.ans-line`, distinguished only by its shorter height. Surfaced reviewing Set 1 Lesson 3 (Intermediate) "Running Shoes": Task A's Q1-4 answer lines (and the matching Task A/B lines in that lesson's Unit 3B) read as visibly narrower than the full-width lines two questions later in the same section, for no real reason. Going-forward only for the prompt; every already-generated packet across all three modalities that embeds `.ans-line-sm` was hand-corrected in the same pass - see `shared/Changelog.md` for the full file list. Renamed `_v1.8.md` -> `_v1.9.md`. |
| 2026-09-08 | Assessment Student Packet v1 (new) | New companion prompt, `Generate_Assessment_Student_Packet_Prompt_v1.md`, closing the gap noted in `Index.md`'s Pending work: Reading previously had no way to turn a completed Assessment `.md` into a student-facing handout, unlike Listening/Speaking's and Writing's own assessment-packet prompts. Mirrors the Student Print Formatting Prompt's translation principle and the base stylesheet, adapted for an assessment's four self-contained Task-Level sections (no taught Unit A/B) rather than a lesson's: source-lesson/Tests tags and the entire Scoring Guide (point values, rubrics) are stripped, each Task-Level section gets `page-break-before: always` for selective printing, and - a deliberate departure from Listening/Speaking's Speaking-Task-card checklist - rubric-scored items print as plain answer-line questions with no self-check-checklist substitute, matching how the lesson packet already treats extended-response items. No new CSS classes. First run produced `Set_1/Set1_Intermediate_Assessment_Packet.html` from the existing `Set1_Intermediate_Assessment.md`, in the same pass. |
| 2026-09-08 | v1.8 (print) | Two Section 2.13/2.12 fixes, plus a compliance correction against the existing Section 2.6 rule, found reviewing Set 1 Lesson 3 (Intermediate) "Check Your Understanding" Task A: (1) Section 2.13's `.ans-line-sm` spec changed from "inline after the question" to beginning on its own line below the question - the old spec let the short answer line pack onto the same row as the question whenever there was room, which reads as cramped and inconsistent. Fixed at the shared CSS layer (`shared/Student_Packet_Style_Guide.md` v1.6 -> v1.7, `display: inline-block` -> `display: block`; see `shared/Changelog.md`), not by rewording every call site. (2) Section 2.12's word-bank exception now also requires a word bank sit immediately before the question(s)/sentence frame it supplies words for, not after - previously undocumented. (3) The "Investigate the Text (Choose Your Task)" section title already violated Section 2.6's existing "no choose-your-own-adventure framing" rule; not a spec change, just a compliance bug. Going-forward only for the prompt; all 8 already-generated Set 1 packets (both bands, Lessons 1-4) were hand-corrected in this same pass for all three issues, except the word-bank reorder, which only applied to the 4 Intermediate packets (Kitchen, YoyogiPark, RunningShoes, WynwoodWalls) - the 4 Advanced packets' word bank already sits directly before the sentence-frame "starter" it actually supplies, not before the preceding open-response questions, so their existing placement already satisfied the new rule. Renamed `_v1.7.md` -> `_v1.8.md`. |
| 2026-09-07 | v1.7 (print) | Correction to the same-day masthead-meta version-stack change (below): the three-line stack (Reading / Band / version) is now two lines, with Band and version combined onto the second (`<Band> S<Set>.<Lesson>.<Iteration>`, e.g. `Advanced S1.1.0`) - the user's actual intent. Section 2.4 and the review checklist rewritten to match; see `shared/Student_Packet_Style_Guide.md` (v1.5 → v1.6) and `shared/Changelog.md`. Renamed `_v1.6.md` → `_v1.7.md`. |
| 2026-09-07 | v1.6 (print) | Follow-up on the idiom-table rule added earlier the same day (below): Section 2.12 now also specifies that each idiom row's phrase carries a plain "N. " number prefix, restarting at 1 - matching `.vocab-list .word`'s existing numbering convention, not previously written down for either list. Also, the shared `.idiom-list .irow .idiom-phrase` column width (`shared/Student_Packet_Style_Guide.md` v1.4 → v1.5) changed from `min-width: 160px` to a fixed `width: 180px`, since a floor let a long phrase grow past the column instead of wrapping, misaligning the gloss column row-to-row - see `shared/Changelog.md` for the full reasoning. Renamed `_v1.5.md` → `_v1.6.md`. |
| 2026-09-07 | v1.5 (print) | Section 2.12 extended: when a lesson glosses 2 or more idioms (no single spotlight idiom), the "Idioms to Know" section now renders as a table using the new `.idiom-list`/`.irow` classes (`shared/Student_Packet_Style_Guide.md` v1.3 → v1.4), mirroring the `.vocab-list` treatment "Words to Know" already gets, instead of stacked plain `.idiom-item` paragraphs. A single spotlight idiom is unchanged - still `.spotlight-box`. Listening/Speaking's own packet prompt inherits this automatically via its existing "same rules as Passage Reading 2.12-2.14" pointer; no edit needed there. Prompted by a review of Listening/Speaking's LostKitchen packet - see that modality's own Changelog and `shared/Changelog.md`. Going-forward only: of this modality's two already-generated packets with 2+ idioms (`SagradaFamilia_Advanced_L1`, `ForgeAtDawn_Advanced_L2`), neither was retroactively updated in this pass; noted as pending in `Index.md`. |
| 2026-09-07 | v2.8 / v1.4 (print) | New cross-modality **lesson version number** (`shared/Program_Conventions.md` new §G): `S<Set>.<Lesson>.<Iteration>`, distinct from this prompt family's own "vX.Y" numbering. Lesson Generation Prompt now requires a `**Version:**` field in the opening metadata line, alongside the existing `**Module:**`/`**Band:**`/`**Task Levels:**` fields. Student Print Formatting Prompt's Section 2.4 changed from a single modality-only `.masthead-tag` to a three-line `.masthead-meta` stack (Reading, Band, version code), reversing the v1.1 "no Level/Band" restriction now that Band and version are load-bearing, not decorative - see `shared/Student_Packet_Style_Guide.md` (v1.2 → v1.3) and `shared/Changelog.md`. Renamed `Generate_Lesson_Prompt_v2.7.md` → `_v2.8.md` and `Generate_Student_Packet_Prompt_v1.3.md` → `_v1.4.md`. Going-forward only: no already-generated lesson doc, packet, or Rotation Log row is retrofitted with a version. |
| 2026-09-06 | v1.3 (print) | Student Print Formatting Prompt's Section 2.4 rewritten: the opening masthead (never the Unit 1B masthead) now carries a `.masthead-tag` span reading exactly "Reading," per the new class added to `shared/Student_Packet_Style_Guide.md` §B (v1.0 → v1.1) - a plain, skill-based class-identifier for whoever is handling the printed packet, so a teacher can tell packets from different classes apart at a glance. Everything else the old "no kicker lines" rule banned (Name/Date field, subtitle line under any heading, footer note) is still banned; `.kicker`/`.sub` are not revived. No already-generated packet under `lessons/` has been retrofitted with the tag - only packets generated from v1.3 onward will carry it. |
| 2026-09-06 | v1.3 (Module Lesson-Plan) / v4.1 (Assessment) | **Sets concept introduced**, for parity/consistency with Listening/Speaking (which adopted it 2026-09-03 - see that modality's Changelog) rather than fixing a miscount as in Listening/Speaking's case: a Module/Band can now hold more than one Set over time - a fresh rotation of lessons for a semester where the module is retaught, without discarding or conflating it with what was taught before. A Set is however many lessons together cover a module's 8 instructional days; today that's 4 lessons, since Passage Reading's lesson is a 2-day cycle - so every Module/Band planned so far is retroactively that Module/Band's Set 1 (no Set 2 exists anywhere yet). Lesson numbering stays global within a Module/Band, continuing across Sets rather than restarting. Module Lesson-Plan Prompt bumped v1.2 → v1.3 (plans one Set at a time; adds a Set-number input; adds the cross-Set adjacency rule, one level narrower than the existing cross-module rule). Assessment Prompt bumped v4 → v4.1 (adds a Set-number input; a full-Set assessment now saves as `Set{N}_{Band}_Assessment.md` inside that Set's folder). `Rotation_Log.md` restructured to nest each Module/Band's existing table under a `### Set 1` subsection, matching Listening/Speaking's log shape - no row data changed, only the heading structure. Existing lesson/packet files physically migrated from a flat `lessons/<band>/` layout into `lessons/<band>/Set_1/Lesson_<N>_<Slug>/`, matching Listening/Speaking's folder convention exactly; `Index.md` updated to reference the new paths and, in the same edit, corrected several lesson/packet filenames that no longer matched what's on disk (a pre-existing mismatch, e.g. `Module 1 Lesson 1 - A Grandmother's Kitchen.md` → the real `Lesson1_Kitchen.md`) - homework/assessment/study-guide rows referencing files that don't exist on disk at all were left untouched, as a separate, larger known issue. |
| 2026-09-06 | v1.2 (print) | Section 3.5's base stylesheet extracted out of this prompt into `shared/Student_Packet_Style_Guide.md`, the new single source of truth for print styling across all three modalities (Listening/Speaking had been cross-referencing this section by prose with no mechanical link; Writing had no styling content at all). While extracting, the stylesheet was reformatted onto one combined convention and two fixes Listening/Speaking had been patching on top of its own copy were folded in at the source (see the shared doc's own Changelog). This prompt's Section 3 is now a short pointer; no behavior change to what a Passage Reading packet looks like. |
| 2026-09-01 | v2.7 | Module = 4 lessons (2-day cycle), not 8; Module 1 Intermediate's already-generated Lessons 1-4 confirmed as the complete module, and its cumulative assessment as the full end-of-module assessment. Module Lesson-Plan Prompt and Rotation Log corrected to match: it had been treating a module's 8 instructional days as "8 lessons," actually 16 days at 2 days/lesson; corrected to "4 lessons" (4 x 2 days = 8 days) throughout, with every "8 lessons/8 rows/Lesson 8" reference relabeled "4 lessons/4 rows/Lesson 4" (Module Lesson-Plan Prompt v1.1). Any module plan already approved under the old 8-lesson count should be read as having only its first 4 rows in scope. |
| 2026-09-01 | v2.7 | Section 0.10 corrected: v2.6's diagnosis was half right — a single static discussion prompt circulating for 30 minutes past an unscaffolded outer circle is genuinely redundant at this program's actual class size (8-12 students), but the fix is to scaffold the format properly, not retire it. Fishbowl and Concentric Circles reinstated as valid Phase 3 choices at 8-12 students (a concrete outer-circle task for every non-inner-circle student; 2-3 rotated discussion prompts per changeover), reversing the same-day v2.6 call to replace them with a small-group default. Module Lesson-Plan Prompt v1.2 realigned to match: all five Phase 3 protocols (Town Hall Role-Play, Fishbowl, Concentric Circles, Jigsaw Expert Panels, small-group discussion carousel) are equally valid rotation options again, not a tiered "defaults vs. large-class-only" split. |
| — | v1.2 (print) | Wording-only fix, no behavior change: Section 2.9's rationale for rendering Fishbowl/Concentric Circles as simultaneous small groups on the page was rewritten (the old "a class can't be usefully split into a talking inner circle and a listening outer circle" reasoning was accurate under the corrected-away v2.6 diagnosis but not under v2.7's live-Fishbowl model, which requires an active task for every outer-circle student). The printed output is unchanged — Fishbowl/Concentric Circles lessons still render as simultaneous small groups — only the stated reason why. |
| 2026-09-01 | v1.1 | Student Print Formatting Prompt corrected against two finished packets (Module 1 Lesson 1 Intermediate "A Grandmother's Kitchen," Module 1 Lesson 1 Advanced "La Sagrada Família"): (1) resolved a contradiction where Section 2.9 required 2-3 discussion prompts while Section 0.2 promised never to add new content — a v2.6+ source lesson now supplies 2-3 prompts directly, so the packet pulls them rather than inventing them, with a documented fallback for an older lesson that still has only one; (2) generalized Section 2.10's annotation key ("Read & Mark It Up") to be the standing default for every packet regardless of the source lesson's Phase 2 strategy, adding the "!" writer's-opinion mark only when task Levels include evaluative-language objectives; (3) documented a word-bank's light dashed top/bottom rule as the one allowed exception to "no border around a word bank"; (4) added Section 3.5, embedding the settled base stylesheet (fonts, color tokens, class treatments) as a required starting point so a fresh run reproduces the same look without an existing packet to copy from. |
| 2026-09-01 | — | Module 1 Lesson 1 (Advanced) raw doc and the full Advanced module plan, previously referenced but never actually saved/logged, are now saved and the Rotation Log has a proper Advanced Band entry. |
| 2026-08-31 | v1.0 | Student Print Formatting Prompt added (new Step 6): produces a print-ready black-and-white student handout from a completed lesson; tested against Module 1 Lesson 1 Advanced. |
| — | v2.6 | Fishbowl and Concentric Circles/Speed-Dating, as previously written, assumed a class large enough that a 5-6 student inner circle still leaves a meaningful outer circle of rotating observers; at this program's actual class size (roughly 8-12 students), an inner circle already contains most of the room, making one question circulating for the full 30-minute Phase 3 window redundant rather than differentiated. Phase 3 defaulted to simultaneous small groups (3-4 students, mixed task Level) across 2-3 distinct discussion prompts instead; Fishbowl/Concentric Circles reserved for genuinely large classes (16+). Also resolved a standing gap with the Student Print Formatting Prompt, which had independently reached the same small-groups conclusion but had to invent discussion prompts on the spot. Self-check item 18 added. (Reversed in v2.7 above.) |
| 2026-08-30 | v2.5 | All five prompts synced to v2.5; Module Lesson-Plan now reality-grounded with a band-conditioned topic rule. Module 1 Intermediate plan approved and logged, Lessons 1-4 + homework + cumulative assessment + 4 study guides generated; Module 1 Advanced plan approved, Lesson 1 generated. |
| 2026-08-30 | v2.5 | Section 0.1's tier model replaced: the old model served every band with exactly three tiers (Level A/B/C) mapped somewhere within that band's own two levels; the new model gives each band a fixed table of task Levels pulled from the real 1-8 Level scale (three for Beginner, four each for Intermediate/Advanced/Proficient), each anchored to an actual `learningobjectives.csv` row rather than an invented curve. A lesson request now needs only a Module and a Band — a specific Level is no longer part of the request. Every "Level A/B/C" reference in the document replaced with plain language. Self-check items 6, 7, 14 reworded; new item 17 added. Document and lesson type renamed from "Reading Lesson Generation Prompt" to "Passage Reading Lesson Generation Prompt" to distinguish it from the planned, not-yet-written Novel Reading Lesson type (naming only, no rule change). |
| — | v2.5 (addendum, no version bump) | Section 0.6's genre bank expanded with 6 new genres (text-message/chat thread and product listing for Intermediate; social media post thread and advice-column letter-with-response for Advanced; podcast transcript excerpt and historical primary-source-style document for Proficient), each with a matching formatting-convention sentence. Reading Execution Rules expanded with 3 new strategies (Think-Aloud Modeling, Say Something, Close Reading with Annotation). Grows both banks without changing any existing entry, ceiling, or self-check item. |
| — | v2.4 | Section 0.9 added, in two parts: Part A requires every anchor text's paragraphs labeled with sequential capital letters (A, B, C...) so they can be referenced precisely across STOP & CHECK questions, Fact Finders, and the Collaborative Evidence Matrix, instead of by number. Part B adds textbook-style contextual footnotes on a real person/place/reference the text names but doesn't explain, distinct from vocabulary pre-teaching and the Idiom/Slang Spotlight. Self-check items 15-16 added. |
| — | v2.3 | Section 0.8 Part A's closing mechanism corrected: the v2.2 draft closed Day 2 with a thumbs-up/sideways/down self-report against a can-do statement, which is unreliable evidence since a whole-class visible confidence vote tends to converge on "yes" regardless of whether the skill actually transferred. Replaced with a Closing Transfer Check: every student produces one instance of the skill on something new, out loud to a partner, and the teacher cold-calls two or three pairs to share — evidence is a demonstration, not a declaration. Self-check item 13 reworded to match. |
| — | v2.2 | Section 0.8 added, in two parts. Part A (Skill Spotlight) requires the lesson to name the actual transferable skill in plain student-facing language at the start of Day 1 and close the loop at the end of Day 2. Part B (Differentiated Participation) requires Phase 3's oral protocol to give the lesson's lowest task level a genuinely different participation mode, not just easier sentence stems inside the same live-debate format, and adds a Foundation Support layer for students functioning below that floor. Self-check items 13-14 added. |
| — | v2.1 | Section 0.7 added: requires at least one genuine, board-dependent activity per day (something co-constructed live by the class, not a restatement of material already on a handout or slide). Self-check item 12 added to enforce it. |
| — | Assessment v4 | The highest task Level's cross-text synthesis requirement (inherited unchanged from v1) is now scoped explicitly: a cumulative assessment (e.g. end-of-module, spanning all lessons) generates a second new passage so the highest task Level has two fresh passages to genuinely compare; a per-lesson assessment drops cross-text synthesis for the highest task Level and substitutes extended craft/technique reasoning within the one passage in scope. Self-check item 13 added. Also (no version bump): Section 0.3 added explicit source-lesson tags and CSV-objective citations per item, for traceability and so a lesson can be cleanly dropped from an assessment's scope; Section 1.1 restructured around per-lesson blocks (one new passage per lesson) with an explicit removal procedure (1.1a). |

Dates marked `—` predate the earliest date recorded elsewhere in this log (2026-08-30) but were never
themselves dated in the source material.
