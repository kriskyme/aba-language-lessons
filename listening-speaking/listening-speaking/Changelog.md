# Listening/Speaking Changelog

| Date | Version | Summary |
| --- | --- | --- |
| 2026-09-09 | v2.3 (Lesson) / v2.1 (Assessment) | One Output clause each: any embedded image is cited in the `.md` and added as a row to the Set's `Set<N>_<Band>_Image_Credits.md` in the same pass, and the packet prints no credit - see `shared/Changelog.md`'s Conventions v1.14 / Quality Standards v1.10 entry. |
| 2026-09-09 | v2.2 (Lesson) / v2.3 (print) | **A stem never contains its own answer** - see `shared/Changelog.md`'s Quality Standards v1.9 / Style Guide v2.18 entry, prompted by the user's KeyDeer classroom run. Lesson prompt: Day 1 Phase 4 puts every exemplar answer on an `**Answer note:**` line under its Level's task; self-check item 15. Packet prompt: §2.7 carries stems only and strips Answer notes, the Level 7 compare layout holds each source's own content and never the contrast; self-check item 5. Retroactive sweep of Advanced Set 1: `Lesson1_LostKitchen`, `Lesson2_LivingTextbooks`, `Lesson3_KeyDeer`, `Lesson4_GlassBender` (`.md` Phase 4 parentheticals moved to Answer notes, version codes bumped to `1.1.N.1`) and their four packets plus the `Set_1_T` LostKitchen fork (`.md` and packet, `1.1T.1.5`): Task B-C stems stripped, Task D compare rows rewritten as source content. Proficient `Lesson1_LowerNinthWard` still carries the pattern - logged in `Index.md` Pending work. |
| 2026-09-09 | — | Photo credits move out of packets into one `Set<N>_<Band>_Image_Credits.md` per Set - see `shared/Changelog.md`'s Conventions v1.13 / Style Guide v2.17 entry. Created `lessons/intermediate/Module_1/Set_1/Set1_Intermediate_Image_Credits.md` (19 images across NewBakery, PortoFoodTour, Backpack, GreatGrandmother); the four packets' credit clauses stripped and their `.pic-options` credit captions removed. |
| 2026-09-09 | — (retroactive, GreatGrandmother) | Three choice images embedded from Wikimedia Commons per `shared/Program_Conventions.md` §I (a smiling elderly Takayama market seller CC BY-SA 3.0, a high-school teacher CC BY 4.0, an FC Cincinnati team photo CC BY 2.0) into Day 1 Phase 4's Level 2 "who is this story about?" item, closing the last empty `.pic-box` in Intermediate Set 1's four lessons; `.md` citation lines and packet caption credit added. |
| 2026-09-09 | — (retroactive, NewBakery + PortoFoodTour) | Twelve images embedded from Wikimedia Commons per `shared/Program_Conventions.md` §I, closing both packets' §D8 photo notes and empty picture boxes; `.md` citation lines and packet caption credits added; seven hand-supplied raw files deleted. Porto's "old grocery store" picture is A Pérola do Bolhão, the real 1896 shop from the video. Both packets also received the §B `.photo` rule and the v2.16 `.pic-box` block. |
| 2026-09-09 | — (retroactive, Backpack) | All four hand-supplied images replaced with cited Wikimedia Commons files per `shared/Program_Conventions.md` §I (hook: hip-belt crop of a CC0 backpack photo; choices: the same backpack, a Dutch bicycle CC BY-SA 4.0, a camping tent CC0); `.md` citation lines, packet caption credits, `.webp` assets renamed to `.jpg`. |
| 2026-09-09 | — | Backpack packet's local `.photo` rule replaced by the shared §B rule - see `shared/Changelog.md`'s Style Guide v2.13 entry. |
| 2026-09-09 | v2.2 (print) | Phrase Spotlight label retired - see `shared/Changelog.md`'s Style Guide v2.12 entry. §2.13's one mention dropped; the `Slang` tag stripped from `LostKitchen_Advanced_L1` (both `Set_1` and `Set_1_T`) and `LivingTextbooks_Advanced_L2`, and the dead `.idiom-tag` CSS rule removed from those plus GlassBender, KeyDeer, and LowerNinthWard. |
| 2026-09-08 | v2.1 (Lesson) / v2.1 (Module Lesson-Plan) | **Fixed-output Levels rotate task shape** (`shared/Changelog.md`'s Quality Standards v1.6 entry). Lesson prompt: new 0.6 shape bank and slot assignment for Levels 1-2 with the once-per-day cap; Day 1 Phase 2 and Phase 4 and Day 2 Phase 2 lines cite it; self-check item 14. Module Lesson-Plan: the Task-Level basis names the shape per slot for a fixed-output Level; self-check adds no-shape-in-the-same-slot. |
| 2026-09-08 | v2.1 (print) / v2.1 (Assessment print) | Self-contained rule applied - see `shared/Changelog.md`'s Quality Standards v1.1 entry. Picture-based items embed their real images in `.pic-options`; "real placeholder boxes" is no longer an acceptable rendering, and a fixed-frame item whose images cannot be embedded is rewritten around the student's own object. The Lesson prompt needed no edit (its Foundation Support line names no cards). Renamed `_v2.0.md` -> `_v2.1.md`, `_v2.md` -> `_v2.1.md`; `Index.md` updated. |
| 2026-09-08 | v2 (Module Lesson-Plan) | Converted to the shared-layer structure - see `shared/Changelog.md`'s 2026-09-08 entry. Section 0's restated definition of a Rotation Log, its read/append mechanics, and the cross-Set and cross-Module rules ("same lighter touch as Passage Reading's log, for the same reason") now point to Conventions §F; the prompt keeps its column list and log template. The 2026-09-03 8-lesson correction note moved to this Changelog's history; "same four-option banks as Passage Reading" now points to the Lesson prompt's own 0.4 banks. 152 lines -> 81. Renamed `_v1.md` -> `_v2.md`; `Index.md` updated (file table, workflow Step 1). |
| 2026-09-08 | v2 (Assessment) / v2 (Assessment print) | Both converted to the shared-layer structure - see `shared/Changelog.md`'s 2026-09-08 entry. Assessment: the distractor-word rule (A.1, A.4 item 8), the near-miss multiple-choice engineering standard, the balanced-duration principle, and Respectful Tiers now live in Quality Standards §C/§B and are pointed to (they were this modality's contribution to the program; Reading and Writing now inherit them); A.4 and B.5 are "run Quality Standards §F, then these 8 / 6 items"; the "Open items for this v1" section and the "per user direction 2026-09-04" narration removed, with the two genuinely open policy items (swap approval, gradebook conversion) kept as one line each. 315 lines -> 179. Assessment print: Section 1's shared rows, 2.4/2.4a, the multiple-choice and placement rules, Section 4, and most of Section 5 point to Style Guide §E/§F/§G/§I; `.checklist` moved to Style Guide §H.4 as a shared assessment class (Writing's packets define the identical rule). 200 lines -> 102. Renamed `_v1.md` -> `_v2.md` (both); `Index.md` updated (file table, workflow Step 3). |
| 2026-09-08 | v2.0 (print) | Converted to the shared-layer structure - see `shared/Changelog.md`'s 2026-09-08 entry. The 143-line delta CSS block in Section 3 moved verbatim to `shared/Student_Packet_Style_Guide.md` §H.1 (this was the only packet prompt still inlining a stylesheet); Section 4's `.md`-is-source-of-truth regeneration rule moved to Style Guide §G, where every modality now inherits it; 2.4a's every-question-numbered rule, 2.7's word-bank placement, 2.10's starter-box placement, and the multiple-choice and picture-placeholder layout rules moved to Style Guide §F; Section 1's six modality-neutral translation rows to §E. The six TOEFL sub-sections (2.6a, 2.7a, 2.8a, 2.10a, 2.10b, 2.11a) and the TOEFL translation rows moved to `Generate_TOEFL_Track_Tier_Prompt_v1.md` Section 2, together with the 16-line dated CSS provenance note and the ten dated heading parentheticals, which go to this Changelog's history. Every "same as Passage Reading 2.x" cross-reference replaced by a pointer to the shared section. What remains is Listening/Speaking-specific, renumbered 2.1-2.13 with no lettered sub-sections. Section 5 is now "run Style Guide §I, then these 10 items." 582 lines -> 182. Renamed `_v1.10.md` -> `_v2.0.md`; `Index.md` updated. |
| 2026-09-08 | v2.0 (Lesson) / v1 (TOEFL Track Tier, new) | Converted to the shared-layer structure - see `shared/Changelog.md`'s 2026-09-08 Quality Standards entry for the cross-modality picture. Every modality-neutral rule this prompt carried (balanced time-on-task, item distinctness, Respectful Tiers, differentiated participation and Foundation Support, board-dependent moments, demonstration-only Closing Transfer Check, adjacency rotation, the `.md`-is-source-of-truth regeneration rule, no em-dashes, the metadata line) now lives in `shared/Generation_Quality_Standards.md` or `shared/Student_Packet_Style_Guide.md` §G and is pointed to, not restated. **Section 0.6 (TOEFL Track Tier, 137 lines) and the nine inline "if a TOEFL Track Tier is being generated" bullets extracted** into the new `Generate_TOEFL_Track_Tier_Prompt_v1.md`, together with the packet prompt's six TOEFL sub-sections (2.6a/2.7a/2.8a/2.10a/2.10b/2.11a, moved in the packet prompt's own v2.0 pass), mirroring Passage Reading's separate TOEFL companion; the new prompt's Section 3 self-check replaces the old 18-line self-check item 19. Content of the mechanism is unchanged. Preamble cut from 78 lines (including the v1.2-v1.7 TOEFL history and a "this is a first draft (v1)" line that was false by v1.10) to a lesson-type description, paste bundle, Changelog pointer, inputs, the three-artifact output, and scope; the standing operational rules it listed (soft runtime target, invented read-aloud closing script, complete inline frame instructions, three artifacts in order) moved into 0.2, 0.3, 0.5, and the phase text where they apply. Surviving 8-day-model text ("Days 1-4 / Days 5-8", "Day 3-8 tasks", the "Corrected 2026-09-03" Unit Architecture narrative) removed; Section 0 renumbered so the self-check is 0.5 in order (0.4 rotation banks, no 0.6); 0.2's ladder reformatted to one block per Level. Self-check is now "run Quality Standards §F, then these 13 Listening/Speaking items." 635 lines -> 317 (+159 in the TOEFL prompt). Renamed `_v1.10.md` -> `_v2.0.md`; `Index.md` updated (file table rows for both prompts, workflow Steps 2 and 4). |
| 2026-09-08 | — | Passage Reading retired its v1.6 idiom-table rule (`Generate_Student_Packet_Prompt` v1.11 -> v1.12): "Idioms to Know" always renders as the unnumbered `.spotlight-box`/`.idiom-item` "Phrase Spotlight" treatment now, regardless of idiom count, and a transparent-chunk phrase gets a visible `.idiom-tag` "Slang" label (opaque idioms stay unlabeled, the default) - see Passage Reading's own `Changelog.md`. This modality's own prompt needs no direct edit, inheriting the rule via Section 2.13's existing "same rules as Passage Reading 2.12-2.14" pointer, the same way it never needed editing for the original table rule. All 6 packets that had picked up the `.idiom-list`/`.irow` table were hand-reverted in the same pass: `GlassBender_Advanced_L4`, `KeyDeer_Advanced_L3`, `LivingTextbooks_Advanced_L2`, `LowerNinthWard_Proficient_L1`, and both `LostKitchen_Advanced_L1` copies (`Module_1/Set_1` and the TOEFL-track `Module_1/Set_1_T`). Each also gained the `.idiom-tag` CSS rule; per each lesson's own transparent/opaque classification, `LivingTextbooks`'s two idioms and `LostKitchen`'s "in the middle of nowhere" are tagged "Slang," `LostKitchen`'s "keep my feet on the ground" and `LowerNinthWard`'s "mixed blessing" stay unlabeled (opaque), and `GlassBender`/`KeyDeer`'s phrases stay unlabeled since neither lesson doc recorded a transparent/opaque classification for them. See `shared/Changelog.md` and `Index.md` for the same note. |
| 2026-09-08 | v1.10 (Lesson) / v1.10 (print) | Per user review of the Great-Grandmother packet (Intermediate Set 1 Lesson 4), four fixes hand-applied to that lesson's `.md`/`.html` and then generalized into the prompts so future lessons don't repeat them. **(1)** Removed a trivial 2-item Task C matching question ("match each time period to what it meant for English in Japan") that duplicated a comparison a free-response item in the same task Level already required - `Generate_Lesson_Prompt_v1.9.md` → `_v1.10.md` Phase 4 gains an item-distinctness rule (no item may test what another item in the same Level's merged set already tests, under a different format); self-check gains item 17 (existing items 17-18 renumbered 18-19). **(2)** Task A's instruction "Practice saying both frames above to a partner" referenced a `.starter` box that actually rendered *after* the instruction - reordered so it renders first; `Generate_Student_Packet_Prompt_v1.9.md` → `_v1.10.md` Section 2.10 gains a starter-box placement rule (must render before any instruction referencing it), extending the same reasoning 2.7 already applies to word-bank placement. **(3)** The Wrap It Up section's closing heading "Finish the K-W-L Chart" renamed to "Finish the Chart" (a heading naming a teacher-planning protocol/organizer type is jargon a student doesn't need); Section 2.12 gains a closing-loop-heading rule to the same effect, scoped to headings only (2.4a's existing inline "the K-W-L chart" reference example is unchanged). **(4)** `.vocab-list .word`'s alignment bug - see `shared/Changelog.md`'s matching v1.10 (Style Guide) entry for the fix and the retroactive sweep across every already-generated packet; no direct edit needed in either prompt here, inherited via the shared stylesheet. Renamed `_v1.9.md` → `_v1.10.md` (both files); both files' stale "Current version" preamble pointers (which had drifted to v1.7/v1.8) corrected to v1.10 in the same pass. **Related correction:** the Great-Grandmother lesson's own version code, which should have been bumped when fixes (1)-(3) changed its content, is now `1.1.4.1` (was still `1.1.4.0`) - updated in `Lesson4_GreatGrandmother.md`, its packet masthead, `Rotation_Log_Intermediate.md`, and `Index.md`. |
| 2026-09-08 | v1.9 (Lesson) / v1.9 (print) | Lesson version-code shape changed program-wide from `S<Set>.<Lesson>.<Iteration>` to `<Module>.<Set>.<Lesson>.<Version>` (`shared/Program_Conventions.md` §G, v1.5), since Set numbering restarts per Module and the code needs the Module to stay unambiguous. The TOEFL Track Tier variant-track letter suffix stays, now on the `<Set>` component (`<Module>.<Set>T.<Lesson>.<Version>`, e.g. `1.1T.1.4`). Both prompts' version-code placeholder mentions updated (Lesson's metadata-line spec, "Where this lives" note, self-check question; Student Packet's masthead construction, TOEFL-variant note). Renamed `_v1.8.md` -> `_v1.9.md` (both files). Every already-assigned version code for this modality (all four Bands' lesson `.md`/packet `.html` mastheads, the `Set_1_T` fork's `.md`/`.html`/`_TOEFL_Homework.html`, every `Rotation_Log_<Band>.md`) rewritten to match in the same pass - see `shared/Changelog.md` for the full cross-modality note; `Index.md` updated throughout. |
| 2026-09-08 | v1.8 (Lesson) | `Module_<N>/` folder level inserted between `lessons/<band>/` and `Set_<N>/` program-wide (`shared/Program_Conventions.md` §D, v1.4), since Set numbering restarts at 1 for each new Module and would otherwise collide across Modules in the same Band. Section 0.6's "Where this lives" note updated: a TOEFL Track Tier fork's path is now `lessons/<band>/Module_<N>/Set_<N>_T/`. Renamed `_v1.7.md` -> `_v1.8.md`. Existing Module 1 content physically migrated into `Module_1/` across all four Bands (including the `Set_1_T/` fork) in the same pass; `Index.md` and every `Rotation_Log_<Band>.md` that cites a lesson path updated to match - see `shared/Changelog.md` for the full cross-modality note. |
| 2026-09-08 | v1 | `Generate_Assessment_Prompt_v1.md` Section A.1 item 3 gains a distractor-word rule, per user direction: every word bank or matching list must include 1-2 extra already-taught words beyond what its blanks/matches actually require (scaled to bank size - a 3-word bank gets +1, a 5-word bank gets +2), so a student can't finish the last blank by elimination alone; A.4 self-check gained a matching item 8 (existing items 8-11 renumbered 9-12). Also, per user direction, both existing Module 1 Set 1 assessments' Listening Test (Part A) grew from a flat "approx. 40 MIN" toward the prompt's own upper range, "approx. 45 MIN" - not just relabeled, but lengthened with one genuine new item per clip block per band, each grounded in an already-cited clip segment no existing item tested, verified against the real transcript before wording was finalized. **Advanced** (`Set1_Advanced_Assessment.md`/`.html`): Level 4 Item 1's word bank gains `viable` (Key Deer, L3) as a near-miss distractor for the "dwindle" blank; new Level 6 item (Recognize Examples, Fact vs. Evaluative comment) added from Clip 1 Segment 4 (Alex and Lori Jenson); new Level 5 item (Stated vs. Implied Opinion) added from Clip 2 Segment 2 (Emily Fish's logbook entries). New point totals: L4 11, L5 9, L6 8, L7 8 (was 11/7/6/8); structure now Setup 5 + Clip 1 20 + Clip 2 15 + Wrap 5 = 45. **Intermediate** (`Set1_Intermediate_Assessment.md`/`.html`): Level 2 Item 1's matching bank gains `bakery` (New Bakery, L1) as an unmatched distractor; Level 3 Item 1's matching bank gains `sardine` (Porto Food Tour, L2) and `waterproof` (Backpack, L3); Level 4 Item 1's fill-in-blank bank gains `variety` (already pooled elsewhere in this same assessment) as a grammatically-plausible near-miss for the capacity blank; new Level 4 item (Main Ideas/Gist) added from Clip 1 Segment 2 (Golubic's pandemic-era "mission to heal people with laughter" and childhood photo); new Level 2 item (Main Ideas/Gist plus a supporting-detail check) added from Clip 2 (the Pompeii bathhouse discovery and its caldarium/cold-water-tub rooms). New point totals: L2 10, L3 9, L4 9, L5 8 (was 8/9/7/8); structure now Setup 5 + Clip 1 17 + Clip 2 18 + Wrap 5 = 45. See `Index.md` and both `Rotation_Log_Advanced.md`/`Rotation_Log_Intermediate.md` for the same note. |
| 2026-09-07 | v1.7 (Lesson) / v1.8 (print) | **Follow-up, same day, on the homework-file-split entry below:** the teacher offered a real, better delivery mechanism for D's take-home Speaking practice - record (or otherwise produce) real audio of the sentences/questions, post it to the class's Teams, and have students listen once and record their spoken response there as the homework submission, instead of asking a family member to read the items aloud from a printed page. This reuses `Generate_Assessment_Prompt_v1.md` Part B's own established "Teams Speaking Progress recording" mechanism (already used for Beginner/Intermediate Speaking assessment) rather than inventing new terminology, and is strictly more authentic to the real test (single real audio playback, not a text stand-in read by whoever happens to be home). `Generate_Lesson_Prompt_v1.6.md` → `_v1.7.md` Section 0.6D's "At home" bullet rewritten around this mechanism; "Heard, not read" updated (D's content is now genuinely heard via real recorded audio, the strongest version of that rule yet); the "Three artifacts" note updated to say the homework file is teacher-only. `Generate_Student_Packet_Prompt_v1.7.md` → `_v1.8.md` Section 2.10b rewritten: the file's `.reader-warn` label changes from "For your reading partner only" to "Teacher use only - recording script and scoring guide," with instructions to record per-item audio, post to Teams, and score submissions - no new CSS classes needed, same `.reader-copy` structure. The worked example, `lessons/advanced/Set_1_T/Lesson_1_LostKitchen/`, updated to match: `LostKitchen_Advanced_L1_TOEFL_Homework.html`'s masthead, objective paragraph, and warning box reframed as teacher-only instructions (content - the sentences, questions, time windows, scoring guides - unchanged, since it's still exactly what the teacher needs to record from and score against); the lesson `.md`'s Section 0.6D description and self-check updated to match; version bumped `S1T.1.3` → `S1T.1.4`. The main packet (`LostKitchen_Advanced_L1_Packet.html`) needed no change - its Task D (TOEFL) box already made no reference to the take-home mechanism, from the prior turn's fix. No `shared/` changes needed. See `Index.md` (file table, Advanced Band progress) and `Rotation_Log_Advanced.md`. |
| 2026-09-07 | v1 | **Follow-up, same day, on the Part B redesign entry below:** three small fixes to the Speaking Task cards, per user direction, after seeing them rendered. **(1)** the rubric shown to students is replaced with a plain self-check checklist: `Generate_Assessment_Student_Packet_Prompt_v1.md` no longer prints Part B's rubric as a `.rubric-table` (the teacher's Not yet/Developing/Meets scoring instrument); it now derives one checklist item per criterion from that rubric's Meets column, reworded directly to the student (e.g. "Both rooms described using the fixed frame"), rendered via a new plainer `.checklist` class (checkbox glyph, no table/borders) that replaces `.rubric-table`. **(2)** the "Submit by: [teacher-set date], using Teams Speaking Progress" line is dropped from every card entirely - platform name, submit-by date, and any recording-mechanism note are all teacher-communicated logistics now, not part of this document. **(3)** Level 2 item 3 in the Listening Test ("Complete: ...") reworded to "Fill in the blanks: ..." to match every other fill-in-the-blank item's phrasing in the document - the only place "Complete:" appeared. `Generate_Assessment_Prompt_v1.md` B.4 updated to describe the checklist-derivation (the rubric itself is unchanged, still teacher-only). `lessons/intermediate/Set_1/Set1_Intermediate_Assessment.md` and its packet regenerated to match: all 4 Speaking Task cards now show a 3-item checklist instead of a rubric table, with no submission line; the Listening Test's one "Complete:" item reworded. `Set1_Advanced_Assessment.md`/`.html` still needs this fix too, folded into the existing Advanced pending-work note in `Index.md`. |
| 2026-09-07 | v1.6 (Lesson) / v1.7 (print) | **Follow-up, same day, on the `Task D (T)`→`(TOEFL)` relabeling entry below:** per user direction, D's take-home content (the real 7 Listen and Repeat sentences, 4 Take an Interview questions, response-time windows, both TOEFL scoring guides) is moved out of the main student packet entirely, into its own separate file - `<TopicSlug>_<Level>_L<n>_TOEFL_Homework.html`, generated alongside the main packet (same pass, same folder). `Generate_Lesson_Prompt_v1.5.md` → `_v1.6.md` Section 0.6D and "Where this lives" updated to describe three artifacts per TOEFL Track Tier lesson (was two); item ID/self-check updated to match. `Generate_Student_Packet_Prompt_v1.6.md` → `_v1.7.md` Section 2.10b rewritten from "append a page to the end of the packet" to "generate a separate self-contained file, reusing the same base stylesheet/masthead convention"; Section 0/4 (scope, workflow) updated to say this prompt now produces two output files when a TOEFL Track Tier is present. Task D (TOEFL)'s in-class rehearsal (2.10a) also cleaned up per user direction: the pointer line to "your take-home page" is gone (the homework file is a wholly separate document, never cross-referenced from the main packet - same convention already used for the standalone transcript file); "no clock needed, just practice the shape of the real thing" is dropped; and the vague "take turns with a partner" instruction is replaced with an explicit, up-front reader/listener instruction ("one person reads, the other listens and repeats or answers - switch roles halfway through"), stated once before both practice lists rather than buried in the scenario text. The worked example, `lessons/advanced/Set_1_T/Lesson_1_LostKitchen/`, updated to match: the trailing `.reader-copy` section removed from `LostKitchen_Advanced_L1_Packet.html` (along with its now-unused CSS) and rebuilt as the new standalone `LostKitchen_Advanced_L1_TOEFL_Homework.html`; Task D (TOEFL)'s in-class box reworded in both the `.md` and `.html`; version bumped `S1T.1.2` → `S1T.1.3`. No `shared/` changes needed. See `Index.md` (file table, Advanced Band progress) and `Rotation_Log_Advanced.md`. |
| 2026-09-07 | (no version bump - copy edit) | **Follow-up, same day, on the `Task D (T)` relabeling entry below:** per user direction, the letter-only `(T)` suffix on the TOEFL capstone label is spelled out as `(TOEFL)` - `Task D (T)` → `Task D (TOEFL)` - and the inline `(TOEFL Track)` tag used on the two B touchpoints (the Day 1 Phase 5 extra question, the Day 2 Phase 4 reframed Discuss It prompt) and the packet's `<title>` are shortened to `(TOEFL)` to match. Pure string rename across `Generate_Lesson_Prompt_v1.5.md`, `Generate_Student_Packet_Prompt_v1.6.md`, `Index.md`, `Rotation_Log_Advanced.md`, and the `Set_1_T/Lesson_1_LostKitchen` worked example (both `.md` and `.html`) - no structural, content, or version-code change, so no iteration bump on `S1T.1.2`. The overall mechanism's own name, "TOEFL Track Tier" (Section 0.6), is unchanged - only these two specific inline labels were renamed. |
| 2026-09-07 | v1 | **Follow-up, same day, on the Part A redesign entry below:** Part B (Speaking Assessment) redesigned in `Generate_Assessment_Prompt_v1.md` and `Generate_Assessment_Student_Packet_Prompt_v1.md`, per user direction, after reviewing the Speaking Task section of the regenerated Intermediate Set 1 packet. Three changes: **(1)** each task Level's prompt (B.3) now states concrete, countable content requirements (feature counts, comparison counts, connector counts) instead of only a verb plus a target length - the previous "in your own words" / "extended, organized description" phrasing left longer target lengths (60-120 sec) feeling open-ended with no sense of how much content was actually expected. **(2)** the rubric (B.4) is now shown directly to students, verbatim, on their own task card in the student-facing packet - a reversal of the packet prompt's prior "never show a rubric" rule, which now applies to Part A's answer keys/point values/pass notes only. **(3)** the generic top-of-section paragraph telling students how to record, that re-recording is allowed, and that a live-delivery option exists is dropped entirely from the student packet - real information, but redundant boilerplate the packet no longer needs to carry (a teacher-communicated policy instead). `Generate_Assessment_Student_Packet_Prompt_v1.md` Section 2.7 rewritten accordingly; new `.rubric-table` delta class added (bordered/shaded-header, the same visual pattern the retired `.notes-table` used). `lessons/intermediate/Set_1/Set1_Intermediate_Assessment.md` and its packet regenerated to match: all 4 Levels' B.3 prompts gained explicit counts, and each Speaking Task card in the packet now shows its Level's full 3-criterion rubric table with no top recording-instructions paragraph. `Set1_Advanced_Assessment.md`/`.html` still needs the same Part B fix, added to the existing Advanced pending-work note in `Index.md`. |
| 2026-09-07 | v1.5 (Lesson) / v1.6 (print) | **Follow-up, same day, on the TOEFL Track Tier redesign entry below:** review of that redesign surfaced a real flaw in touchpoint C (the Day 1 Phase 4 Listening capstone) plus a labeling request, both from the user. **Flaw:** C had TOEFL-track students listen to a freshly-authored "Academic Talk" passage that a teacher or partner had to read aloud *separately, just for them*, while the rest of the class worked independently on their own Level's task - exactly the shared-classroom-timing problem D already solved for Speaking, just never applied to C. User's words: *"I don't want TOEFL track students to be listening to their own thing while the rest do something else."* **Fix:** `Generate_Lesson_Prompt_v1.4.md` → `_v1.5.md` Section 0.6C rewritten to drop the invented passage and task-format selection (Conversation/Announcement/Academic Talk) entirely - TOEFL-track students now answer TOEFL-question-type items (Main Idea, Factual, Inference, etc.) directly against the same real source every student already heard together in Phase 2, exactly like every other Level's task. Item-metadata TASK code `CONV/ANNC/ATALK` retired in favor of a single `LSN` code. **Labeling:** both capstones (C and D's in-class rehearsal) now render in the student packet as their own sibling **`Task D (T)`** block - the same `exercise-label`/star styling as the regular lettered tasks - instead of a `.section-label` "TOEFL Track option" line nested inside the regular task's block; C also drops its long explanatory paragraph, now that there's no separate passage/format to explain. `Generate_Student_Packet_Prompt_v1.5.md` → `_v1.6.md` Sections 2.7a/2.10a rewritten to match (2.1/2.6a/2.8a/2.10b/2.11a unaffected). No item-metadata or CSS additions needed - `Task D (T)` reuses the existing `.instr-line`/`.exercise-label`/`.stars` markup already used by every other lettered task. The worked example, `lessons/advanced/Set_1_T/Lesson_1_LostKitchen/`, updated in place (both `.md` and `.html`): the invented passage removed, its 4 questions rewritten to reference "the segment" and reground their answer-key explanations in facts/quotes already established elsewhere in the lesson (Segments 1-6, the target-vocabulary context sentences, the Phase 3/Day 2 Phase 1 quotes) rather than the now-deleted passage text; version bumped `S1T.1.1` → `S1T.1.2`. No `shared/` changes needed - the fork/version mechanic itself is unchanged. See `Index.md` (file table, Generation workflow, Advanced Band progress) and `Rotation_Log_Advanced.md`. |
| 2026-09-07 | v1 | **Follow-up, same day, on the vocabulary-pooling entry below:** `Generate_Assessment_Prompt_v1.md`'s Part A redesigned further, per user direction, beyond just where vocabulary comes from. Four changes: **(1)** the vocabulary review step is removed entirely - if the assessment tests Set-taught vocabulary, handing students a pre-teach/review list first defeats the point of testing retention, so vocabulary now appears only inside the graded items themselves (a matching or fill-in-the-blank item using the same pooled words). **(2)** the Play & Notes free-form notes-taking organizer is removed - it isn't itself gradable, so items are now answered immediately after the relevant clip plays instead. **(3)** every task Level's items become multiple choice, fill-in-the-blank, or matching only, no open-ended short-answer/extended-response at any Level; higher Levels (whose can-dos call for summarizing, critically assessing, or comparing framing) get a new format-engineering standard - plausible near-miss MC options or functional (not dictionary) matching - so the objective format still requires real judgment, not recall; Respectful Tiers still governs the lowest Level's item. **(4)** 2-3 shorter new clips become the default instead of one longer source, generalizing the two-source Level 7 pattern Advanced Set 1's assessment already used; all clips are heard by the whole class, and a Level's items may draw on any of them. A.0, A.1, A.2, A.3, and A.4 all updated; self-check grew from 8 to 11 items. Applied program-wide (all bands). `Generate_Assessment_Student_Packet_Prompt_v1.md` updated to match (Section 2.5 retired, `.notes-table`/`.vocab-list` dropped from its reused-class list, matching-item rendering guidance added). `lessons/intermediate/Set_1/Set1_Intermediate_Assessment.md` and its packet fully regenerated the same day: Clip 1 stays the already-verified HaHaHouse Museum source; Clip 2 is a newly-sourced VOA Learning English piece, "Researchers Uncover a Bathhouse Complex in Ancient Pompeii" (January 2025, runtime 3:17, within Section 0.2's target with no overshoot). Every item across Levels 2-5 rewritten as MC/fill-in-the-blank/matching, including a cross-clip framing-comparison matching item at Level 5. `Set1_Advanced_Assessment.md`/`.html` still uses the fully retired single-source/vocab-preteach/notes-organizer/open-ended shape and needs the same redesign - see `Index.md` Pending work. |
| 2026-09-07 | v1.4 (Lesson) / v1.5 (print) | **Follow-up, same day, on the TOEFL Track Tier entry below:** real feedback on that first embedded pass (`S1T.1.0`, one alternate task isolated to Day 1 Phase 4 and Day 2 Phase 2) was that it read as disconnected from the rest of the lesson - TOEFL-relevant *skills* should be practiced throughout the cycle, not necessarily via literal TOEFL test-question format at one spot. `Generate_Lesson_Prompt_v1.2.md` → `_v1.4.md` Section 0.6 redesigned around four touchpoint kinds: **(A) Framing connections** - a one-sentence "TOEFL Connection" note in Day 1 Phases 1 & 3 and Day 2 Phases 1 & 3, naming which real TOEFL question type/scoring criterion that phase's already-named skill maps to; teacher narration only, never printed in the student packet. **(B) In-class differentiated touchpoints** - a note-organizer tag column (Day 1 Phase 2), one extra transfer-check question (Day 1 Phase 5), and one reframed oral-output turn (Day 2 Phase 4); all opt-in but run on the class's existing shared pacing, no individual timing. **(C) The original Day 1 Phase 4 Listening capstone**, unchanged. **(D) The Day 2 Phase 2 Speaking capstone**, changed: real individually-timed TOEFL Speaking mechanics (8/10/12-sec Listen and Repeat windows, 45-sec Take an Interview windows) can't run live against one student while the rest of a mixed class works a different task with no spare adult to time them - so the timed, scored version moves to an independent/homework deliverable, and in-class Day 2 Phase 2 becomes an untimed paired rehearsal instead. `Generate_Student_Packet_Prompt_v1.4.md` → `_v1.5.md` adds matching rendering rules: Sections 2.6a, 2.8a, 2.11a for the B touchpoints; 2.10a rewritten for the untimed in-class rehearsal (no `.time-list`); new 2.10b for a separate, heavily-bordered take-home **"Partner/Family Reader Copy"** page (new `.reader-copy`/`.reader-warn` classes) - the one place the real timed sentences/questions/scoring guides are printed, a stated exception to "heard, not read" since no teacher is present at home to read them aloud. Item metadata (Section 0.6) extended to cover the B touchpoint that carries a correct answer (the Phase 5 extra question, tagged `XFER`). The worked example, `lessons/advanced/Set_1_T/Lesson_1_LostKitchen/`, was updated in place to the new design (both `.md` and `.html`), version bumped `S1T.1.0` → `S1T.1.1` per `shared/Program_Conventions.md` §G's iteration-on-substantive-revision rule. No change to the fork/version *mechanic* itself (`Set_<N>_T/`, `S<Set>T.<Lesson>.<Iteration>`) - `shared/Program_Conventions.md`/`shared/Changelog.md` need no further edit this pass. See `Index.md` (file table, Generation workflow Step 4, Advanced Band progress) and `Rotation_Log_Advanced.md`. |
| 2026-09-07 | v1 | `Generate_Assessment_Prompt_v1.md`'s Part A "Setup & Vocabulary" step reversed, per user direction: it now reviews 3-5 words already taught across the Set's 4 lessons (pooled across all 4, not limited to one) using each word's real taught definition, instead of pre-teaching new/unseen words from the assessment's own listening source. Rationale: the assessment should measure retention of what was actually taught, not exposure to more material; pooling across all 4 lessons (rather than testing only one lesson's words) keeps a student who missed a single lesson from losing the whole tested set, addressing the original design's absence-penalty concern the other way. The new-source requirement for the listening passage itself is unchanged - only the vocabulary pre-teach step moved. A.1 and A.4 (self-check) updated to match; self-check gained a new item verifying the vocabulary pool (existing item 7 renumbered 8). Applied program-wide (all bands), since this is a single shared Part A rule. `lessons/intermediate/Set_1/Set1_Intermediate_Assessment.md` and its packet regenerated the same day to match: Setup & Vocabulary now reviews **ingredient**, **organic** (Lesson 1), **variety** (Lesson 2), **capacity** (Lesson 3), and **challenge** (Lesson 4), replacing the retired new-source words (museum, negativity, interactive, embrace, tickle). `Set1_Advanced_Assessment.md`/`.html` still uses the retired approach and needs the same fix - see `Index.md` Pending work. |
| 2026-09-07 | v1.2 (Lesson) / v1.4 (print) | **Follow-up, same day, on the standalone TOEFL Practice Extension entry below:** per user direction, that approach is retired - `Generate_TOEFL_LS_Extension_Prompt_v1.md` and its worked-example output (`LostKitchen_Advanced_L1_TOEFL.md`/`_Packet.html`) are deleted. TOEFL practice is now embedded directly in the lesson instead of living in a separate companion document: `Generate_Lesson_Prompt_v1.1.md` → `_v1.2.md` adds Section 0.6, an optional, Advanced/Proficient-only, opt-in-per-Set "TOEFL Track Tier" - the band's highest task Level gains one additional, clearly labeled TOEFL-formatted alternate task in both Day 1 Phase 4 and Day 2 Phase 2, alongside (never replacing) its existing task, so one mixed class can differentiate by TOEFL interest as well as by ability. Content rules carried over from the retired prompt largely unchanged, with one correction: the Listening alternate cannot be a long verbatim excerpt the way Passage Reading's TOEFL extension excerpts its anchor text, since TOEFL's 35-250 word task lengths exceed this family's own one-to-two-sentence fair-use ceiling (Section 0.3 item 4) - it is instead an original passage faithful to the real source, reusing only quotes the lesson document already established elsewhere. `Generate_Student_Packet_Prompt_v1.3.md` → `_v1.4.md` adds Sections 2.7a/2.10a (how to render the alternate as a labeled sub-option inside the existing task-block) and two delta classes, `.mc-list`/`.mc-letter` and `.time-list`. **Where a TOEFL Track Tier variant lives:** since the alternate only applies to one Set's one tier, generating it forks that Set's lesson files into a sibling `Set_<N>_T/` folder (`shared/Program_Conventions.md` §D) rather than editing the existing `Set_<N>/` files in place, and its version code folds a `T` into the Set token - `S<Set>T.<Lesson>.<Iteration>` (e.g. `S1T.1.0`) - per a new note in §G (see `shared/Changelog.md`). First (and so far only) fork generated the same day: `lessons/advanced/Set_1_T/Lesson_1_LostKitchen/Lesson1_LostKitchen.md` and its matching packet, reusing the retired prompt's already-authored TOEFL passage/questions/answer key/sentences content rather than re-authoring it. See `Index.md` (file table, Generation workflow Step 4, Pending work) and `Rotation_Log_Advanced.md`. *(Superseded same-day by the row above: `Generate_Lesson_Prompt_v1.2.md` was itself renamed `_v1.3.md` for the unrelated standalone-transcript-file addition, which carries Section 0.6 forward unchanged.)* |
| 2026-09-07 | v1.3 | `Generate_Lesson_Prompt_v1.2.md` → `_v1.3.md`: new Section 0.3 item 7, **standalone transcript file**. Every lesson now also produces a `<Slug>_Transcript.md` file in the same `Lesson_<N>_<Slug>/` folder as the lesson `.md` and packet `.html`, holding the source's full real transcript/captions verbatim. Unlike the lesson `.md`'s own quoting (capped at item 4's one-to-two-sentence fair-use ceiling), this file is exempt from that ceiling - it's a teacher-only working reference, never printed in, derived into, or otherwise surfaced in the student packet, the same teacher-only status Section 0.6's TOEFL passage/answer key already has. Motivated by a real gap surfaced generating Beginner Lesson 1 (`WhatIsIt`): only short dialogue fragments of the real source could be confirmed through automated page-fetching, not a full verbatim script. If a genuine full transcript can't be retrieved, the file must say so plainly (what's missing, how the included content was verified) rather than silently passing off a partial fetch as complete or padding the gap with invented dialogue - the same honesty this family already required of segment-label/timestamp gaps. Workflow updated to a three-artifact sequence (transcript file first, then the lesson `.md`, then the packet `.html`); self-check gained item 17 (existing TOEFL item renumbered 17→18). Companion `Generate_Student_Packet_Prompt_v1.4.md`'s own "Companion to..." cross-reference line updated to point at `_v1.3.md`. **Going forward only**: the 9 lessons already generated (Beginner `WhatIsIt`; Intermediate `NewBakery`, `PortoFoodTour`, `Backpack`, `GreatGrandmother`; Advanced `LostKitchen`, `LivingTextbooks`, `KeyDeer`, `GlassBender`) don't have a transcript file yet - tracked in `Index.md`'s Pending work rather than backfilled in this pass, since each would need its source re-fetched fresh, not just re-derived from the lesson `.md`'s existing short quotes. See `Index.md` (file table, Generation workflow Step 2, Pending work). |
| 2026-09-07 | v1 | `Generate_TOEFL_LS_Extension_Prompt_v1.md` added: this family's TOEFL Practice Extension, the sibling to Passage Reading's `Generate_TOEFL_Extension_Prompt_v1.md`, generating an optional Advanced/Proficient-only practice packet from a completed lesson. Grounded in two new source files - `source/TOEFL_Listening_extracted_text.txt` (the real TOEFL iBT Listening section: 4 task formats, 6 question types) and `source/TOEFL_Speaking_extracted_text.txt` (the real TOEFL iBT Speaking section: Listen and Repeat and Take an Interview, with their 0-5 scoring guides). Two grounding rules, not one: Part A (Listening) writes an original passage faithful to the lesson's real source, reusing only the same short quotes the base lesson itself already established, since TOEFL's 35-250 word task lengths exceed the one-to-two-sentence fair-use ceiling `Generate_Lesson_Prompt_v1.1.md` Section 0.3 already sets for this family's real third-party sources (unlike Passage Reading's anchor texts, which aren't bound by that ceiling); Part B (Speaking) authors fully original sentences/questions grounded in the lesson's real topic and vocabulary, matching how the real TOEFL Speaking tasks are freshly authored in the actual test too. Also corrected mid-build: TOEFL Listening and Speaking are heard, not read, so (unlike Reading's extension) the student-facing packet never prints Part A's passage or Part B's sentences/questions - only instructions, response-time windows, and Part A's questions/answer choices; a teacher or partner reads the source text aloud once from the instructor copy, matching the real test's single-playback rule and this family's own Day 1 Phase 5 Closing Transfer Check convention. First worked example generated the same day: `lessons/advanced/Set_1/Lesson_1_LostKitchen/LostKitchen_Advanced_L1_TOEFL.md` (instructor document) and its matching `..._TOEFL_Packet.html` (student packet), against Lesson 1 (LostKitchen). Does not alter the base Day 1/Day 2 lesson; not yet given to a real class. See `Index.md` (file table, Generation workflow Step 4, Pending work) and `Rotation_Log_Advanced.md`. |
| 2026-09-07 | — | Set 1 Intermediate's remaining three lessons (`PortoFoodTour_Intermediate_L2_Packet.html`, `Backpack_Intermediate_L3_Packet.html`, `GreatGrandmother_Intermediate_L4_Packet.html`) synced to the current shared spec, closing out the same sweep Lesson 1 (NewBakery) got above - all three had predated every hand-correction pass and carried no masthead tag or `.masthead-tag`/`.masthead-meta` CSS at all. Each gained: the `.masthead-meta` two-tag stack (`Listening & Speaking` / `Intermediate S1.<N>.0`, opening masthead only) plus its CSS, including restoring `.masthead`'s `display: flex` layout so the tag stack sits top-right of the h1; the missing `.refresher`/`.refresher-noline`/`.refresher p` CSS their "Check What You Heard" (and, for Backpack, also its Mystery Quote reveal) upside-down boxes were already using in markup with no rule for, plus `.refresher` added to each file's `@media print` list; `.task-block .instr-line`'s margin-bottom fixed to `10px` (was `4px`); `.idiom-item`/`.idiom-phrase`'s missing `font-family`/`font-size` restored; every multi-line `font-family` declaration reflowed to one line per the style guide's §C convention (21-22 instances per file). Lesson-specific fixes: PortoFoodTour's one idiom ("in spite of") moved out of a `Phrase Spotlight` spotlight-box into a plain "Idioms to Know" section-label block, matching NewBakery/LostKitchen; GreatGrandmother's `.vocab-list .word` column width realigned to the shared `130px` (was locally widened to `150px`), the same drift LostKitchen had. `Lesson2_PortoFoodTour.md`, `Lesson3_Backpack.md`, and `Lesson4_GreatGrandmother.md` each gained a `**Band:** Intermediate \| **Version:** S1.<N>.0` metadata line under their H1 (`S1.2.0`, `S1.3.0`, `S1.4.0` respectively). Deliberate one-lesson-per-file exceptions to §G's going-forward-only rule, completing Set 1 Intermediate's sync. See `Rotation_Log_Intermediate.md` and `Index.md` for the same note. |
| 2026-09-07 | — | `NewBakery_Intermediate_L1_Packet.html` (Set 1, global Lesson 1) synced to the current shared spec, matching the same retrofit Advanced's `Lesson1_LostKitchen`/`Lesson2_LivingTextbooks` already got: added the `.masthead-meta` two-tag stack (`Listening & Speaking` / `Intermediate S1.1.0`, opening masthead only) and its `.masthead-meta` CSS rule, removing the now-redundant `margin-top` from `.masthead-tag` itself. Also fixed Task A item 3's word bank, which sat in a `.wordbank` block after the whole list instead of inline with its item - moved inside the item's `.qbody`, matching Task B item 2 and the "word banks positioned right before their items" convention Lessons 3-4 already follow. `Lesson1_NewBakery.md` also gained a `**Band:** Intermediate \| **Version:** S1.1.0` metadata line under its H1, matching Lost Kitchen. This is a deliberate one-lesson exception to §G's going-forward-only rule, not a reopening of Set 1's other lessons. See `Rotation_Log_Intermediate.md` and `Index.md` for the same note. |
| 2026-09-07 | — | Advanced Module 1 Set 1's assessment generated against `Generate_Assessment_Prompt_v1.md` and `Generate_Assessment_Student_Packet_Prompt_v1.md` - the first assessment run against the Advanced band. Part A (Listening) source: "Artists with disabilities let their creativity soar at this Utah studio" (PBS News Weekend, Ali Rogin, 2025-05-03; Jump the Moon art studio, Logan, Utah), runtime 5:32, distinct from all 4 taught sources, tiered items covering all four of the Set's listening strategies (Stated vs. Implied Opinion, Recognize Examples, Cause-and-Effect Language, Signposting/Discourse Markers). Level 7 also draws on a second real source (a Utah Division of Arts & Museums profile page) for a two-source framing-comparison item, matching this Set's own Level 7 lesson pattern. Part B (Speaking): live solo presentation (Advanced's default mechanism) plus a same-task, same-rubric Teams-recording standing alternate, emphasizing Making Comparisons and Hedging an Opinion. See `lessons/advanced/Set_1/Set1_Advanced_Assessment.md`, its matching `..._Packet.html`, `Rotation_Log_Advanced.md`, and `Index.md`. Not yet given to a real class. |
| 2026-09-07 | — | Advanced Module 1, Lesson 4 ("The Last Glass Bender") generated against `Generate_Lesson_Prompt_v1.1.md` and `Generate_Student_Packet_Prompt_v1.3.md`, **completing Set 1 (4 of 4 lessons)**. Primary source: Idaho Public Television's *createid* series, Season 2 Episode 2, "Glass Bender: Wil Kirkman" (Rocket Neon, Boise; runtime 6:10, stated on the PBS.org video page). Secondary (Level 7 excerpt only): Boise Art Scene's written interview with Kirkman. Version `S1.4.0`. Generated fresh against the current spec throughout (2-day Unit A/B architecture, masthead-meta stack, idiom-table format) - no restructure or later sync needed. See `lessons/advanced/Set_1/Lesson_4_GlassBender/Lesson4_GlassBender.md` and its matching `..._Packet.html`, `Rotation_Log_Advanced.md`, and `Index.md`. |
| 2026-09-07 | — | `LivingTextbooks_Advanced_L2_Packet.html` (Set 1, global Lesson 2) fully synced to the current shared spec, having predated every hand-correction pass so far: added the `.masthead-meta` two-tag stack (`Listening & Speaking` / `Advanced S1.2.0`, opening masthead only) and its embedded `.masthead`/`.masthead-meta`/`.masthead-tag` CSS (the file's own `.masthead` rule was still the pre-flex version with no CSS for a tag stack at all); converted its two idioms from plain `.idiom-item` paragraphs under "Phrases to Know" to the `.idiom-list`/`.irow` numbered table under "Idioms to Know," adding the missing `.idiom-list`/`.irow` CSS; realigned `.vocab-list .word` to the shared `130px` column width (was locally widened to `150px`); restored `.idiom-item`/`.idiom-phrase`'s missing `font-family`/`font-size`; fixed `.task-block .instr-line`'s margin to `10px` (was `4px`); added the missing `.refresher`/`.refresher-noline`/`.refresher p` CSS the "Check What You Heard" upside-down box already used in its markup, plus `.refresher` to the `@media print` page-break list; reflowed every multi-line `font-family` declaration onto one line per the style guide's §C formatting convention. `Lesson2_LivingTextbooks.md` also gained a `**Band:** ... \| **Version:** S1.2.0` metadata line under its H1, matching Lost Kitchen. See `Rotation_Log_Advanced.md` and `Index.md` for the same note. |
| 2026-09-07 | v1.3 (print) | Correction to the same-day masthead-meta version-stack change (below): the three-line stack (Listening & Speaking / Band / version) is now two lines, with Band and version combined onto the second (`<Band> S<Set>.<Lesson>.<Iteration>`, e.g. `Advanced S1.1.0`) - the user's actual intent. Section 2.4 and the review checklist rewritten to match; see `shared/Student_Packet_Style_Guide.md` (v1.5 → v1.6) and `shared/Changelog.md`. Renamed `_v1.2.md` → `_v1.3.md`. `LostKitchen_Advanced_L1_Packet.html`'s masthead (the lesson this convention was first retrofitted onto) corrected in the same pass. |
| 2026-09-07 | — | Follow-up on `LostKitchen_Advanced_L1_Packet.html`'s idiom-table hand-correction earlier the same day (below): its two idiom rows now carry a plain "N. " number prefix ("1. in the middle of nowhere," "2. keep my feet on the ground"), matching "Words to Know"'s existing numbering. Also, the phrase column's width was a `min-width` (a floor), so the longer second phrase grew past it instead of wrapping, leaving the two rows' gloss text starting at different x-positions; changed to a fixed `width: 180px` so both rows' columns actually match. Passage Reading's packet prompt bumped to match (v1.5 → v1.6); this modality's own prompt still needs no direct edit (inherits via its existing pointer to Passage Reading Section 2.12). See `shared/Changelog.md` and Passage Reading's own Changelog for the full reasoning. |
| 2026-09-07 | v1.1 (Lesson) / v1.2 (print) | New cross-modality **lesson version number** (`shared/Program_Conventions.md` new §G): `S<Set>.<Lesson>.<Iteration>`, distinct from this prompt family's own "vX.Y" numbering. Lesson Generation Prompt now requires a `**Band:** ... \| **Version:** ...` field in the opening metadata line. Student Print Formatting Prompt's Section 2.4 changed from a single modality-only `.masthead-tag` to a three-line `.masthead-meta` stack (Listening & Speaking, Band, version code), reversing the v1.1 "no Level/Band" restriction now that Band and version are load-bearing, not decorative - see `shared/Student_Packet_Style_Guide.md` and `shared/Changelog.md`. Renamed `Generate_Lesson_Prompt_v1.md` → `_v1.1.md` and `Generate_Student_Packet_Prompt_v1.1.md` → `_v1.2.md`. Going-forward only: none of the six already-generated lesson docs/packets are retrofitted with a version. |
| 2026-09-07 | — | `LostKitchen_Advanced_L1_Packet.html` hand-corrected: its two idioms ("in the middle of nowhere," "keep my feet on the ground") now render as a table (`.idiom-list`/`.irow`, new in `shared/Student_Packet_Style_Guide.md` v1.4) instead of stacked plain `.idiom-item` paragraphs, matching how "Words to Know" already renders as a `.vocab-list` table. This modality's own packet prompt didn't need editing - Section 2.13 already defers to Passage Reading's Section 2.12 ("same rules as..."), which now documents the rule (Passage Reading `Generate_Student_Packet_Prompt` v1.4 → v1.5). Also fixed in the same pass: `Index.md`'s file table cited this modality's own packet prompt as `_v1.1.md`, a version that predates the 2026-09-07 masthead-meta version-stack change already recorded in `shared/Changelog.md`; corrected to the actual `_v1.2.md` on disk. `LivingTextbooks_Advanced_L2_Packet.html` also has 2 idioms in the old plain-paragraph style and was left as-is - noted pending in `Index.md`, not swept in this pass. |
| 2026-09-06 | — | Follow-up on the two Set 1 Lesson 1 packets' 2026-09-06 hand-correction (below): that pass added `.masthead-tag` to both packets but placed it on both the Unit 1A and Unit 1B mastheads - removed from Unit 1B in both `LostKitchen_Advanced_L1_Packet.html` and `NewBakery_Intermediate_L1_Packet.html`, since the tag belongs on the document's opening masthead only, never a later `.masthead.masthead-later` heading (`shared/Student_Packet_Style_Guide.md` §B). Also found and fixed, in both files: the "Check What You Heard" upside-down answer box (`class="refresher refresher-noline upside-down"`) had no `.refresher`/`.refresher-noline`/`.refresher p` CSS defined anywhere in the packet - only `.upside-down`'s rotation existed - so the box was rendering with none of its intended padding/border/font-size treatment. Added the missing rules verbatim from the shared style guide, plus `.refresher` to each file's `@media print` page-break-avoid list. |
| 2026-09-06 | v1.1 (print) | Student Print Formatting Prompt's Section 2.4 rewritten: the opening masthead now carries a `.masthead-tag` span reading exactly "Listening & Speaking," per the new class added to `shared/Student_Packet_Style_Guide.md` §B (v1.0 → v1.1) - a plain, skill-based class-identifier for whoever is handling the printed packet. Everything else the old "no kicker lines" rule banned (Name/Date field, subtitle line under any heading, footer note) is still banned. Renamed `Generate_Student_Packet_Prompt_v1.md` → `_v1.1.md`; this is also the first version of this prompt to carry an explicit "Current version" pointer line in its preamble, matching Reading and Writing's convention. None of the six already-generated packets (Intermediate 1-4, Advanced 1-2) have been retrofitted with the tag - only packets generated from v1.1 onward will carry it. The companion Assessment Student Print Formatting Prompt (`Generate_Assessment_Student_Packet_Prompt_v1.md`) was deliberately left unchanged for now - its own Section 2.4 already relies on a different running-head mechanism (Section 2.2) that makes a masthead tag redundant there; worth a separate look if that assumption changes. |
| 2026-09-06 | v1 | Both Student Print Formatting Prompts' Section 3 now point to `shared/Student_Packet_Style_Guide.md` (the new cross-modality single source of truth for print styling) instead of cross-referencing Passage Reading's prompt file by prose. The two exceptions the lesson packet prompt had been patching onto the base stylesheet (broadening `.qlist .num`, dropping `.discuss-block .prompt`'s font-size rule) are now folded into the shared stylesheet itself, so this family no longer needs to track them as exceptions. No change to what a generated packet looks like; this family's own delta classes (`.citebox`, `.notes-table`, etc.) are unchanged. |
| 2026-09-06 | v1 | Assessment Student Print Formatting Prompt added (`Generate_Assessment_Student_Packet_Prompt_v1.md`), mirroring the Student Print Formatting Prompt's translation approach and base stylesheet, adapted for an assessment's shape: per-task-Level printable Listening Test pages (no choose-your-task menu, since a teacher assigns the Level) and plain Speaking Task instruction cards, with every answer key, point value, holistic pass note, and rubric stripped from the student copy. First packet generated the same day against Intermediate Set 1's assessment: `lessons/intermediate/Set_1/Set1_Intermediate_Assessment_Packet.html`. Intermediate Set 1's own assessment `.md` was also regenerated the same day, replacing its corrupted stub with real content (see `Rotation_Log.md`'s Set 1 entry). |
| 2026-09-04 | — | Advanced Module 1 Set 1's Lessons 1-2 restructured to the current 2-day cycle and their student packets rebuilt to current formatting conventions, closing the last known gap between generated content and the current prompts across the whole family. |
| 2026-09-04 | v1 | Assessment Generation Prompt added and then revised (earlier the same day): Listening and Speaking assessments now run every Set (not staggered); Speaking scoring splits by band (Beginner/Intermediate via a Teams solo recording as the formal assessment; Advanced/Proficient via live solo/group presentation, with a Teams-recording version as a standing scored alternate). |
| 2026-09-03 | — | Unit Architecture corrected from an 8-day to a 2-day cycle (Day 1 = Unit A/Listening, Day 2 = Unit B/Speaking, 75 min each) and module size corrected from 8 to 4 lessons to match, restoring parity with Passage Reading's cycle length. The Sets concept introduced (a Module/Band can hold more than one 4-lesson rotation over time); Module 1 Intermediate Set 1 completed (Lessons 1-4) and retroactively rewritten to match. Practical effect: one module (8 real class days) needs 4 lessons at 2 days each, not 8 lessons at 8 days each; the Module Lesson Plan's "~64 instructional days" figure describes the whole 8-module band curriculum, not one module. |
| 2026-09-03 | — | Item counts per task Level must be calibrated so time-on-task is roughly balanced across all task Levels, not just item difficulty (surfaced reviewing Module 1 Lesson 3: the lowest task Level had roughly one merged item for every two-to-three the highest Level received). Fix: lower task Levels get more, individually shorter items; higher task Levels get fewer, individually deeper items, sized against rough per-item time estimates (30-60s for a circle/point item up to 3-5min for an extended composition/analysis item). Self-check item 16 added. |
| 2026-09-03 | — | Workflow clarified: generate the .md lesson and its student-facing .html packet back-to-back in one action (run this prompt, then immediately the Student Print Formatting Prompt against the finished .md), rather than as two separately-requested steps. The .md is the single source of truth; the .html is always a regeneration from it, never independently hand-edited for anything that changes lesson content. |
| 2026-09-01 | — | Two real constraints surfaced generating Module 1 Lesson 1: (1) real video timestamps often aren't independently verifiable from a generation pass, so a source's own internal structure (headed sections, chapter markers) is used as the segment label instead of an invented timestamp — teachers pencil in real elapsed times on first playthrough. (2) Section 0.2's runtime ranges are a soft target for found sources, not a hard ceiling like Passage Reading's word counts — a source running somewhat past its band's range is a much smaller problem than an off-ceiling anchor text and can be kept if otherwise a strong fit. |
| — | — | After user review of a student packet: the Day 4 Listening Closing Transfer Check must use an actual short invented script the teacher reads aloud, not an unspecified "new clip" left to improvisation; and a fixed-frame listening extraction item needs its instruction stated completely inline, not left as a bare template sentence. |
| 2026-09-01 | — | Module size initially reconfirmed at 8 lessons/module (~64 instructional days as the target, day count set per-source at generation time) rather than adopting Reading's same-day 4-lesson correction, on the reasoning that the two cycle lengths weren't comparable. Superseded 2026-09-03 above once Unit Architecture turned out to already be a 2-day cycle. |
| 2026-08-30 | — | Document created. |
