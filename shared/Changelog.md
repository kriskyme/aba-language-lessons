# Shared Changelog

| Date | Version | Summary |
| --- | --- | --- |
| 2026-09-14 | v2.48 (Style Guide) | **Metalanguage for the skill is not printed unless the page teaches it: a new §E row.** Per user direction on Listening/Speaking Module 2 Intermediate Set 1 Lesson 1, after asking "where do we mention time connectors?" and choosing "translate it away." **What was actually wrong:** the packet paraphrased the skill carefully everywhere it was written with care - "the words that told you when," "circle the words that tell you when" - and then printed the bare term **time connectors** twice, both inside ★★★'s Day 2 task, because Level 4's CSV row is phrased that way and the row's wording was copied into the stem. The student meets a named category once, having been taught the thing under a third name ("sequence markers," teacher-side only) or no name. The new row names the class - time connectors, sequence markers, transition words, discourse markers, cohesive devices, signposting, topic sentence, hedging - says to name what the words **do** instead, and makes the check a **search of the finished packet rather than a read**, since the failure is always a single surviving instance rather than a page written in jargon. Teaching the term is allowed at the upper Bands, but then it is introduced where the skill is introduced and used consistently after. Self-check item 35 added. **Swap test passed:** the defect was found in two other modalities by searching for the pattern. **Content swept, 8 instances in 5 packets across two modalities:** Listening/Speaking NewKid, FireRescue and Block42 lost six "time connector" instances; Passage Reading ForgeAtDawn and VinylComeback each printed **hedging** once, unglossed, in a Level task stem, now "wording that holds something back." All packet-only wording, so under Conventions §G no version code moved. **Not swept, logged instead:** nine Academic Writing packets print "topic sentence," between 1 and 9 times each. That is a different question - at Advanced and Proficient it may be taught vocabulary rather than leaked jargon, and MyHeadphones Lesson 2 does teach it on the page - so it needs one curriculum decision, not a sweep. Named by file with counts in Academic Writing's `Index.md` Pending work. |
| 2026-09-14 | v1.40 (Quality Standards) | **A printed shared core is a promise about every Level, and ★ is where it breaks: §B extended.** Per user direction on Listening/Speaking Module 2 Intermediate Set 1 Lesson 1's Day 2 ★ task: "this one needs more scaffolding it should have a blank for where they can write where they were." **What was actually wrong:** adding that blank exposed a defect underneath it. Practice It's lead-in told the class "everyone is talking about the same thing: a time you were somewhere new," and Tasks B, C and D all did, while ★ alone circled lines that fit **Darrell's** story and said those aloud. A blank asking where *you* were, bolted onto items about someone else's day, would have made the task incoherent rather than better scaffolded. §B v1.39 had just introduced the print-the-core-once form; it said nothing about whether each Level's items are actually on that core. The new paragraph names the failure, its cause (the lowest tier's assigned shape - fixed-frame extraction, word-recognition drill, better-of-two - is source-facing by nature and gets left pointing at the source while the tiers above are rewritten), and the distinction that resolves it: a **supporting step** may stay on the source, the tier's own **production** may not. Where ★ genuinely cannot work from the core, the lead-in must stop claiming it is shared. Self-check item 43 added. **Swap test passed:** a Passage Reading tier set whose lead-in names a shared question, and an Academic Writing Mentor Ladder whose lead-in names the shared Scenario, break the same way. **Prompts swept:** none restate it; all inherit §B. **Content swept:** all 31 packets' Practice It sections were surveyed. Most print a generic "Pick one task below" that names no shared subject, so nothing can contradict it. Six name one; four (HighLine, GodsAndGoddesses, QuietZone, Lesage) are clean, and **both defective cases were fixed**: NewKid (`2.1.1.8`) and FireRescue (`2.1.2.4`), whose ★ said "one sentence about the story" under a lead-in promising a time somebody helped you. |
| 2026-09-14 | v2.47 (Style Guide) | **The lesson's staging is not page content: a new §E row.** Per user direction on Listening/Speaking Module 2 Intermediate Set 1 Lesson 1, quoting the packet's own Listen Closely instruction back: "doesn't need play by play. just say answer the following if you may have to listen to it again." **What was actually wrong:** §E already banned delivery mechanics - who presses play, when it stops, how long a beat lasts - and self-check 32 enforced it, but this paragraph is one level out from that. It narrated the **teaching protocol**: answer from the first listen, then hear it again, then change what moved and tick it. None of that is the student's job at the moment they read it; it is §D12's commit-verify-revise arc written out as a script, and it buries the one actionable sentence under a sequence the student cannot act on yet. The new row keeps the one clause that earns its place - a short warning that the input comes back, so nobody commits too hard too early - and cuts the rest. Self-check item 34 added. **Swap test passed:** a Passage Reading packet saying "answer from your first reading, before you re-read, then re-read and change what moved" is the same defect, as is a Writing packet scripting draft-then-mentor-text-then-revise. **Prompts swept:** none needed a bump - all three modalities' packet prompts delegate this instruction to Style Guide §E/§F rather than restating it. **Content swept:** all three packets in the modality carrying the pattern - NewKid, FireRescue, Block42. Packet-only wording, so under Conventions §G no version code moved for FireRescue or Block42. |
| 2026-09-14 | v2.46 (Style Guide) | **An organizer's rows are the source's own parts whether or not the delivery pauses: §F extended.** Per user direction on Listening/Speaking Module 2 Intermediate Set 1 Lesson 1: "so the steps need more space and timing." **What was actually wrong:** §F already required one timed, named row per part, but it fired only "where the input is delivered in named parts" - that is, only on a source long enough to be played segment by segment. NewKid is 2:55, inside its Level's range, so it plays straight through, and the rule left its organizer as five rows labelled "Step 1" to "Step 5" - counters that tell a student how many boxes there are and nothing about which one they are in, which is the one thing a student who has fallen behind needs. The pause was never what made a locator useful; a student loses the thread on a short source too. §F now triggers on the source having a named part structure, names the locator form per modality (a timestamp range for a clip, a section or chapter heading for a text), keeps the pause-specific reasoning as one of two cases, and bans bare counters where a real part structure exists. Self-check item 31 rewritten. **Swap test passed:** a Passage Reading organizer's rows key to the passage's lettered sections and an Academic Writing one's to the Scenario's stages, in both cases so a student can find their place, with no playback involved at all. **Prompts swept:** `Generate_Student_Packet_Prompt_v2.11.md` §2.6 and its self-check item 4 (the only prompt in the three modalities restating the rule in its own words). **Content swept:** NewKid (`2.1.1.4`); the other Listening/Speaking packets are logged by name in that lesson type's `Index.md` Pending work, since most need their segment timings derived from the saved transcript first. |
| 2026-09-14 | v1.39 (Quality Standards) | **A shared core is printed once above the task set: §B extended.** Per user direction on Listening/Speaking Module 2 Advanced Set 1 Lesson 1's Day 2 tasks: "how about just make it all Tell a true story of a time you were fixing, building, or setting up something and it did not work the first time. each star will get only slight variation." **What was actually wrong:** the same 24-word prompt was printed four times, once inside each of Tasks A to D, and Task A's "tell the sequence ... choosing your own ordering words" and "say how it turned out" were carried, reworded, inside every tier above it. §B already banned a tier re-specifying the tier below it, and named the fix as pointing at that tier ("Do Task D. Start three of your six sentences ..."), which works for two adjacent tiers but not for a core all four share: it leaves the student reading another Task's block to find their own. §B now sanctions the better form for that case - print the shared core once above the set, have each Level state only its addition - which is what the user proposed. **Swap test passed:** Passage Reading's tiered task sets and Academic Writing's Mentor Ladder repeat a shared scenario the same way. **Content swept:** HubbleRepair only (`2.1.1.19`). Its Day 2 set now opens with an "Everyone does this part" box carrying the prompt, the in-order telling and the outcome, and each star lists only its delta: ★ four or five sentences plus a reason; ★★ six to eight plus why it mattered and the unnamed feeling; ★★★ the two mid-story breaks, the direct quotation and the listener adjustment; ★★★★ the two tellings and the three shaping choices. The specification added in v1.38 is unchanged - what went is the repetition, not the detail. The Set's other three lessons repeat their own prompts the same way and are covered by the Pending work entry already logged for their ★★★ and ★★★★ tasks. |
| 2026-09-14 | v1.38 (Quality Standards) | **Specification does not thin out as the Levels rise: §B extended.** Per user report on Listening/Speaking Module 2 Advanced Set 1 Lesson 1's Day 2 tasks: "these need way more instruction. it's too opended." **What was actually wrong:** the ★ and ★★ tasks name a countable product - four or five sentences, one reason, one detail that shows a feeling without naming it - and the ★★★ and ★★★★ tasks hand over an abstraction: "tell it without a fixed pattern, moving between what happened and what it meant," "shape it so the listener finds it funny." That is not a harder task, it is an unspecified one; the student cannot tell whether they have done it, the teacher cannot either, and the Level's CSV row goes unassessed while the telling sounds fine. §B's existing tier rules cover fairness to the *lowest* Level (Respectful Tiers) and re-specification between adjacent tiers, and neither catches the top of the ladder being vaguer than the bottom. The rule now requires the highest Levels to be specified as concretely as the lowest - a count and a place for each move, one worked example in a student's own words - and, where the demand is a rhetorical effect rather than a countable feature, requires the student to commit in writing to the specific choices that produce it before performing, so there is something to check the effect against afterwards. Self-check item 42 added. **Swap test passed:** a Passage Reading production task and an Academic Writing Level 7 rung fail identically when the top tier's instruction is the only one without a countable product. **Prompts swept:** none states a task's specificity in its own words; the lesson prompts hand the wording to the CSV row. **Content swept:** HubbleRepair only (`2.1.1.18`) - ★★★ now eight to ten sentences with the reflective break given a count and a position and a worked example, direct quotation contrasted against reported speech, and the listener adjustment reported back and checked; ★★★★ now writes down its three shaping choices before the first telling and asks the second partner which were noticed. The Set's other three lessons carry the same template but need specifying against their own stories, which is new content, so they are named in Listening/Speaking's `Index.md` Pending work. |
| 2026-09-14 | v1.37 (Quality Standards) | **An item printed over a compare layout has to require both sides: §C3 extended.** Per user challenge to the question written one turn earlier: "What does each one leave the listener concluding? is that the best question. isn't like what's the difference between the two." **What was actually wrong:** the question could be finished having read one column. Task D prints two accounts of the same hours side by side, subject by subject, and "what does each one leave the listener concluding" is answerable one account at a time, so the comparison the layout exists for never has to happen. §C3 required an item to need the centerpiece; nothing required an item over a compare layout to need the comparison. **The user's own suggestion is half the fix and is recorded as such:** naming the comparison is right, but "what is the difference between the two" alone invites a list of surface contrasts, which is a lower Level's work than Level 7's row, which asks the student to assess how each teller's structural choices shape what the listener is meant to conclude. The rule now requires both halves - name the comparison, and make the effect turn on it. **Content swept, the three lessons carrying the collapsed Task D question:** HubbleRepair `2.1.1.16`, FlintWater `2.1.2.8`, MountBowen `2.1.3.9`, each now reading "What is different about the way each one tells it, and if you had only read one, what would you think had happened?" against its own subject. The counterfactual clause is what carries the row's assessment half: a student cannot answer it without having used both columns. **Swap test passed:** a Passage Reading two-text compare and an Academic Writing Mentor comparison fail identically when their question can be answered from one side. |
| 2026-09-14 | v1.36 (Quality Standards) | **When a stem gains a premise, the items after it have to be re-read: §C8 extended again.** Per user direction on Listening/Speaking Module 2 Advanced Set 1 Lesson 1, to remove "Write one sentence saying what that detail tells you, and why you read it that way" as redundant, and to remove Task C's summary item. **What was actually wrong, and this one was caused by the previous entry:** while the ★★ stem still made the student infer the feeling, the follow-up asking what the detail tells them was doing real work; the v1.35 reword supplied the feeling in the stem, and the follow-up became a restatement of it - a C9 violation created downstream by an edit that fixed the item above it. §C8's scoping paragraph now says to re-read the items after any stem that gains a premise. No new self-check item: item 41 already covers it once the re-read is part of the rule. **Content swept, both removals, all three lessons carrying the templates:** HubbleRepair `2.1.1.10`, FlintWater `2.1.2.5`, MountBowen `2.1.3.6`. Packet item numbering in Task B and Task C renumbered to close the gaps. **One consequence recorded rather than hidden:** removing Task C's summary leaves Level 6's Day 1 set with no standalone summary item, while its CSV row reads "identify one clearly missing piece of information ... in addition to summarizing events." The three remaining items are all accounts of what the telling does and does not give, and Level 6's Day 2 row carries the narrating load outright, so the row is weakly rather than not served; each lesson's `.md` now carries a note under Level 6 saying the absence is a decision, so a later pass does not read it as an omission and restore the duplicate. |
| 2026-09-14 | v1.35 (Quality Standards) | **An item is scoped to its CSV row, not just traceable to it: §C8 extended.** Per user direction on Listening/Speaking Module 2 Advanced Set 1 Lesson 1's ★★ item ("He never uses the word ashamed or guilty. Write down one thing he does or says that tells you he feels something like it anyway"), proposing "something like He never uses the word ashamed or guilty, but he seemed to feel that way when the repair wasn't going well. how does he describe it." **What was actually wrong:** the item asked for two inferences where its row asks for one. Level 5's row names the product as a detail - "identify one clearly telling action or detail that signals how the speaker felt, **even though the speaker never names the feeling directly**" - so the feeling is the row's premise, not the student's task; the item made them derive the feeling first and only then look for its evidence, and a student who read the feeling wrong could no longer answer the half the objective assesses. §C8 said every item is *traceable* to a row, which this was; nothing said it must be *scoped* to what the row names as the product. It now does, with a carve-out for the case where the withholding is itself the content. Self-check item 41 added. **Swap test passed:** a Reading item asking a student to work out a writer's stance and then find the words carrying it, or a Writing item asking them to infer a Mentor's tone before locating it, fails identically. **Content swept - the fourth Set-wide copied template found in this Set today:** HubbleRepair `2.1.1.9`, FlintWater `2.1.2.4` and MountBowen `2.1.3.5` all carried the two-inference shape and now supply the feeling and ask for the evidence as a direct question. **Block42 `2.1.4.2` is the carve-out and was deliberately reworded differently:** Tanimoto's account registers what was done to him without protest, so the flatness is what the lesson is about and naming a feeling in the stem would answer the item; its stem now says the flatness is the point and asks the question without supplying a word the source refuses. The `.md` records why it diverges from its Set-mates, so the difference is not read later as an oversight. |
| 2026-09-14 | v1.34 (Quality Standards) | **Where two task Levels carry the same beat, the higher one carries its own demand: §B extended.** Per user question on Listening/Speaking Module 2 Advanced Set 1 Lesson 1, reading its ★★ and ★★★ tasks side by side: "Summarize the repair in four sentences, in order. Summarize the day in four sentences. aren't those the same thing essentially." They are. **What was actually wrong:** not that both Levels summarize - both CSV rows require it, Level 5's to "summarize the sequence and why it mattered" and Level 6's to identify a missing piece "in addition to summarizing events" - so the beat belongs in both task sets. The defect is that the two were printed in the same words at the same length, so a ★★★ student spends their first item doing the ★★ item, and worse, Level 6's version had **dropped** Level 5's "in order," making the harder task read as the easier one at exactly the point a student compares them. §C1's distinctness rule is scoped to items within one Level's own set and so could not see it; §B's existing "a tier that adds a demand says so" governs a tier that is the Level below plus one thing, which these are not - Level 6 is its own task that happens to share one beat. §B now covers the shared-beat case directly, requires the higher Level's version to carry what its own row adds, forbids losing a constraint on the way up, and says to check it at Set level rather than per lesson. Self-check item 40 added. **Swap test passed:** the same shape is available to Passage Reading's tiered task sets and to Academic Writing's Mentor Ladder rungs, where a higher rung repeating a lower rung's instruction verbatim is the identical failure. **Prompts swept:** none states a summary item's wording. **Content swept:** the three lessons of this Set with a Level 5/6 pair, where it was a copied template rather than one lesson's slip - HubbleRepair `2.1.1.8`, FlintWater `2.1.2.3`, MountBowen `2.1.3.4`. Each Level 6 summary regains "in order" and gains its own Level's demand, to summarize using only what the account actually shows, which is the base the missing-information items then interrogate. Block42 has no summary pair and is unaffected. **Noted, not swept:** this is the third Set-wide template defect found in this one Set today, after the impossible underline item (v1.33) and the "In one sentence, say why" formula (Style Guide v2.45). All three were written once and copied across the Set's lessons, so a defect in any one of its task shapes is a defect in all four. |
| 2026-09-14 | v1.33 (Quality Standards) | **Printing the container is not printing the thing: §C10 extended.** Per user direction on Listening/Speaking Module 2 Advanced Set 1 Lesson 1, to remove "Underline the two words in your ordered list above that told you the order." **What was actually wrong:** §C10 already said "underline" appears only where the thing to be underlined is printed, and the item still shipped, because the *list* is printed - five event labels the packet wrote - while the ordering words are in the clip and nowhere on the page. The task was not merely confusing, it was impossible, and the lesson's own Answer note admitted it: "Accept any ordering words the student actually used." §C10 now says that where the named thing is a property of printed material rather than the material itself - the time words in a list, the linking word in a plan, the evaluative adjective in a quotation - an actual instance has to be printed before the action is named. Self-check item 39 added. **Swap test passed:** identical failure in a Reading packet ("underline the two time words in the list above") or a Writing one ("circle the linking word in your plan"). **Prompts swept:** none restates the pattern. **Content swept:** all three lessons of Module 2 Advanced Set 1 carried the same item as a Set-wide template - HubbleRepair `2.1.1.7`, FlintWater `2.1.2.2`, MountBowen `2.1.3.3` - item removed from `.md` and packet, and each Answer note's trailing "accept any ordering words" clause with it. |
| 2026-09-14 | v2.45 (Style Guide) | **A question is printed as a question: §F.** Per user direction on the same lesson: "reword In one sentence, say why this day mattered to him. jsut say why did this day matter to him." **What was actually wrong:** nothing said how an item that is simply a question should be phrased, so the Module 2 Advanced Set had settled into a house formula - "In one sentence, say why this mattered to him" - across all four of its lessons. The wrapper adds a verb the student does not need and a length the answer line already states, and buries the question at the end of an instruction. §F now requires the page to ask it: "Why did this day matter to him?" A stated length survives only where the length is part of what the answer must do ("Summarize the repair in four sentences, in order"), and a real constraint stays as its own sentence after the question mark ("Use what he says, not what you assume"). Self-check item 33 added. **Swap test passed:** the formula is available to any modality's packet and fails the same way in each. **Prompts swept:** none restates it. **Content swept:** five items across five lessons, every instance in the repo - HubbleRepair `2.1.1.7`, FlintWater `2.1.2.2`, MountBowen `2.1.3.3`, Block42 `2.1.4.1`, and QuietZone `1.2.7.3` (whose variant read "In one sentence, how would you describe this place ..."). FlintWater's Level 7 speaking frame "In one sentence, what you told me was ___." was checked and left: it is a stem the student completes aloud, not an instruction. |
| 2026-09-14 | v2.44 (Style Guide) | **A packet does not narrate the playback: §E gains a delivery-mechanics row.** Per user direction on Listening/Speaking Module 2 Advanced Set 1 Lesson 1's Listening Notes instruction: "remove The audio stops after each part, and you will have about half a minute to write and the the - late. just say Fill in only the box for the part you have just heard." **What was actually wrong, and it was mine:** the Student Packet prompt §2.6, written earlier the same day in v2.9, had *required* the instruction to narrate the audio's behaviour - that it stops after each part, that there is about half a minute to write. That contradicted a principle the Style Guide already held but had only stated for sentence stems ("delivery is teacher-led facilitation, not page content"), and §E's translation table had no row to catch it. The narration fails on its own terms too: a printed time allowance is a clock the student cannot see and the teacher overrides anyway, and where the table already prints the parts and their times, saying the audio stops between them tells the student nothing the page has not shown. **The rule:** §E now translates delivery mechanics - who presses play, when it stops, how long a beat lasts - to nothing at all, with an explicit carve-out for what a task genuinely turns on, stated as the student's own experience rather than as teacher action ("You will hear two parts of it," not "Your teacher will play two parts of it"). Self-check item 32 added. **Swap test passed:** "your teacher will pause the read-aloud" and "you have five minutes to plan" fail identically in a Reading or Writing packet, so it belongs here rather than in one modality's prompt. **Prompts swept:** `grep` found one prompt stating it in its own words, Listening/Speaking's Student Packet prompt, `_v2.9.md` to `_v2.10.md` §2.6 and self-check item 4 - the rule it had carried is reversed, not softened. **Packets swept, four files, all four instances in this modality:** `HubbleRepair_Advanced_L1_Packet.html` cut to the user's own sentence; `FireRescue_Intermediate_L2_Packet.html` lost "Your teacher will stop the audio partway through" while keeping the two sentences that are the student's job and the prediction task's own promise that the ending arrives later; `WrangellStElias_Intermediate_L8_Packet.html` and `PapaHemingway_Proficient_L3_Packet.html` had their citeboxes reworded from "Your teacher will stop the recording / play two parts" to "You will hear the first part of it" / "You will hear two parts of it," which keeps the fact and drops the narration. Packet wording with no `.md` source, so under Conventions §G no lesson document changed and no version code moved. |
| 2026-09-14 | v2.43 (Style Guide) | **An organizer cell is counted like an answer line, and its rows follow the delivered parts.** Per user direction on Listening/Speaking Module 2 Advanced Set 1 Lesson 1, straight after its listen was segmented: "let's update the listening note boxes into 4 rows corresponding to those segments. include the timing in the segments. give more vertical space to write the notes." **What was actually wrong:** §F's "Counting answer lines" already said answer space is counted from the expected answer and never eyeballed, but it governed `.ans-line` only. `.notes-table td` ships a fixed `height: 38px`, so a cell holding one link of a chain and a cell holding everything a student took from a five-minute segment printed identically - and the rule that would have caught it did not reach organizer cells. The row structure was the same gap one level up: HubbleRepair's organizer kept the six links of its sequence chain while the audio now stops four times, so during each pause a student had two half-boxes open and no box that corresponded to what they had just heard. **The rule:** §F extended - an organizer cell's height is counted from what its row holds, a phrase-sized cell keeping the base height and a cell standing in for one delivered part taking the new `.notes-tall` modifier (§H.1, `height: 104px`, three to four handwritten lines); and where the input is delivered in named parts (Quality Standards §D15) the organizer carries one row per part, in playing order, each naming its part and its timing ("Part 3 · 5:27-10:18"), so a student who loses the thread knows which box they are in. Self-check item 31 added. **Swap test passed:** the same failure is available to a Passage Reading organizer filled per chapter and to a Writing planning table filled per Mentor rung, and `.notes-table` is a shared class, so the rule lives here rather than in one modality's packet prompt. **Prompts swept:** one restated the organizer's shape in its own words - Listening/Speaking's Student Packet prompt §2.6, `_v2.8.md` to `_v2.9.md`, which now branches on whether the lesson plays straight through or in segments and requires the student-side parts to be numbered from 1 with "Segment" kept off the page (§E); its self-check item 4 extended. Passage Reading's packet prompt names no organizer shape and inherits. **Generated content:** HubbleRepair only (`2.1.1.5`), the one lesson with a segmented play - four rows replacing six, each carrying its part number and timing, the six chain links grouped into them rather than split, the board chain regrouped to match, and the instruction rewritten to say the audio stops after each part. The `.notes-tall` declaration was added to that packet alone rather than to every packet embedding `.notes-table`, per the precedent set when `.idiom-tag` was stripped from packets that carried it unused. |
| 2026-09-14 | v1.32 (Quality Standards) | **A long input is taken in in defined parts, not one unbroken pass: new §D15.** Per user direction on Listening/Speaking Module 2 Advanced Set 1 Lesson 1, after its runtime was corrected: "I don't want to just play this long audio. I only want to play segments." **What was actually wrong:** nothing in `shared/` said how a long anchor input reaches a student. Passage Reading had solved it years-deep in its own prompt, through the lettered `[STOP & CHECK]` pause points embedded in its anchor text, but the rule was never stated program-wide - so Listening/Speaking's Lesson prompt §0.2 said the opposite in so many words ("Play the full source once"), and HubbleRepair duly played an 11.5-minute block against a 5-to-8-minute Band range. **The rule:** where the delivered input runs past its own modality's ceiling for that Band, it is delivered in the named parts the lesson already labels, with a short catch-up checkpoint between them that reveals no wording and answers nothing, so a segmented first pass is still the single uncued first contact §D12 requires; parts follow the source's own order and skip nothing inside the span. Two stated consequences: a beat that re-delivers a named part budgets at least that part's length, and where a Level's purpose points outside the part being re-delivered it is the purpose that gets re-pointed, not the part stretched. An input inside its ceiling is still delivered whole. Self-check item 38 added. **Swap test passed:** "text" for "clip" gives exactly Passage Reading's existing `[STOP & CHECK]` practice, so the rule is program-wide and lives here. **Repaired in the same pass:** §D14 ("A concept met for the first time is taught, not just ruled") had been written as a bare numbered list item rather than a `**D14.**` rule, while self-check item 34 already cited it as D14; it now carries its prefix and matches its siblings, and the new rule took D15. **Prompts swept:** `grep` across `*/*/prompts/` found one prompt restating the pattern in its own words - Listening/Speaking's Lesson prompt, `_v2.8.md` to `_v2.9.md`, whose Phase 2 bullet now branches on whether the played span fits §0.2's range and whose self-check gains items 3b (extended) and 3c. Passage Reading's Lesson prompt already implements the rule and cites nothing contrary, so it inherits; Academic Writing has no played or continuously-read anchor input. **Generated content:** HubbleRepair swept in full (`2.1.1.4`) - Phase 2 now plays its four segments with 30-to-45-second pauses mapped one-to-one onto the sequence chain's cells, Phase 4 Beat 2 re-delivers Segment 5 alone (3:13 inside a 5-minute beat) with all four Levels' purposes re-pointed into it, which also clears the open item flagged there earlier today. Five further lessons play a span past their Band's range in one block and need segment boundaries chosen from their transcripts, which is new content; each is named in Listening/Speaking's `Index.md` Pending work. |
| 2026-09-14 | no version change (§D13 sweep completed) | **§D13's content sweep is finished; the entry below that claimed it was overstated.** Per user report on Listening/Speaking Module 2 Advanced Set 1 Lesson 1, the packet's Unit 1A: "I added in the reading passage lessons that I wanted the lead ins to be less guessy more topic." **What was actually wrong:** not the rule and not the prompts. Quality Standards §D13 already says a hook names topic, situation and text type before its first question and forbids any hook question whose answer lies only in withheld material; Quality Standards §F item 27 and Style Guide §I item 24 already check it; all four prompts that restated the old Mystery Quote definition were already bumped. What did not happen was step 4 of the generalization pass. The v1.18/v2.30 entry below claims "all 20 lessons reframed in this pass"; in fact only Passage Reading's Module 2 lessons were, and an audit of every hook in all 81 generated packets found **13 lessons (14 files)** still opening on a question whose answer is the withheld material. Because the rule and its self-check items were already correct, nothing in `shared/` changed and no version stamp moved; this row exists so the earlier entry's claim is not left standing. **Swept now, `.md` and packet together, each bumped one version:** Listening/Speaking LostKitchen `1.1.1.4` and its TOEFL fork `1.1T.1.8`, LivingTextbooks `1.1.2.5`, HighLine `1.2.5.3`, HubbleRepair `2.1.1.2`, HowAboutThis `1.1.4.1`, NewBakery `1.1.1.4`, Backpack `1.1.3.3`, RosettaStone `1.2.7.3`, FireRescue `2.1.2.3`, PapaHemingway `1.1.3.2`; Passage Reading VinylComeback `1.1.3.2`, LostCat `1.1.4.3`, RunningShoes `1.1.3.2`. Academic Writing has no activation hook and is untouched, as before. Two incidental fixes found on touch: VinylComeback's `.md` hook quoted a different line from its own packet's and was aligned to the packet's, and RunningShoes' `.md` carried no `**Version:**` line at all and now does. Every affected Band's `Rotation_Log_<Band>.md` carries the matching entry. |
| 2026-09-14 | v2.42 (Style Guide) | **"Everyone does this one" is stated once in the set's lead-in, never on the Tasks themselves: §F extended.** Per user direction on Passage Reading Module 2 Intermediate Lesson 1, twice ("remove Everyone does this one", then "dno't need Everyone does this one. and Everyone does this one."): four un-starred Tasks across the lesson's two Units each carried the sentence. An un-starred Task is already an everyone-Task - the absence of a star tag is the notation that says so - so the sentence restates the page's own convention on the line a student is least likely to read. §F already sanctioned the lead-in form ("Everyone does Tasks A and B. Then choose one of the others.") and now says it belongs there only. Added as self-check item 30. Swept: no prompt restated it; `Tortuguero_Intermediate_L1_Packet.html` was the only packet carrying it (all four instances removed, `2.1.1.4`), and Writing's `MyLastMeal_Advanced_L1_Packet.html` already uses the compliant lead-in. |
| 2026-09-14 | v1.31 (Quality Standards) | **A written step whose product is already printed is a copy, not a step: §C11 extended.** Per user report on Passage Reading Module 2 Intermediate Lesson 1's Task C, on the wording the §C16 pass had just produced ("this is given.. in the example"): "circle the pair the sentence goes with, then write the sentence's two past forms" is finished by copying the frame sentence directly above it. Rule states the test - if a student could fill the line without doing the thinking step, it is a copy - and carves out the two cases that are not copies: saying a printed word out loud (decoding is the production at a reading Level) and writing down which printed thing the student picked (a judgment the page does not make). Self-check item 30 extended. Swept: no prompt restated the pattern; all four Module 2 Intermediate Set 1 lessons carried it at Levels 2 and 3 ("write the word on the line under that photo," "write the two words on the lines under the pair") and lost the copy step, keeping the decode-and-match the CSV row actually assigns. Writing's "Better of two" shape was checked against the carve-out and left alone. Stamped v1.31, not v1.30: the Closing Transfer Check entry below had claimed v1.30 without bumping the file's own header, which this pass corrected. |
| 2026-09-14 | v1.30 (Quality Standards) | **A Closing Transfer Check's stated length has to hold the skill it transfers.** Per user question on Passage Reading Module 2 Advanced Lesson 1 ("how do you do it in one sentence but have order"). **What was actually wrong:** all four lessons in the Set closed with "In one sentence, tell your partner..." while also asking for the events in order with one moment made to matter - and the Module's verb is Narrating, whose Level 5 row is an *extended* account. The cap and the demand contradicted each other in the same instruction, so a student could only obey one; Lessons 2 and 3 had also gone grammatically adrift trying to fit both ("with one moment in it made the moment that mattered"). §D1 already required the Spotlight and the Check to name the same skill, but nothing required the Check's form to leave room for that skill, which is how a one-sentence sequence got written four times without tripping anything. Swap test: a one-sentence cap on a Narrating check fails identically in a Listening/Speaking or Writing lesson, so this is program-wide. §D2 gains it; §F gains item 37. **Swept:** the four Module 2 Advanced Set 1 lessons, now three or four sentences, with the two broken sentences repaired. A repo-wide check of every lesson's Closing Transfer Check found no other one-sentence cap on a sequence. Versions to `2.1.1.18`, `2.1.2.11`, `2.1.3.11`, `2.1.4.11`. |
| 2026-09-14 | v1.29 (Quality Standards) | **An instruction may only call something "missing" where the page prints a blank: §C16 added.** Per user report on Passage Reading Module 2 Intermediate Lesson 1's Task C ("the missingwords isn't obvious what words are missing"): the frame sentence "I walked and then I counted." was printed complete, so "write the two missing words here" named nothing a student could see. Rule states the mechanical test (point at what the instruction calls for; a student reading only the page must be able to point at the same thing) and requires such an instruction to name what to write by its place in the printed material instead. Added as self-check item 36. Swept: no prompt in any modality restated the pattern, so none was bumped; four generated packets carried it — Intermediate `Tortuguero_Intermediate_L1_Packet.html` (reworded to "the sentence's two past forms, in the order they happened") and Beginner `SchoolBag_Beginner_L1_Packet.html`, `BeachPostcard_Beginner_L2_Packet.html`, `LostCat_Beginner_L4_Packet.html`. Same pass fixed §C's duplicate item numbering (the trailing 11/12 became C14/C15) and §F's duplicate 28/29, updating the self-check citations to match. |
| 2026-09-14 | v2.41 (Style Guide) | **A packet never names an artifact it does not print or locate.** Per user direction on Passage Reading Module 2 Advanced Set 1 ("okay don't work in a group. just say do that task and get together and share. it doesn't have to be a group porject. for task A don't ask for a groups chart"). **What was actually wrong:** all four packets in that Set opened Investigate the Text with "build your group's chart together," and each one's Task A then told the student to write "in the left column of your group's chart" - a chart that exists only on the teacher's board and is printed nowhere. A survey of all 84 packets found none prints a collaborative-matrix grid, and no rule anywhere required or caught this: "Collaborative Evidence Matrix" appears zero times in this Style Guide and zero times in Generation Quality Standards, and neither v2.8 packet prompt has a rendering section for Day 2 Phase 2 at all. The other 13 Reading packets avoid the problem only by never using the chart noun. Listening/Speaking already had the compliant form in four Advanced Module 1 Set 2 packets ("Add one row to the class chart on the board. Write the row here first."), which is now the rule's cited example. Swap test: a clip's organizer or a scenario's plan named but not printed fails identically, so this is program-wide. §F gains the rule, covering both halves - the unprintable artifact and the group deliverable a single student's page cannot hold; §I gains item 29, and §I's duplicate "25" numbering was corrected in the same pass (old 25-27 are now 26-28). No CSS was added: Reading gets no table class, because the chart is the board's, not the packet's. **Swept:** the four Advanced Module 2 Set 1 lessons and packets, whose Investigate the Text lead now assigns one Task to one student and then shares, and whose Level 4 cell prompt no longer names a chart. Versions to `2.1.1.14`, `2.1.2.8`, `2.1.3.8`, `2.1.4.8`. No prompt bumped: every packet prompt cites Style Guide §A-§D by name rather than restating §F, so all three inherit. |
| 2026-09-14 | v2.40 (Style Guide) | **`.byline` unscoped, and every printed text now names its source.** Per user report on Passage Reading Module 2 Advanced Lesson 1 ("the second text should probably say here's a note (curently we have ot guess which is the note)"). **What was actually wrong:** a stylesheet scoping bug with a content consequence. §B defined the class as `.article .byline`, but the Reading packet prompt's §2.10 puts a second comparison text in a `.refresher` block, so a second text had no class available for a source line. The packet had therefore dropped the line its `.md` supplied ("From a trail association's route notes"), leaving a block headed only "The Laugavegur: Four Days, Four Huts" while the items referred to "the route notes" and expected the student to work out which was which. Nobody had noticed because the anchor text always sits in `.article`, where the class did work. Swap test: a second source needing to say what it is holds for a clip transcript excerpt or a second scenario just as much as a second text, so both the selector fix and the rule are program-wide. §B's selector is now plain `.byline`; §F gains a paragraph requiring every printed block of source material to carry one, and forbidding teacher-facing provenance in it ("Invented for this lesson"). **Swept:** all 37 packets' `<style>` synced to the unscoped selector, across both the expanded and collapsed rule forms. All four Module 2 Advanced second texts gained a byline, Lesson 3's anchor gained the one its `.md` never had, and each Task D lead now names the text instead of saying "the second text." Versions to `2.1.1.10`, `2.1.2.7`, `2.1.3.7`, `2.1.4.7`. |
| 2026-09-14 | v1.28 (Quality Standards) | **A sort's categories must fit the material it prints: §C12 added.** Per user question on Passage Reading Module 2 Advanced Lesson 1's Level 6 sort ("'she sat down on a rock and did not get up for a long time' is this E or R? feels like both"). **What was actually wrong:** the student was right and the item was not answerable as specified. The Level 6 Reading row asks for "separating stated events from clearly-signaled reactions," and the item turned that into a two-way sort; but in a narrative an action routinely does both at once, reporting what happened and showing how somebody took it in the same clause. Several printed lines had two defensible homes, so a student who read well was penalised, and the key was arbitrary. Nothing in §C covered this: §C1 governs duplication, §C2 triviality, §C11 repeated templates, and none of them says a sort's categories must exhaust its own material. New §C12 requires every printed line to have exactly one defensible category, and where the material honestly supports "both," both is a category rather than a wrong answer; it names the narrative case explicitly and requires testing every line against every category before printing. Swap test: the failure is a property of how a classification item is built and is identical for a clip or a scenario, so it lives in §C. Matching self-check item §F 29. **Swept:** all four lessons' Level 6 sort rebuilt as E / R / B, keys E,B,R,E,B / E,B,R,R / E,E,B,E,B / E,R,E,B,E,E, still varying in length, split and order per §C11. Versions to `2.1.1.9`, `2.1.2.6`, `2.1.3.6`, `2.1.4.6`. |
| 2026-09-14 | v1.27 (Quality Standards), v2.39 (Style Guide) | **§C9's two-source excerpt floor is conditional on the source not being re-inspectable, and inverts where it is.** Per user report on Passage Reading Module 2 Advanced Set 1's Level 7 task ("in that question question 1, it tells you where to look so it seems easy"). **What was actually wrong:** the item printed a table naming the three shared subjects and pairing both texts' quotations, then asked what each writer's wording was built to make the reader conclude. The Level 7 Reading row has the student read two narratives and assess how each writer's structural choices shape the reader; locating and pairing the comparable moments is most of that, and the table had done it. The lesson was following §C9, which requires at least three verbatim excerpts per source on shared subjects. §C9's own stated reason is that "a student cannot rewind the clip or open the second source mid-task, so the page carries the comparison's evidence" - a rationale that holds for Listening/Speaking, where the source is audio, and fails for Passage Reading, where the packet prints the anchor text and the second text in full on the same page. So the rule was right where it was written and backwards where it was inherited. §C9 now states the condition explicitly: where the source cannot be re-inspected the floor stands, and where both sources are printed in full the floor does not apply and pre-extracting is itself the error, with the item saying how many shared subjects to find and the page giving ruled space to write each side's wording into. Swap test: the distinction is not between modalities but between re-inspectable and not, which is why the rule is stated that way rather than as a Reading exception. Style Guide §F's compare-layout paragraph and §I item 19 carry the same condition; the `.match-list`/`.match-row` classes still serve both cases, filled for the excerpt case and blank for the find-it case. **Swept:** all four lessons' Level 7 item, `.md` and packet, versions to `2.1.1.7`, `2.1.2.4`, `2.1.3.4`, `2.1.4.4`. |
| 2026-09-14 | v1.26 (Quality Standards) | **A stem may not perform the Level's own verb, and a template repeated across a Set must vary its key: §C9 extended, §C11 added.** Per user report on Passage Reading Module 2 Advanced Set 1 ("took at task C questions. aren't some of them really obvious"). **What was actually wrong, in two separate ways.** First, Task C item 1 in all four lessons named the pacing contrast in its own stem, then asked what it made the reader weigh more heavily. The Level 6 Reading row has the student *identify* pacing choices, so the stem was doing the graded work, and what remained was near-tautological: the stretch given more space is self-evidently the weightier one. §C9 already forbade a stem stating what an item asks for, but nothing said a stem may not perform the *verb* the can-do assigns; it does now, with the rule "name the place to look, never the thing to find." Second, Task C item 2 was the same four-line sort with the same key, E, R, E, R, in all four lessons: an even split solvable by elimination (C2), a fixed order learnable after one lesson, and every reaction line marked by a word of absence ("nobody," "without," "never," "hardly") so the surface cue, not the judgement, carried the item. Nothing in the file covered this: §C1 governs duplication *within* one Level's set in one lesson, and §D6's adjacency rules cover genre, hook, strategy and protocol, not item templates. New §C11 requires a recurring template to vary its option count, category split and answer order across a Set, and to keep at least one member of each category free of the category's surface cue. Swap test: both failures are properties of how an item is built and read identically for a text, a clip or a scenario, so both stay in §C. Matching self-check items: §F 23 extended, §F 28 added. **Swept:** all eight affected items across the Set's four lessons rewritten, `.md` and packet, versions to `2.1.1.4`, `2.1.2.2`, `2.1.3.2`, `2.1.4.2`. The wider audit of existing items against §C9 and §C11 is the Pending-work entry already logged in all three modalities. |
| 2026-09-14 | v2.38 (Style Guide) | **The picture-choice grid is a base class, not a Listening/Speaking delta.** Found generating the first Passage Reading packet whose objective ends in matching a decoded sentence to a picture (Intermediate Module 2 Narrating, where both the Level 2 and Level 3 rows are picture rows). §F already required, for every modality, that "a genuinely picture-based item embeds its real images (`.pic-options`, an `<img>` inside each `.pic-box`, the caption in `.pic-label`)", but those four classes were defined only in §H.1, and §H.2 says a Passage Reading packet has no delta CSS - so the layout §F mandates could not be built without a selector §I item 13 forbids. Same shape as the `.match-list` move at v2.29, and fixed the same way: the classes move to §B beside `.photo` and `.image-caption`, §H.1 keeps the genuinely Listening/Speaking ones, and §H.2 names both moved groups. No rule text changed, so no packet already on disk needs regenerating: every existing Listening/Speaking packet already carries these exact declarations in its own `<style>`. The two Listening/Speaking prompts that listed the classes under §H.1 were corrected in place, without a version bump, because the edit only moves a pointer to where a class is defined and restates no rule - and because both prompts were in use by a concurrent session, where a rename mid-run would have broken it. |
| 2026-09-14 | v1.25 (Quality Standards) | **A stem's own premise can contain the answer, not just its parenthetical: §C9 extended.** Per user report on Passage Reading Module 2 Advanced Lesson 1's Task A ("the answer to said before is that it's easy..."). **What was actually wrong:** the item read "On the first day, the writer stops telling people the trail is easy. Which paragraph says this, and what had the writer said before?" A stem saying somebody *stopped saying X* has already said X, so the item answered itself. §C9 existed and §F item 23 checks it, but every example in the rule was a **parenthetical** appended after the question, so a self-answering premise in the stem's own first sentence read as compliant. The rule was not missing; its examples were too narrow to catch the form. §C9 now names the pattern directly: a stem asserting a change, reversal, or negation hands over the state it changed from and cannot then ask for that state, with "she no longer uses the old method, what did she use before?" and "the town rebuilt after the flood, what happened to it?" given as the same shape. The fix is to write the premise as the neutral fact that locates the moment and let the item ask for the content. Swap test: the failure is a property of how a question is worded and is identical for a text, a clip, or a scenario, so it stays in §C9 rather than any modality's prompt. **Swept:** the item was the only one of its shape in the Set; the other three lessons' Task A items state an event and ask for a stated reason, which is sound. A wider audit of existing items against the extended rule is not done and is logged in Passage Reading's `Index.md` Pending work. |
| 2026-09-14 | v2.37 (Style Guide) | **A star-rated set is routed by a plain "Choose one task," not by an assigned star count.** Per user direction on the Module 2 Advanced Reading packets ("can it be more like choose a task to do. I don't like 'your number of stars'"), immediately after v2.36 had replaced teacher-narrated routing with "Do the Task with your number of stars." **What was actually wrong:** §F was out of step with the repo's own practice, and v2.36 had codified the wrong half of it. Three Proficient Listening/Speaking packets (NeoNurture, PapaHemingway, BeninPlaque) already carried the right form, "Choose one task. When you finish, find someone who did a task with a different number of stars and tell them what you found," and had done so since before either of today's entries; §F meanwhile both banned framing the set as a choice and required the teacher to be the one deciding, so the rule contradicted working packets in two directions at once. Routing by an assigned star count fails for its own reason, separate from teacher-narration: it presumes the student has been handed a number and then labels them with it, which is the tier-labelling §F already forbids elsewhere. **The rule now:** the lead-in is "Choose one task."; the star count still appears in the share instruction, where it does real work pairing students across different Tasks; the mixed case reads "Everyone does Tasks A and B. Then choose one of the others."; the game-framing ban narrows to actual game framing ("choose your own adventure," "pick your challenge," "level up") so it no longer catches a plain instruction to choose; and the teacher still steers individuals in the room, that steering simply is not printed. §I item 6 updated to match. **Swept:** the four Module 2 Advanced Reading packets and one Academic Writing packet (`MyLastMeal_Advanced_L1_Packet.html`, the only file using the assigned-star variant). All ten routing lead-ins in the repo now read "Choose one task." Packet wording with no `.md` source, so under §G no lesson document changed and no version code moved. |
| 2026-09-14 | v2.36 (Style Guide) | **A star-rated task set routes by the stars, never by what the teacher does.** Per user report on Passage Reading Module 2 Advanced Set 1's packets, quoting the lead-in and saying "Don't say Answer the Task your teacher points you to." **What was actually wrong:** not a missing rule. §F already said "Which star a student works at is decided live by the teacher, not narrated on the page," and the phrasing narrated exactly that decision. The rule was stated but not enforceable: nothing named the form the violation takes, so it was written into four packets in a row without tripping any check, and an existing compliant packet (`MyClothing_Intermediate_L3_Packet.html`) already carried the right wording, "Everyone does Tasks A and B. Then do the task with your number of stars," with no rule pointing to it as the model. §F now names the failure by example ("answer the Task your teacher points you to," "your teacher will tell you which one") and gives the compliant form in the student's own terms, and §I item 6 checks for it. Swap test: routing a differentiated task set is something every modality's packet does, and the rule reads identically for a text, a clip, or a scenario, so it stays in the Style Guide rather than any one prompt. **Swept:** the four Module 2 Advanced Reading packets, the only files in the repo carrying the phrase. Packet-only wording with no `.md` source, so under §G this is a translation-layer fix: no lesson document changed and no version code moved. |
| 2026-09-14 | v2.31 (Style Guide) | **Footnote references actually print as superscripts, and there is now one markup form for them.** Per user report on Passage Reading Module 2 Advanced Set 1 Lesson 1 ("Landmannalaugar1 the superscript isn't properly set in the reading"). **What was actually wrong:** §B's `.footref` rule was `font-weight: 700` and nothing else, so it never raised anything; a reference written as a plain digit rendered as a bold numeral run straight into the preceding word. Reading's own Lesson prompt §0.9 Part B has always specified "superscript numeral at first mention," so the stylesheet simply did not implement the spec it was serving. With no working rule and no stated markup convention, three forms had drifted into the repo: a plain ASCII digit (broken, the reported case), a Unicode superscript character (looks right, cannot be styled, unevenly covered in print fonts, and does not match the footer's own numeral), and a `<sup>` wrapper; `WynwoodWalls_Intermediate_L4_Packet.html` carried two of the three in the same file. **Swap test passed:** `.footref` is a base §B class and a footnote marker behaves identically in a text-, clip-, or scenario-based packet, so the fix is program-wide, not Reading's. **The fix:** §B's `.footref` gains `font-size: 0.72em`, `vertical-align: super`, and `line-height: 0` (the last so a raised numeral does not open up the line's leading); §C gains a markup convention naming `<span class="footref">1</span>` with a plain ASCII digit as the single form, and forbidding both the `<sup>` wrapper, which would raise it a second time on top of the new CSS, and Unicode superscript characters; §I gains item 25. The CSS and the markup had to land together, since raising an already-raised character is a new bug. **Swept:** all 37 packets carrying the rule had their `<style>` synced (§G's "a packet generated before a §B rule existed gets its `<style>` synced to the current §B in the same edit"), across three different indentation styles and one collapsed one-line rule; all 17 footnote references repo-wide, in 7 packets, normalised to the single form. **No lesson version bumps and no `.md` changes:** this is a pure formatting fix under §G's test, since nothing a teacher reads in the Markdown changed. |
| 2026-09-14 | v1.24 (Quality Standards) | **A pointer into printed material is counted, and says what it points at: §C13.** Per user report on Academic Writing Advanced Module 2 Set 1 Lesson 1 ("After you have fixed Task A, rewrite its third and fourth sentences as one sentence, without repeating 'my roommate'. it's not the third and fourth sentence"). **What was actually wrong:** it was the second and third. The editing paragraph's second and third sentences are the roommate's two consecutive reported lines, which is what makes them combinable without naming her twice; the third and fourth are one of her lines and one of the narrator's own thoughts, which cannot be joined that way at all. The instruction was written from memory of how the paragraph went, and the task it gates (Levels 6-7's combining item) was unperformable as printed. §C10 governs whether an instruction's action is possible on the page; nothing governed whether its *reference* lands where it says. **The rule:** §C13 requires every ordinal pointer into printed material to be verified by counting on the page, and, where it reaches the packet, to name the content it points at as well as the position, since a bare ordinal silently re-points the moment anyone edits the passage. A pointer into the student's own writing needs no gloss. New §F item 33. **Swap test passed:** a Reading packet's "paragraph 3" and a Listening packet's "the second thing she says" fail in exactly this way. **Prompts swept:** none instructs how to write a positional reference; none bumped. **Generated content:** all 18 ordinal pointers in the three modalities' packets were counted against their pages. This was the only wrong one. The rest either point into the student's own draft ("your second paragraph," "the first sentence of each middle paragraph") or are correct, including Proficient Module 2 Lesson 1's "last paragraph of the model essay above" and Advanced Module 2 Lesson 2's "the last sentence of the first paragraph," both verified against the essays printed above them. |
| 2026-09-14 | v1.23 (Quality Standards) | **A concept met for the first time is taught, not just ruled: §D14.** Per user report on Academic Writing Advanced Module 2 Set 1 Lesson 1 ("the grammar concept is hard we need more background. with lots of example. for example, this is direct speech. this is indirect speech. it has three featuers, no quotation mark, tense shift, pronoun shift. this is the reporting verb. for each example show the tense shift, pronoun shift"). **What was actually wrong:** the Grammar box opened on the backshift rule - "when your reporting verb is in the past, the verb inside usually steps back one tense" - over a two-column table of quoted lines and their reported versions. It never named direct or indirect speech, never named the reporting verb, never listed what changes, and left the pronoun shift to a single sentence after the table. A student who already knew reported speech could use it; a student meeting it had a pattern to copy. Nothing in `shared/` covered the inside of a teaching callout: §D's rules place callouts and the Style Guide's govern their markup, but neither says what has to be in one. **The rule:** §D14 sets a floor for a first introduction - name the thing, show it beside what it contrasts with (both written out), number the features that change, and annotate every worked example feature by feature, one column per feature, saying "none" in a column where a feature does not apply rather than leaving the cell blank. Stating a rule over before-and-after pairs is explicitly not enough. The floor is for first introductions only; later practice may state the rule in one line. New §F item 32. **Swap test passed:** a reading strategy and a listening signposting move introduced the same way fail identically. **Prompts swept:** no prompt specifies what a teaching callout contains; none bumped. **Generated content:** every two-column teaching table in all three modalities was read. The reported-speech box was rebuilt (`2.1.1.3`): direct and indirect speech named and set side by side with the reporting verb labelled in both, the three features numbered in their own table, a tense-shift reference, and five worked examples carrying a tense-shift and a pronoun-shift column, the last row moving two pronouns in opposite directions. Two Proficient boxes already named their concept and its features but showed bare before-and-after examples, and gained per-feature columns: Module 1 Lesson 2's reduced relative clauses (`1.1.2.1`, what dropped / which participle and why) and Module 2 Lesson 2's fronting (`2.1.2.2`, what moved / whether the verb inverts, the non-inverting row now saying so in its own column). Every other match was a contrast or reference table, not a first introduction. |
| 2026-09-14 | - (repo incident, no rule change) | **Eight `Rotation_Log_<Band>.md` files were truncated by a tooling error during the "frame" sweep, and restored.** A logging helper was written as `open(path,'w').write(open(path).read()+note)`; Python evaluates the `open(path,'w')` first, which empties the file, so the read that follows returns nothing and each file was left holding only the note. Affected: all four Listening/Speaking Band logs, Passage Reading's Intermediate log, and Academic Writing's Beginner, Intermediate and Proficient logs. All eight were restored from the last commit and this session's own entries re-applied. **Three had uncommitted work from earlier the same day that the commit did not hold, rebuilt from the surviving cross-records** - Academic Writing Intermediate's whole `## Module 2: Narrating` section (from the approved plan 1.0, `Index.md`, `Changelog.md` and the four lesson documents), Passage Reading Intermediate's `## Module 2` section (from its plan and changelog row), and Listening/Speaking Advanced's Lesson 4 Block42 entry (from the lesson document). Each rebuilt block says so in place and names what it was rebuilt from, so a later reader can tell reconstructed text from original. Anything those three sections held that is not derivable from the surviving records is gone. The repo has no test to catch this; the guard is that these files are only ever appended to by reading fully into a variable first, and that work is committed before a multi-file sweep. |
| 2026-09-14 | v2.35 (Style Guide) | **"Frame" is a teacher's word: §E gains its row.** Per user report on Academic Writing Intermediate Module 2 Set 1 Lesson 1 ("in the instruction, instead of complete the frame, we can just say complete the sentence"). **What was actually wrong:** §E exists so that no teacher-facing term reaches a student page, and it already carried rows for Task Level, Skill Spotlight, protocol names, hook names and internal item labels - but not for the one piece of jargon printed more often than any of them. "Frame" is the name of a teaching device; the student's job is to finish a sentence, and a student who asks what a frame is gets a lesson in pedagogy rather than an answer. The term was never translated because §F speaks of frames in its own voice ("a fixed frame is printed once per masthead section"), which reads as licence to print the word. **The rule:** a §E row mapping frame / sentence frame / fixed frame / two-slot frame to **sentence**, with the exemption that matters: the word stays where it is the content - a backpack's internal frame, a loom's wooden frame, the verb ("how the report frames the story") - and stays in the lesson `.md`, which is the teacher's document, and in this guide. No new §I item: item 1 already asks whether any §E left-column term appears in student-facing text, and this is now one. **Swap test passed:** the word is printed in all three modalities and is jargon in each. **Prompts swept:** the prompts are teacher-facing documents and say "frame" correctly throughout; none bumped. **Generated content: 43 packets across all three modalities**, every one carrying at least one printed "frame," swept by phrase rather than by word so the content senses survived: Backpack's internal and external frames, TapestryFactory's wooden frame, and the verb in four Reading and Listening comparison stems are all untouched. Each swept lesson's version code and `.md` moved with it; the two Listening/Speaking assessment packets carry no version code (Conventions §G). |
| 2026-09-14 | v1.22 (Quality Standards) | **A worked example never answers an item its own Task asks: §C9 extended.** Per user report on Academic Writing Intermediate Module 2 Set 1 Lesson 1 ("the example provided is the answer to sentence 2 right? so let's just ask them to do 5 and 8 and refer to sentence 2 as an example"). **What was actually wrong:** Unit 1A's Level 5 Task asked students to rewrite sentences 2, 5 and 8 so each opens by saying when it happened, and printed "Yesterday afternoon my phone died because I had left the charger at home" as its model - which is sentence 2, rewritten. A student who noticed had two items to do, one who did not was marked against a peer who copied. §C9 already forbade a stem stating what its item asks for and exempted frame models, but said nothing about a starter that happens to be a finished item of the same task. **The rule:** §C9 now holds worked examples to the same line - build the example from something the task does not ask for, or drop that item from the ask and name it as the worked one - and prefers the second where it applies, since showing an item before and after makes the change visible where a finished sentence only shows where to arrive. New §F item 31. **Swap test passed:** a model answer that is one of the printed comprehension questions fails identically in a Reading or Listening packet. **Prompts swept:** none; the rule lives in a section every prompt cites. **Generated content:** every `.starter` and model block in all three modalities was compared against the items printed in its own Task. Two hits. The reported one (`2.1.1.9`): sentence 2 now prints before and after, and only 5 and 8 are asked. Listening/Speaking Beginner Module 1 Lesson 1 (`1.1.1.2`): Unit 1A's Task C modelled "It is big and heavy. = the book" directly above an item asking the student to complete "The book: It is ___ and ___" from a bank containing big and heavy; the model now uses the pen, which no item in either Unit describes. Its Unit 1B Task C keeps the book model, whose own items are the pillow and the map. Everything else matching was a fixed-form frame model, exempt under this same rule. |
| 2026-09-14 | v1.21 (Quality Standards) | **A condition names what the answer must do, not an incidental shape: §C12.** Per user report on Academic Writing Intermediate Module 2 Set 1 Lesson 1 ("question feels awkward 'a time phrases of two or more words? why not one?"). **What was actually wrong:** Unit 1A's Level 5 Task asked students to rewrite three sentences so each "opens with a time phrase of two or more words." There is no answer to the user's question, which is the point - a one-word "Yesterday" places the event exactly as well as "Yesterday afternoon," so the threshold marks nothing and no teacher can justify it at the desk. It also displaced the real demand, which is that the sentence start by saying when it happened. Nothing in `shared/` distinguished a condition that measures something from one that only looks precise: §C2 governs difficulty, §C6 requires counts to be counted rather than estimated, and both read as encouragement to state a number. **The rule:** §C12 requires a condition inside an instruction to name the function the answer performs, and keeps counts only where they measure something real - a Level's own CSV ceiling, a count that is the task ("write three sentences"), or a structural minimum that changes what the answer is ("at least two words that keep the events in order," where one word cannot sequence three events). New §F item 30. **Swap test passed:** "quote at least five words of what the speaker said" and "underline a phrase of three or more words" fail identically in the other two modalities. **Prompts swept:** none imposes a length threshold on a feature in its own words; none bumped. **Generated content:** a grep for word-count conditions across all three modalities' lessons and packets found this one instruction and nothing else - the other hits are glossary definitions of "phrase" and "braided," where the count is the definition. Fixed in place (`2.1.1.8`), the Task now reading "Do Task E, then rewrite sentences 2, 5, and 8 so that each one starts by saying when it happened." |
| 2026-09-14 | v1.20 (Quality Standards) | **A tier that adds one demand says so; it does not re-specify the task.** Per user report on Academic Writing Intermediate Module 2 Set 1 Lesson 1 ("For task E, instruction is confusing. how about we say do task D but start 3 of them with the reason part and put a comma after it"). **What was actually wrong:** Unit 1B's Level 5 Task restated Level 4's production task in fresh wording - three sentences instead of six, its own reason-word list (since, as, or given that, not because), its own requirement sentence, its own six answer lines - when the only real difference was that some sentences open on the reason clause. A student had to read two complete sets of requirements to find the one difference, and the two statements had already drifted on sentence count and reason words. §B's Respectful Tiers governs whether a tier is worth doing, never how its instruction is written, and nothing else in `shared/` covered it. **The rule:** §B now requires a higher Level that does the Level below plus one more thing to name that Task and state only the addition ("Do Task D. Start three of your six sentences with the reason part, and put a comma after it."), the student writing in the Task they were sent to. It is explicitly bounded: a tier with its own object, source, ceiling or product is its own task and is still written out in full, which is most of this lesson type's tiers. New §F item 29. **Swap test passed:** a Listening tier that restates the Level below's speaking prompt, and a Reading tier that restates its marking task, fail identically. **Prompts swept:** none states how a tier's instruction is worded; none bumped. **Generated content:** every packet's consecutive star-rising Task pairs were compared by instruction overlap; three were real re-specifications and are fixed - the reported one (`2.1.1.7`), Intermediate Module 2 Lesson 2's Task M, which repeated Task L's two steps verbatim over a reprinted copy of the same paragraph before adding its own two (`2.1.2.2`), and Intermediate Module 1 Lesson 4's Task H, which restated Task G's comparison sentence while quietly dropping G's worked example (`1.1.4.17`). The rest of the ~55 candidate pairs differ in ceiling or product and are exempt by the rule's own terms. |
| 2026-09-14 | v2.34 (Style Guide) | **Answer lines are counted from the answer, at eight to ten handwritten words a line.** Per user report on Academic Writing Intermediate Module 2 Set 1 Lesson 1 ("too many lines for Task A, for Task D where you have to write a sentence, add one line per word under full width"). **What was actually wrong:** §I item 11 said answer space is "sized to the expected answer" and gave no way to count it, so the same packet over-provisioned in one place and under-provisioned in another - twelve lines under an 82-word paragraph that needs nine, and a single line under each of six prompts asking the student to compose a sentence about their own charger. Handwriting is far larger than 14.5px body type, which is the trap: the paragraph prints in five lines and reads as needing about five. **The rule:** §F gains "Counting answer lines" - eight to ten handwritten words per full-width `.ans-line`, counted, with three cases spelled out: copied or rewritten text counted from its own word count (never from how many printed lines it occupies), a student-composed sentence given two lines, and a prompt that states a one-line answer ("Write one line saying what it withholds") kept at one. It also names over-provisioning as its own fault, since spare lines push the next task onto another page and tell the student the answer should be longer than it is. §I item 11 rewritten to match. **Swap test passed:** the word-count arithmetic is medium-independent. **Prompts swept:** none states a line count in its own words; none bumped. **Generated content:** mechanical, all swept - the reported lesson (`2.1.1.6`), the two Advanced Writing editing paragraphs whose rewrite space this same day's v2.32 pass had eyeballed at 12 and 14 lines and which recount to 10 and 11 (`2.1.1.2`, `2.1.2.2`), and nine composed-sentence prompts sitting on one line across eight Beginner and Intermediate packets in all three modalities. Prompts that ask for exactly one line were left alone by rule, which is most of the 78 single-line items the sweep surfaced. |
| 2026-09-14 | v2.33 (Style Guide) | **One continuous procedure is one instruction, not a numbered list.** Per user report on Academic Writing Intermediate Module 2 Set 1 Lesson 1 ("task A 1B, don't make it three questions, just 1 question. you can easily just combine the questions into 1"). **What was actually wrong:** the rule itself. §F's "Instructions as steps" said a Task with more than one action is a lead line plus a numbered list, one action per step, with no test for whether the actions are separable - so read the paragraph, cross out the mistakes, write it out corrected became three numbered steps over one paragraph, preceded by a lead sentence saying what the three steps were about to say. A student performs that as one motion and only the last part leaves anything on the page; numbered, it reads as three questions, which is how the user read it. This is the packet-side twin of Quality Standards §C11 (a step must ask something new), which had been written the same day for steps that re-ask; this one covers steps that never needed separating at all. **The rule:** §F now scopes numbering to actions a student could stop between - each with its own product, target, or choice - and requires a single continuous procedure on one object to be written as one instruction of two or three clauses over its answer lines. The existing ban on "a prose paragraph of chained instructions" is bounded so it cannot be read as forbidding this: it means an unnumbered block burying several separable tasks. New §I item 27. **Swap test passed:** read the passage, mark the evidence, write the answer, and its listening equivalent, fail identically. **Prompts swept:** none restates the numbering rule in its own words; all cite §F. **Generated content:** a sweep of every packet for a 2-3 step Task whose first step only orients and whose parts share one product found three, all in the lesson reported - Unit 1B's Tasks A, B and C, each now one instruction (`2.1.1.5`). Multi-product step lists were left numbered: the Reading Intermediate photo Tasks (a frame to complete, then a word to circle), the speaking-requirement lists, and Unit 1A's choose-and-complete Task, whose three steps leave a marked Word Bank, three completed frames, and a partner read. |
| 2026-09-14 | v2.32 (Style Guide) | **A mark the packet asks for must have the room to make it: "Writing on printed text."** Per user report on Academic Writing Intermediate Module 2 Set 1 Lesson 1 ("Task 1 in 1B, there's no space to write the word above it. how about making them just rewrite the whole paragraph in the underline space rather than listing the words"). **What was actually wrong:** Unit 1B's editing Task printed a seven-sentence paragraph at body leading and told students to cross out each of eight mistakes and write the correction above it. There is nowhere to write. No rule covered the room a mark needs: §I item 11 sizes *answer lines* to the expected answer, and §F's instruction rules govern wording, not whether the page can physically take the writing. **The rule:** §F gains "Writing on printed text" - an instruction to write above, between, or next to packet-printed text appears only where the page has room; a line written into carries the new `.editable` class (line-height 2.5, added to §B beside the answer-space classes); a printed block of more than about two lines is never annotated between its lines but crossed out in place and **rewritten in full on answer lines below**, which also makes the corrected text the product rather than a list of loose words. Crossing out, circling and underlining need no room and are untouched, and a student's own handwriting is theirs to space, so the "cross out the old version and write the new one above it" self-edit instruction stays. Matching §I item 26 added. **Swap test passed:** a Reading packet's fix-the-wrong-word line and a Listening packet's check-what-you-heard line fail identically, and the sweep found both. **Prompts swept:** no prompt restates a writing-room rule in its own words; none bumped. **Generated content: mechanical, so all nine affected lessons were fixed in this pass** across all three modalities - two Writing paragraphs converted to rewrites (Intermediate `2.1.1.4`, Advanced `2.1.1.1` and `2.1.2.1`), two Writing Beginner Tasks pointed at the answer line they already printed (`1.1.1.1`, `2.1.2.1`), two Reading Beginner Task sets given `.editable` leading (`1.1.2.1`, `1.1.4.1`), and two Listening/Speaking quoted lines moved onto their own `.editable` block (`1.2.5.1`, `1.2.7.1`). |
| 2026-09-14 | v1.19 (Quality Standards) | **A step inside a Task earns its number by asking something new: §C11.** Per user report on Academic Writing Intermediate Module 2 Set 1 Lesson 1 ("task E, questions 1, 2, and 3 are the essentially the same question. so why not just make 1 instruction that says underline the time word and circle the reason word"). **What was actually wrong:** Task E gave eight sentences three numbered steps - write S, O, or SO on the line; underline the time word; circle the reason word - but the letter in step 1 is decided by exactly the two words steps 2 and 3 ask for, so a student who can do either step has already done all three. The page then printed eight answer blanks for a code that carried no information the marking did not. No rule caught it: §C1's distinctness test is scoped to *items* in a set, and both packet self-check items that touch multi-step Tasks (Style Guide §F, §I item 6) govern their shape - a one-sentence lead plus numbered steps - never whether the steps ask different things. **The rule:** §C11 requires each numbered step to require a judgment the earlier steps did not settle, names the three shapes that fail (re-marking what a classifying step turned on, naming a category just underlined, restating step 1 in a second notation), says to fold them into one instruction naming every mark to make, and keeps the `.md` activity and the packet Task at the same step count. A later step is still distinct when it acts on the first step's result - sorting, rewriting, explaining. Matching §F item 28 added. **Swap test passed:** classify the evidence, then underline it, then circle the signal word fails identically in a Reading packet and in a Listening one, so the rule is program-wide. **Prompts swept:** no prompt in any modality restated a step-distinctness rule in its own words, because none existed, so none was bumped. **Generated content:** mechanical, swept in this pass - every affected Task named in the modality changelogs, with each lesson's version code and `Rotation_Log_<Band>.md` row bumped. |
| 2026-09-14 | v2.31 (Style Guide) | **A paired-form reference list is a table, not a prose run.** Per user report on Academic Writing Intermediate Module 2 Set 1 Lesson 1 ("format verbs that do not take -ed better. a table/list is probably better rather than current"). **What was actually wrong:** the Unit 1A Grammar box gave the four regular `-ed` spelling rules a bordered `.rule-table` and then printed the twelve irregular pairs the same box teaches (get to got, buy to bought, is / are to was / were) as one unbroken comma run inside a `.model-step` paragraph. The list students actually copy a form out of mid-task was the least scannable thing on the page, and sat directly beneath a table that showed what the alternative looked like. No rule anywhere covered it: §C governed indentation, void elements and CSS formatting only, §F's word-bank markup covers banks of single words with no pairing, and §H.3 defined `.rule-table` without saying what belongs in one. **The rule:** §C now requires any student-facing list of paired items - base form to past form, word to meaning, term to example - to print as the modality's table class (`.rule-table`, `.notes-table`) with a header row naming both columns, never as a comma run of pairs in a `<p>`, `.model-step` or word bank; short pairs may be laid two across to halve the row count and keep the table inside one page break. Matching §I item 25 added. **Swap test passed:** a clip's term-to-meaning glossary and a reading's word-to-form list fail the old shape identically, so the rule is program-wide and lives here rather than in Academic Writing's packet prompt. **Prompts swept:** `grep` across `*/*/prompts/` found no prompt restating a markup shape for paired lists in its own words, so none was bumped; all six packet prompts cite the Style Guide by section and inherit. **Generated content:** mechanical, so swept in this pass - Lesson 1's `.md` and packet both carry the two-across table and the lesson moves 2.1.1.1 to 2.1.1.2. |
| 2026-09-14 | v1.18 (Quality Standards), v2.30 (Style Guide) | **A hook orients before it asks: §D13.** Per user feedback on Passage Reading Module 2 Advanced Set 1 Lesson 1 ("in before you read, I got a feedback saying they want more of a lead in into the topic. they didn't like 'what do you think based on this incomplete data' questions. this lesson does that. it says i'm not giving you much to work with, what do you think"). **What was actually wrong:** not the lesson, which followed its prompt exactly. The Mystery Quote's only definition anywhere in the repo, `Generate_Lesson_Prompt_v3.5.md` §0.7, prescribed the defect in so many words: "a Mystery Quote collects student guesses about **the quote's source** or meaning on the board and checks them off against the text." The source is precisely what the hook withholds, so the prompt was instructing the generator to ask an unanswerable question. No rule anywhere required a hook to give students anything to reason from; the nearest rules, §C2 and §C3, govern items and push the other way, since §C3's "answerable only with the centerpiece" is something a pre-reading hook structurally cannot satisfy. The defect was also systematic rather than incidental: `Generate_Module_Lesson_Plan_Prompt_v2.2.md` requires each of the four hooks be "used once across the 4 lessons," so every Reading and every Listening/Speaking Set contains one Mystery Quote lesson by construction. An audit of all 24 Reading, 21 Listening/Speaking and 32 Writing lesson files found **20 affected lessons (21 files)**: Mystery Quote 13 of 13, Visual Inquiry 7 of 13, Take a Side 0 of 11, K-W-L 0 of 12; Academic Writing 0, since it never adopted the bank ("hook" there is the essay introduction's hook). **The rule:** §D13 requires the hook to name topic, situation and text type before its first question, and forbids any hook question whose answer lies only in material the student has not been given. Withholding itself is not forbidden and the rule is applied per question, not per hook: an uncaptioned photo is fine where what is asked of it is answerable by looking, which is why the sound Visual Inquiries (SagradaFamilia, Kitchen, FlintWater, IberianLynx, NeoNurture and others) need no change. **Swap test passed:** the rule holds when "text" is swapped for "clip" or "scenario" - an unattributed quote, an uncaptioned photograph and an unexplained scenario fail it identically - so it is program-wide and lives here, not in one modality's prompt. Matching self-check items added: Quality Standards §F item 27, Style Guide §I item 24, since the packet is where students actually meet the hook. Style Guide §E's activation-hook translation row also still listed the pre-§D11 names (K-W-L Walk, Stand Up/Move, Four Corners) and omitted Visual Inquiry and Take a Side; corrected in the same pass, and it now states that the orienting sentence is always printed rather than stripped as a teacher-facing term. **Prompts swept:** Reading Lesson v3.5 to v3.6, Reading Module Lesson-Plan v2.2 to v2.3, Reading Student Packet v2.7 to v2.8, Listening/Speaking Lesson v2.6 to v2.7. Listening/Speaking's Module Lesson-Plan prompt cites the bank by name and inherits; Academic Writing is outside the system and is untouched. **Generated content:** all 20 lessons reframed in this pass, `.md` and packet together, since adding an orienting sentence and rewriting a question is mechanical; anything needing a new image is named by path in its modality's `Index.md` Pending work instead. |
| 2026-09-14 | v1.16 (Program Conventions) | **Advanced Modules 1-2 de-paired too; Proficient is now the only complete Pair.** Per user direction on Advanced Module 2 ("in intermedaite module 2, I deviated away from making the two modules a pair. do the same here too. It's just for module 2 advanced and module 2 intermdiate. also adopt the different topic strategy we did in module 2 intermediate"), applied hours after the Intermediate de-pairing that v1.15 recorded. What was actually wrong: the same thing, one Band up. Advanced's Module 2 had been planned that morning as Part 2 of Module Pair 1-2, inheriting Module 1's Two Places to Study Scenario and comparison-essay genre across all four lessons, so it carried the identical monotony by construction; it was caught before any lesson was generated, and only a plan was discarded. Module 2 is replanned standalone on its own food-and-cooking Scenario, with its own genre (Module 2's own row: personal narrative at Levels 4-5, reaction essay at 6-7), which also means its essay Levels draft a complete essay at Lesson 3 rather than an introduction and conclusion around inherited bodies. §C's Standing exception is rewritten: it had cited "Advanced Modules 1-2 is planned but ungenerated" as one of two props for the question being open, and that prop is gone. Swap test: unchanged from v1.15, the Module Pair is Academic Writing-only by §C's own terms, so nothing swaps to Reading or Listening/Speaking. No self-check item added, for v1.15's reason: this records a deferred decision, not a generation rule to enforce at output time. Two Module 1 Sets now end in a hand-off with no receiver, both logged in Academic Writing's Pending work. |
| 2026-09-14 | v2.29 (Style Guide) | **Two-source compare classes promoted from a Listening/Speaking delta to the base stylesheet.** Found while generating Passage Reading's Module 2 (Narrating) Advanced Set 1, where the Level 7 Reading row requires two extended narratives in every lesson: §F mandates a two-source compare layout built on `.match-row`/`.match-src`, but those classes sat in §H.1 as Listening/Speaking deltas while §H.2 said Passage Reading needs no delta CSS, so a compliant Reading packet could not be generated without defining a selector §I item 13 forbids. Swap test: a rule about printing two sources to compare stays true when "clip" is swapped for "text" or "scenario," so the classes are program-wide, not one modality's. `.match-list`, `.match-row`, `.match-label`, and `.match-src` move into §B unchanged; §H.1's class list drops them; §F now names §B as where they live; §H.2 records the move. No new self-check item: §I item 13 (no selector §B/§H does not define) and item 19 (the compare layout's excerpt floor) already enforce this between them, and the defect was the classes' location, not a missing rule. Sweep: Listening/Speaking's two packet prompts cite §H.1 for these classes by name and are bumped, since a pointer that is now wrong is not a pointer a prompt inherits. |
| 2026-09-14 | v1.15 (Program Conventions) | **Intermediate Modules 1-2 de-paired; the Module Pair's status deferred.** Per user report on Intermediate Module 2 Set 1 ("currently the topic is still all my clothing, could we change to something else. can each lesson be a different topic?") and, once the cost of each option was laid out, the user's direction to "say we'll revisit this. for now I just want module 2 that's separate." What was actually wrong: nothing in the generated Set - the monotony was a correct consequence of §C's Module Pair rule, which made clothing the draft object across six consecutive lessons (Module 1 Lessons 3-4 plus all of Module 2) because a Pair's second Module inherits the first's Scenario unchanged. §C gains a **Standing exception** paragraph recording that Intermediate Modules 1-2 is de-paired, that Module 1's Set 1 consequently ends in a hand-off with no receiver (logged in Academic Writing's Pending work), and that the convention's fate - mandatory, optional, or retired - is explicitly undecided while Proficient Modules 1-2 stands as a complete Pair. Swap test: the Module Pair is Academic Writing-only by §C's own terms, so the exception does not swap to Reading or Listening/Speaking; the underlying rule it suspends (one Scenario per Set, introduction through published product) is untouched and still program-wide. No self-check item added in Quality Standards or the Style Guide: this records a deferred decision, not a generation rule to enforce at output time. |
| 2026-09-10 | v1.17 (Quality Standards) | **Commit, verify against the source, then return unsupported.** Per user question about listening pedagogy ("what's the research on providing the listening transcript. so maybe first time they listen without it, then secodn time they listen with it"). The instinct is well supported and the program had no rule for it at all: nothing in `shared/` covered how many times a student meets the anchor input, or whether they ever get to see the input's own wording. Listening/Speaking's Day 1 played the clip exactly once and locked the transcript teacher-side, so a student who misheard a word boundary had no way to find out why; its Phase 5 produce-then-reveal shape ran on an invented script, not on the real source. The research is consistent that a transcript works as feedback on a hypothesis already committed to, not as a co-present channel (Diao, Chandler & Sweller 2007 found simultaneous transcript plus audio underperformed audio alone; Vandergrift & Goh's 2012 metacognitive sequence puts the transcript last; Montero Perez et al. 2013 on captions carries the same dependency caveat), and that the final exposure should be unsupported so the form-to-sound mapping consolidates in the ear. New **§D12** states the three-beat arc (Commit, Verify, Return unsupported), forbids folding Verify into Commit, names each lesson type's own form of it, and exempts assessments, where a verification beat would contaminate the measure. New §F item 26 matches. Swap test passed on the arc, not on the mechanics: the transcript window is Listening-only, but "commit, then check against the source, then meet it again unsupported" swaps to Passage Reading (locate the evidence line after committing a `[STOP & CHECK]` answer) and to Academic Writing (compare a draft against the Mentor form, then produce one fresh instance without it). Only Listening/Speaking's mechanics were built this pass, at the user's direction; Reading's and Writing's forms of the beat are logged by name in their own `Index.md` Pending work so §D12 does not quietly become a one-modality rule. Prompts swept: no prompt in any modality restated this rule, because none existed, so no prompt was bumped for §D12 itself; Listening/Speaking's Lesson and Student Packet prompts were bumped for the mechanics (see that lesson type's `Changelog.md`). Its Assessment prompts were deliberately left alone under the exemption. Generated content: needs-new-research branch, since choosing a window means reading each transcript and picking a stretch, so zero lessons were swept and all 23 are named by path in Listening/Speaking's `Index.md`. |
| 2026-09-10 | v2.28 (Style Guide) | **The verification window is a printed packet block, not a teacher's board.** The companion to Quality Standards v1.17 §D12: if a student is to check what they wrote against what the speaker actually said, the source's words have to be on the page they wrote on, and §D8 already forbids the beat depending on something the teacher prepares and projects. §H.1 gains `.verify-window`/`.verify-src`/`.verify-instr` and their CSS, built from the existing tokens only (`--ink`, `--ink-soft`, `--paper`) and modeled on `.citebox`, with a heavy left rule and indented quotation so the block reads as the source speaking rather than as another task. New §I item 23 enforces the placement that makes the beat work at all: after the response spaces of every task the window could answer, never before, with no task or answer inside it, and closing on the instruction to cover it for one more unsupported encounter. Fair use is handled on the Listening/Speaking side, where the Lesson prompt bounds a window to at most two per lesson at six lines or eighty words each, each cited with its timestamp range. |
| 2026-09-10 | v2.27 (Style Guide) | **No item is answered by reading a chromatic color off a printed picture.** Found while generating Reading Beginner Module 1 Set 1: the Beginner CSV rows for Module 1 all end in matching a decoded word to an image, and their own examples use color ("It is big and red." next to a small blue umbrella and a big red umbrella), but every packet prints in grayscale, so a color-keyed picture item cannot be answered from the printed page. §A.2 previously governed only the packet's layout; it now binds task content too, and names the features that survive grayscale (size, shape, quantity, texture, setting). New §I item 22. The rule is deliberately narrow: black, white and gray survive grayscale and stay legitimate answers about a printed picture, and a task where the student describes their own belonging may use any color, because nothing printed is being read. Color words remain fine in an anchor text, a vocabulary list, or a spoken task. No prompt was bumped: the six packet prompts describe their output as "black-and-white" but none restates the rule in its own words, so all inherit it. Swept in the same pass, three items in two modalities: Passage Reading Intermediate Lesson 4 (WynwoodWalls), whose Level 2 task offered a word bank of *orange / blue / bright / big* against a grayscale mural photo and whose `.md` gave "It is orange." as the example answer; Listening/Speaking Intermediate Set 2 Lesson 5 (IberianLynx), whose Level 2 Better-of-two pair was "It is brown." / "It is blue." about a printed photograph; and Passage Reading's Beginner Set 1 plan and rotation log, whose Lesson 3 row keyed its vocabulary theme on "fruit, color, and taste." Academic Writing's notebook, phone-case and clothing lessons were checked and left alone: every color there is the student describing their own object. The Beginner plan's Lesson 4 (a black-and-white cat) was also left alone, since those two survive print. |
| 2026-09-10 | v1.16 (Quality Standards) | **Generalizing a fix is the default, not a question.** Per user direction ("I don't want to have to constantly say hey I made this update, let's make sure it doesn't happen again in any modality... decide if it should apply for everyone and then update the appropriate prompt without me having to ask"). Both existing statements of the swap test - `CLAUDE.md`'s "Fix modality-neutral bugs in `shared/` first" bullet and this file's "Test for what belongs here" paragraph - were filing rules saying where a fix goes once someone had decided it was general; neither said to run the test unprompted, to sweep all three modalities' prompts rather than the one in hand, or what to do with already-generated lessons. The repo held no procedure of any kind: `shared/` has rules and two generation-time artifact self-checks, all three `Index.md` "Generation workflow" sections cover generating new content only, and there is no review prompt or runbook anywhere. Two failures followed directly from that: §C9 (2026-09-09) was applied to LostKitchen with four other lessons "logged as pending and left," and the user had to report the same defect again on KeyDeer the next day; §D11 (2026-09-09) backlogged twelve lessons the same way. `CLAUDE.md` gains **"The generalization pass"**, a six-step procedure: run the swap test before any edit; land the rule in the right `shared/` file, bumping both its header and footer stamps, adding the matching self-check item, and logging one dated row here; sweep every prompt in all three modalities, bumping only those that restate the rule in their own words; then sweep the generated content, fixing **every** affected file in the same pass when the fix is mechanical and backlogging only what needs new research, with every affected file named explicitly; clear a backlog entry whenever its lesson is touched for any reason; and report what happened instead of asking. This file's "Test for what belongs here" paragraph gains one sentence pointing at that procedure (the steps live in `CLAUDE.md` only - this file is pasted into generation runs and does not carry maintenance process). Also fixed in the same pass, being exactly what the new step 2 exists to prevent: the Style Guide's footer still read `v2.25` against a `v2.26` header, and `Program_Conventions.md` had no `**Current version:**` footer at all; all three shared files now carry matching header and footer stamps. |
| 2026-09-10 | v1.15 (Quality Standards) | **A two-source item's excerpts live in the lesson `.md`, and the remaining four sparse ones are rebuilt.** Per user review of Listening/Speaking Advanced Lesson 3 (KeyDeer): "lessons ... it doesn't have a lot of meat there." Correct - §C9's enough-to-compare floor (v1.13, 2026-09-09) was applied only to LostKitchen when it landed; the other four two-source items were logged as pending and left. KeyDeer's Task D printed zero verbatim PBS excerpts (one 34-word summary, no quotation marks) against one quoted phrase for the refuge, and the two blocks shared no subject at all, so the page could not support its own question; its `.md` told the student to "note two or three specific descriptive choices the PBS segment makes," offloading the evidence the rule says the page must carry. **Root cause:** the floor was stated in the packet prompt but nowhere in the lesson prompt, and a packet is a regeneration of its `.md` (Style Guide §G), so an `.md` written without excerpts guarantees a sparse packet however good the packet prompt is. §C9 now says the excerpts are written into the `.md` itself, paired by subject; §F item 23 matches; Listening/Speaking's Lesson prompt v2.4 -> v2.5 gains §0.3 item 7 stating it in its own terms (the file had also been carrying the v2.4 room-neutral content under a stale v2.3 filename and header, which this rename corrects). **All four remaining lessons rebuilt**, each source re-fetched from its own page for exact wording and every excerpt verified verbatim against it: Advanced LivingTextbooks (`1.1.2.2`, 3 subject rows, 6 excerpts per source), KeyDeer (`1.1.3.2`, 4 rows, 7 PBS / 6 refuge), GlassBender (`1.1.4.3`, 4 rows, 7 per source), and Proficient LowerNinthWard (`1.1.1.1`, 3 rows, 5 and 6). LowerNinthWard also failed §C9's *first* half and was fixed for it: its labels ("A mixed, on-the-ground account" / "A promotional mission statement") characterized each source's tone and stated the contrast question 1 asks for, and its `.md` printed the answer inline instead of on an `Answer note:` line; only the foundation's own claims are used, never the article's independent commentary on how the houses later fared. Three packets also gained the `.match-src` rule they had never carried. Byproduct: `LivingTextbooks_Transcript.md`, `GlassBender_Transcript.md` and `LowerNinthWard_Transcript.md` saved per Lesson prompt §0.3 (the wider transcript backfill stays going-forward-only). Two PBS transcripts are machine-generated and carry real errors (LivingTextbooks' "there's hon demolition until mid-April"); the defect is recorded in each transcript file's header and no garbled passage was quoted or silently repaired. The rule is modality-neutral: Passage Reading's Proficient two-text items fall under it identically. |
| 2026-09-09 | v2.26 (Style Guide) | **Masthead meta top-aligns again, and the title's leading tightens.** Per user review ("the modality, module, band with lesson numbers need to be updated again - it should be top aligned... it looks bad when the unit title wraps"): v2.23's `align-items: flex-end` pinned the two-line meta stack to the bottom of the masthead, which reads level with the title only while the title is one line - on a wrapping title (Reading Intermediate Lesson 3's "How to Choose Running Shoes That Feel Comfortable") the title block grows downward and the label is left floating beside its last line, identifying nothing at the top of the page. §B supersedes v2.23's alignment: `.masthead` is `align-items: flex-start`, `.masthead-meta` takes `padding-top: 4px`, and `.masthead h1` takes `line-height: 1.2`. The nudge is optical, not flush: a 26px Georgia title at 1.2 starts its capitals ~8px down its line box (half-leading plus the gap between ascender and cap height) against ~4px for the 12px/1.25 uppercase tag, so 4px puts the MODALITY `·` MODULE line's capitals level with the title's. The two values are coupled - the h1's own leading sets the offset, so the 1.2 and the 4px are one change, not two; the title would need ~8px at the inherited `line-height: 1.55`. Tightening the h1 is the same fix as v2.24's: a 26px display title wrapping at body leading spread ~40px a line and pushed the lesson's first content down, which is most of what made the wrapped case look wrong. Synced into all 35 packets under `lessons/` (the 4 compact-CSS Reading files' collapsed one-liners edited in place, each file keeping its own indentation style; the 3 assessment packets carry no meta stack per §H.4, so only their h1 leading changes). CSS only: no §B rule text, §I item 5, markup, prompt, or `.md` lesson doc change, since no prompt states the masthead's alignment. One §B-compliance fix caught by the sweep's declaration-for-declaration check: the `Set_1_T` TOEFL homework was still missing the `.masthead + .masthead, .masthead.masthead-later` break rule (v2.24 reported it brought fully to §B, but it remained a subset), now present - no visible change, since that file has a single masthead. Still outstanding: Sagrada Família's packet carries stale `.masthead .kicker` / `.masthead .sub` rules §B has no such classes for, logged in Passage Reading's `Index.md` Pending work. |
| 2026-09-09 | v1.14 (Quality Standards) / v2.25 (Style Guide) | **Participation is room-neutral.** Per user review of Listening/Speaking Advanced Lesson 4 (GlassBender)'s Discuss It instruction ("in discuss it it tells you to form circles... isn't it the same as just find a new partner... I don't want to have to rearrange the room"): the lesson ran Concentric Circles, and the Style Guide's translation row handled the problem by flattening every such protocol into simultaneous small groups "because a static page cannot run a live rotation" - which silently dropped the rotation D7 requires rather than keeping it. Nothing in either file said an activity has to be runnable from students' seats, so the room-rearranging protocols stayed in every modality's rotation bank, and the same lesson's K-W-L Walk posted three sheets on the walls (a §D8 violation the hook bank kept regenerating). New Quality Standards §D11: an activity may move students to a partner or a small group, never move furniture, form a class-wide formation (facing circles, corners, stations), or post anything around the room; a replacement table gives each old protocol its room-neutral form (Concentric Circles -> Rotating Partners, Fishbowl -> Panel Round, Four-Corner Debate -> Take a Side, K-W-L Walk -> K-W-L Chart, gallery walk/carousel/Stand Up-Move -> the prompts move, the students don't), with D7 still governing all of them - Rotating Partners satisfies the rotation requirement directly, since the prompt changes when the partner does. §F item 25. The Style Guide's protocol translation row now renders the room-neutral form (Rotating Partners prints as find-a-new-partner-as-you-go) instead of flattening it, covers Four Corners, gallery walk, and Stand Up-Move too, and §I item 21 checks the packet. Prompt bumps for the four that restated a bank or the inner/outer-circle mechanics in their own words: Listening/Speaking Lesson v2.3 -> v2.4, Passage Reading Lesson v3.4 -> v3.5 (§0.10 rewritten around seats, keeping the every-listener-has-a-task scaffolding verbatim), Module Lesson-Plan v2.1 -> v2.2, print v2.5 -> v2.6, Academic Writing Lesson v6.15 -> v6.16 (the Beginner finale's gallery walk). Retroactive on one lesson: GlassBender (`1.1.4.1` -> `1.1.4.2`), both its `.md` phases and the two packet instruction lines; its packet already printed the K-W-L table the Markdown had been posting on walls. Twelve other already-generated lessons carry the old pattern (seven Listening/Speaking, four Passage Reading, one Academic Writing), plus two Reading lessons whose hook is misnamed but already seat-based; all are logged in each modality's `Index.md` Pending work. Listening/Speaking's `Rotation_Log.md` gains the old-to-new name mapping so adjacency checks against pre-2026-09-09 rows still resolve; already-generated rows keep the name their lesson was built under, planned-only rows were renamed in place. |
| 2026-09-09 | v2.24 (Style Guide) | **Masthead meta drops to two lines and repeats on the Unit _B masthead.** Per user review after v2.23's bottom-alignment ("the three just take too much vertical space... also should each unit header get this?"): v2.23 moved the stack but did not shorten it - three 12px tags inheriting `body`'s `line-height: 1.55` plus a 4px gap ran ~64px against a 26px `h1`, so the meta block, not the title, set the masthead's height and pushed the lesson's first content down every packet. §B now pairs the modality label and the Module name on one line separated by a new `.masthead-sep` dot (unbolded, `var(--rule)`, so the two facts stay distinct), keeps Band plus version code on the second, and gives `.masthead-tag` an explicit `line-height: 1.25` with `gap: 2px` - a label stack is not running serif prose. Same three facts, same naming rules, ~32px instead of ~64px. The "opening masthead only" restriction is also gone: a `.masthead-later` section always starts its own printed sheet (`page-break-before: always`), so a Unit _B page carried nothing identifying it; it now takes the identical stack, and still nets a saving against the old single three-tag stack. §I item 5 matches. Retroactive across all 35 packets: the 31 lesson packets collapsed to two tags and given the second stack; the `Set_1_T` TOEFL homework, which was stale on two counts (pre-v2.23 `align-items: flex-start` / `margin-bottom: 6px` / `margin-top: 4px`, and a two-tag stack with no Module name), brought fully to §B as `Listening & Speaking · Describing` / `Advanced 1.1T.1.4`; the three assessment packets, which were subsets of §B's masthead block (the two Listening/Speaking ones missing `.masthead`'s flex declarations, Reading's missing the `.masthead-later` break rule), given the full block - they still carry no meta stack (§H.4 unchanged), so no visible change. All 35 files now hold byte-identical masthead CSS modulo each file's own indentation style. Prompt bumps for the four that stated the rule in their own words: Passage Reading print v2.4 -> v2.5, Listening/Speaking print v2.5 -> v2.6, Academic Writing print v3.16 -> v3.17 (each "the masthead"/"the opening masthead" -> "each masthead section", in §2.1 and the Section 5 self-check), and the TOEFL Track Tier prompt v1 -> v1.1, whose §2.5 still specified a "two-tag masthead" - already stale against v2.20's three tags - now a plain pointer to §B so it cannot drift again. An orphaned Listening/Speaking `Generate_Student_Packet_Prompt_v2.4.md`, left behind by the v2.5 bump and referenced by nothing, was removed per the one-current-version-per-prompt-family convention. No `.md` lesson doc touched (a §G formatting-only edit); the three Assessment Student Packet prompts already say "No `.masthead-meta` stack" and were not bumped. |
| 2026-09-09 | v2.23 (Style Guide) | **Masthead title bottom-aligns to the tag stack.** Per user review after the v2.20 three-tag masthead ("the three lines of modality, module and band take too much vertical space"): `.masthead` aligned its children to the top, so the `h1` sat level with the first tag and the stack hung below it. §B: `.masthead` is now `align-items: flex-end`, `.masthead h1` has no bottom margin, and `.masthead-meta` no top margin, so the Unit title's baseline sits level with the Band/version line and the modality and Module tags stack upward within the title's own height. Header stamp corrected from a stale v2.21 (the v2.22 entry bumped only the footer). CSS only; no rule text, markup, or prompt change. Synced into every packet's `<style>` (34 files carrying `.masthead`; the three assessment packets have no meta stack, so only their `align-items` and `h1` margin changed, with no visible effect). |
| 2026-09-09 | v2.22 (Style Guide) | **The "Don't read ahead" instruction gets a STOP badge.** Per user review of Listening/Speaking Advanced Lesson 1 (LostKitchen)'s Show What You Heard close ("can we add a visual clue like an icon to make it really obvious?"): the instruction was bold text inside an unboxed `.wrapup`, indistinguishable at a glance from every other instruction line, and the only concealment was the script's 180-degree rotation further down the page. Style Guide §H.1 adds `.stop-flag` (a 2px-bordered, page-break-avoiding callout) and `.stop-badge` (a black octagon drawn with `clip-path`, white STOP text, `print-color-adjust: exact` so the fill survives printing); a drawn shape rather than an emoji keeps §A.2's black-and-white rule and prints the same in every browser. §I item 20 checks it. Listening/Speaking packet prompt v2.5 §2.8 specifies the wrapper; all 11 packets (incl. `Set_1_T`) swept in place as a §G formatting-only edit, `.md` files untouched. |
| 2026-09-09 | v1.13 (Quality Standards) / v2.21 (Style Guide) | **A two-source item prints enough to compare.** Per user review of Listening/Speaking Advanced Lesson 1 (LostKitchen) Task D ("the two examples just seem too sparse, not enough meat"): the 2026-09-09 stem sweep (v1.9 / v2.18) rewrote every Level 7 compare row as neutral source content, which was right, but nothing said how much source content is enough, and all four Advanced Set 1 packets ended up with a one-sentence topic summary for one source and two-word fragments for the other. A student cannot rewind the clip or open the second source mid-task, so the page has to carry the comparison's evidence. Quality Standards §C9 now sets the floor (at least three short verbatim excerpts per source, on the same two or more subjects in both, within the lesson's fair-use ceiling; a summary or fragment list is not enough) and names the two allowed layouts, chosen per lesson (one block per source, or one row per shared subject when the sources line up); §F item 23 checks it. Style Guide §C states the same, §H.1 adds `.match-src` (a source's excerpt line under a subject-label `.match-row`) to the base stylesheet, and §I item 19 checks it. Applied to LostKitchen (base and `Set_1_T` fork) the same day; Listening/Speaking's packet prompt v2.3 -> v2.4. The rule is modality-neutral: Passage Reading's Proficient Level 7 two-text items and any future two-source item fall under it. |
| 2026-09-09 | v2.20 (Style Guide) / v1.12 (Quality Standards) | **Masthead carries the Module name.** Per user question ("should we add module information in the header too - just the module name like Describing, Evaluating, since module number could change"): the packet masthead named the Module only as the first digit of the version code, so nothing on the page said "Describing." Style Guide §B: the `.masthead-meta` stack is now three tags - modality label, the Module name plain and alone (`Describing`, `Narrating`), spelled as Conventions §A names it, never the number (the version code already carries it, and the name stays readable if Modules are ever reordered), then Band plus version code; §I item 5 matches. Quality Standards §E4 fixes the `.md` metadata line's Module field to one shape, `**Module:** <N>, <Name>`, so the doc and packet agree on the name. No prompt bumps: all three packet prompts cite §B / §I item 5 by name. Retroactive, unambiguous (every lesson is Module 1 Describing or Writing Module 2 Narrating): the Module tag inserted in all 31 lesson packets; Reading's Kitchen and Yoyogi Park packets, which had no `.masthead-meta` at all, gained the stack and its §B CSS (`Reading` / `Describing` / `Intermediate` - Band only, since Reading's Lessons 1-2 stay unversioned by design, as do Sagrada Família and Forge at Dawn); 22 `.md` files normalized to `<N>, <Name>` (Reading Advanced used `1 (Describing)`, Writing used `1: Describing`, five Listening/Speaking lessons had no Module field). Assessment packets unchanged. |
| 2026-09-09 | v1.11 (Quality Standards) / v2.19 (Style Guide) | **"Circle" only when options are printed.** Per user question on Lost Kitchen Advanced L1 Task A item 2 ("Circle one and say why" over a plain answer line, nothing on the page to circle; same shape in Living Textbooks L2 and Lower Ninth Ward Proficient L1, all reworded "Say which, and why" the same day). §C9's "circle one" example now carries the condition; new §C10 states that an instruction verb names an action the page allows, and self-check item 24 enforces it; the Style Guide's Multiple choice paragraph and packet self-check item 8 say the same for the packet. No prompt bumps: prompts cite these sections and self-checks by name. |
| 2026-09-09 | v1.14 (Program Conventions) / v1.10 (Quality Standards) | **Every prompt routes image credits to the Set register.** Per user question ("are all the prompts updated so this doesn't happen across any modality"): an audit found no prompt placing a credit in a caption, but the chain to the new `Set<N>_<Band>_Image_Credits.md` ran only through Quality Standards §D8 / self-check item 19 -> Conventions §I, neither of which named the register, and §H/§I covered lessons only. Now: §D8 and item 19 name the register and say the packet prints no credit; §H adds `Set<N>_<Band>_Assessment_Img_<Purpose>.<ext>` for assessment images, and §I/§D cover assessments (rows under a final `## Assessment` heading). Each Lesson and Assessment prompt gains one Output clause naming the register: Listening/Speaking Lesson v2.3, Assessment v2.1; Passage Reading Lesson v3.4, Assessment v5.2, Student Packet v2.4 (§2.11 caption is descriptive only); Academic Writing Lesson v6.15, Assessment v4.2. (Reading's v3.3/v2.3 and Writing's v6.14 were taken the same day by the stem-answer fix.) |
| 2026-09-09 | v1.9 (Quality Standards) / v2.18 (Style Guide) | **A stem never contains its own answer.** Per user report after teaching Listening/Speaking Advanced Lesson 3 (KeyDeer): "lot of the answers were in the parenthesis of the question." Diagnosis: the lesson `.md` may hold answer keys (§E2) but nothing said where, so the generator inlined the teacher's exemplar answers inside the task sentences ("Summarize how the segment is organized (introducing the deer, then its history...)", "find one evaluative word (e.g. 'success story')", "locate one tonal shift (from a measured tone to a regretful one)"), and the packet regeneration copied the sentences through; the Level 7 two-source compare layout was also rendered as a summary of the contrast the question then asked for. §C2's "not trivially easy" list (scan, elimination, general knowledge, absurd distractors) never named this case. New Quality Standards §C9: no stem, parenthetical, or pre-printed source material states what the item asks for; compare layouts print each source's own content, never its tone or the contrast; stem parentheticals limited to format, a no-correct-option menu, or an untested gloss; frame models exempt. §E2 now gives the exemplar a labeled home, an `**Answer note:**` line under its item. §F item 23. Style Guide: §E translation row (Answer notes never printed), §F multiple-choice paragraph names it as the one content-carrying stem parenthetical, §I item 19. Every item-producing prompt states it in its own terms rather than relying on the paste-along file alone: Listening/Speaking Lesson v2.2 and packet v2.3 (Phase 4 Answer notes; merge carries stems only; Level 7 compare layout holds source content), Passage Reading Lesson v3.3 (STOP & CHECK brackets, Fact Finder / Cause & Effect sets, matrix cells), packet v2.3 (stems only, stops as questions, worked model on untested sentences), Homework v2.3, Academic Writing Lesson v6.14 (activity keys on Answer notes; the Mentor Ladder is a form model, unaffected), packet v3.16 (Answer notes and editing-paragraph error keys stripped), Homework v4.4. No edit to the three Assessment prompts, two Assessment Packet prompts, or two TOEFL prompts (each already keeps its answer key in a separate teacher section the packet prompt strips by name) or the three Module Lesson-Plan prompts (they write no items). Reading's and Writing's existing lessons show no instance; their `(e.g. "It is soft.")` lines are frame models. Retroactive: all four Advanced Set 1 lessons (LostKitchen, LivingTextbooks, KeyDeer, GlassBender) and the `Set_1_T` LostKitchen fork swept, `.md` and packet; Proficient Lesson 1 (LowerNinthWard) carries two of the same leaks and is logged as pending in Listening/Speaking's `Index.md`. |
| 2026-09-09 | v1.13 (Program Conventions) / v2.17 (Style Guide) | **Photo credits move to one document per Set.** Per user request ("can we have photo credit in a separate document... one document per set documenting all the images in each lesson"): the packet caption no longer carries an author/license credit - it is descriptive only, and a `.pic-options` grid has no caption at all (Style Guide §F, self-check item 18). Credits live in a new Set-level `Set<N>_<Band>_Image_Credits.md` beside the assessment (Conventions §D tree; §I now says to append a row there in the same pass that cites the image in the lesson `.md`, which stays the source of truth). Retroactively created for the three Sets with images: Listening/Speaking Intermediate Set 1 (19 images), Passage Reading Intermediate Set 1 (4) and Advanced Set 1 (1); the ten packets' credit clauses were stripped. |
| 2026-09-09 | v2.16 (Style Guide) | **A picture box holds a picture.** §H.1 `.pic-option .pic-box` still described the pre-§D8 empty box (dashed border, centered placeholder text, padding) and had no rule for the `<img>` inside it, so the one packet that embedded choice images (Backpack) carried its own local override. Now: `1px solid` border, no padding, `overflow: hidden`, plus `.pic-option .pic-box img { width/height 100%; object-fit: cover }`. Synced into every packet embedding the block (10 files). Surfaced while embedding NewBakery's and PortoFoodTour's choice pictures. |
| 2026-09-09 | v1.12 (Program Conventions) / v1.8 (Quality Standards) / v2.15 (Style Guide) | **Images are fetched, not found.** Per user request after a day of hand-supplying photos ("could we also just pull the image too instead of me having to find all of them"). New Program Conventions §I: a lesson that needs a picture fetches it from Wikimedia Commons in the same pass that writes the `.md`, via the Commons search API (verified from a Claude Code session: results carry `LicenseShortName`, `Artist`, and an 800px `thumburl`), accepting only public-domain/CC0/CC BY/CC BY-SA files, saving under the §H name, and citing file title, author, license, and URL in the `.md` at the point of use; a fetch-less chat tool records the query and logs "image to fetch" in the Index instead of ever printing a placeholder. Commons is the single allowed source precisely because every result is license-tagged. Quality Standards §D8 and self-check item 19 now require the image be sourced and cited, not just named. Style Guide §F: the packet caption appends the credit the license requires ("Photo: <author>, <license>, Wikimedia Commons"); self-check item 18 checks it. No prompt edits: every lesson prompt is already run pasted with both shared files. Retroactive: the six images embedded so far came from the user with no recorded provenance - logged as pending in Reading's and Listening/Speaking's `Index.md`. |
| 2026-09-09 | v2.14 (Style Guide) / v1.7 (Quality Standards) / v1.11 (Program Conventions) | **Images are a first-class packet element.** Per user question after today's Kitchen/YoyogiPark/WynwoodWalls photo work ("are all the prompts updated so this doesn't happen again?"): the answer was no. §D8 already required a real data-URI image and forbade placeholders, and §B now defined `.photo`, but nothing said how a photo is marked up, that the stylesheet is pasted in full, or that a hand edit must check its classes exist - which is exactly how two packets embedded photos today with no `.photo` rule present. Style Guide: §A.1 states inline data URIs are the one allowed image form; §B intro states every rule is pasted in full, used or not; §F gains an "Embedded photos" paragraph (`<img class="photo">` + `.image-caption`, §H-named source file named in the `.md`, downscaled copy ~800px/~100 KB, `.image-placeholder` only for student drawing); §G requires a hand edit to confirm its classes are defined and to sync an older packet's `<style>` to current §B; new self-check item 18; stale footer (said v2.9) corrected. Quality Standards §D8 and self-check item 19: the lesson `.md` names the asset file at the point of use. Program Conventions §H: the embed step points at §F. Passage Reading's packet prompt bumped (v2.1 → v2.2) with a new 2.11 "Photos" section, since it never mentioned images; Listening/Speaking's and Writing's already point at §F and inherit. |
| 2026-09-09 | v2.13 (Style Guide) | **The base stylesheet owns every selector a packet uses.** Per user review of Reading's Kitchen packet ("are the Discuss It questions a different font size?"): they were - `.discuss-block .prompt` printed at 16px against 14.5px body text, a local rule every one of the 8 Reading packets carried because §B defined `.discuss-block`'s margin but never the prompt itself, so nothing stopped each packet inventing a size. Now defined in §B at body size (14.5px, margin only). Same audit found `.photo` - the embedded-image class - defined three different ways across the 4 packets that embed a picture; now defined once in §B (block, `max-width: 100%`, `max-height: 300px`, centered). §D gains an explicit type-scale rule (one 14.5px body size; only the named display/label classes differ; no packet-local selector or emphasis size), and self-check item 13 checks for it. Retroactive: all 8 Reading packets' `.discuss-block .prompt` and all 4 `.photo` rules (Kitchen, RunningShoes, SagradaFamilia, Backpack) replaced with the §B rules. No packet-prompt bump: neither class was ever mentioned in a prompt; both inherit via §B. |
| 2026-09-09 | v2.12 (Style Guide) | **Phrase Spotlight entries carry no label.** Per user review of Reading's Kitchen packet, where "in no time" printed with the `Slang` tag: the v1.11 rule (below) labeled every *transparent* chunk "Slang," but transparency is not slang - "in no time" is an ordinary idiom - so the label was wrong more often than right. Retired the `.idiom-tag` class from §B; every Phrase Spotlight entry now renders as `<span class="idiom-phrase">phrase</span>: gloss.` with nothing appended. The transparent/opaque classification each lesson's Markdown records stays there for the teacher and is never surfaced to students. Passage Reading's packet prompt bumped to match (v2.0 → v2.1, §2.7 and self-check item 7); Listening/Speaking's bumped for its one direct mention (v2.1 → v2.2, §2.13). Retroactive: the tag was stripped from all 11 packets carrying it (7 Reading, 3 Listening/Speaking, one of them the `Set_1_T` fork), and the now-dead `.idiom-tag` CSS rule was removed from those plus 6 more packets that embedded it without using it (3 Listening/Speaking, 2 Writing, 1 Reading). |
| 2026-09-08 | v1.10 (Program Conventions) | **A Module Pair's piece is built in parts.** Per user review of the regenerated Writing Advanced Set: the Pair model still had the first Module draft a complete essay for the second to revise and expand, so a Level 6-7 student wrote a whole five-paragraph essay inside one Module. §C now says each Module drafts only the part its form fits (essay Levels: bodies under a working thesis in Module N, introduction and conclusion in Module N+1; paragraph Levels: one paragraph per Module), and position 8 assembles, peer-edits, and publishes. Writing's Lesson prompt v6.13, packet prompt v3.15, Module Lesson-Plan prompt v4.5, and Homework prompt v4.3 carry the mechanics. |
| 2026-09-08 | v1.6 (Quality Standards) | **Fixed-form repetition is never a design choice.** Per user direction after the v1.5 retrofit ("I don't know when a repetition like this would be a good thing"): §D10 rewritten as a rule list with no allowance for repeats: the form produced at most once per Level per day, "N times" only in the single CSV-sanctioned guided round, no shape twice in a lesson or in the same slot in consecutive lessons, the form shown once per day, homework one production plus other shapes, assessments once per Level. §D6 points to §D10 for lower-Level task shape; §F item 22 matches. Each lesson type's Lesson, Module Lesson-Plan, and Homework prompts now state the bank in their own terms (Passage Reading Lesson v3.2 0.11, Listening/Speaking Lesson v2.1 0.6, Academic Writing Lesson v6.10 0.4d) rather than relying on the paste-along file alone. |
| 2026-09-08 | v1.5 (Quality Standards) / v2.11 (Style Guide) | **Fixed-output Levels rotate the activity, not the output.** Per user review of Writing Intermediate Set 1 ("a lot of It is ___ and ___"): a one-star student completed the Level 2 frame about nineteen times across the Set because the Writing cycle gave Levels 1-3 "another guided frame round" wherever Levels 4-5 got a new activity, and nothing required the shape to vary. New Quality Standards §D10: where a CSV row fixes a Level's output form, the form stays but the activity shape rotates within a Set (choose-what-fits, fix-the-wrong-item, produce-from-sound, better-of-two, sort-then-produce, a partner's object); at most one "produce the form N times from the list" task per Level per lesson; the form shown once per day as a model. §C5 clause: more items never means the same item again; §F item 22. Style Guide §F: a fixed frame printed once per masthead section, later Tasks show only the blank line and never re-quote it; §I item 17. |
| 2026-09-08 | v2.10 (Style Guide) | **Objective is a student can-do: "I can ..."** Per user review of Writing Intermediate Set 1, whose objectives opened "You will describe..." while Reading's and Listening/Speaking's opened with a bare verb ("Objective: describe..."); neither reads like the `learningobjectives.csv` rows the objective is drawn from. §B, the §E Skill Spotlight row, and §I item 3 now say the sentence is "I can" plus the skill, in the student's voice ("I can describe something of my own by comparing it..."), never "You will," "To describe," or a bare verb. The bare-verb form is retired; every existing Passage Reading and Listening/Speaking packet still carries it and is logged in those lesson types' `Index.md` Pending work for their next regeneration. |
| 2026-09-08 | v1.4 (Quality Standards) / v2.9 (Style Guide) | **Objective names the skill, for everyone.** Per user review of Writing Intermediate Set 1, whose every objective read "describe your phone case ... Some of you will also ...". §D1 and the Style Guide's Skill Spotlight row now say the statement names the transferable skill, not the lesson's object, text, or clip, and is one sentence identical for every task Level with no tier narration; a Level's own feature belongs inside its task. §F item 13 and §I item 3 updated. Writing prompts v6.8 / v3.11 carry it. |
| 2026-09-08 | v2.8 (Style Guide) | **"Check and Improve Your Writing"; instructions as steps; no wrap-up lines.** Per user review of Writing Intermediate Lesson 3: "fix" implies the writing was wrong, so the merged section is "Check and Improve Your Writing" and its wording is improve / make it better / make each change you marked, never fix or correct; every multi-step Task is a one-sentence lead plus a numbered list of single actions or questions, check items phrased as questions, never a prose paragraph (new §F paragraph); "Keep your piece. You'll keep working on it soon" wrap-up lines are not printed outside the Pair position 4 hand-off (§E pacing row). §I items 6 and 16 updated. Writing packet prompt v3.8 carries it. |
| 2026-09-08 | v2.7 (Style Guide) | **Check-then-fix is one Task.** Per user review of Writing Intermediate Lesson 3's Day 2 unit, which printed "Check Your Own Work" (one Task per star) followed by "Make It Better" (one Task per star again). New §F paragraph: a self-check and the revision that acts on it are one section, "Check and Fix Your Work," with one lettered Task per star holding the check items then the fix; like-star Tasks whose second half acts on the first are merged. §I item 6 updated. Writing packet prompt v3.7 carries it. |
| 2026-09-08 | v1.3 (Quality Standards) / v2.6 (Style Guide) | **The object is a subject, not a prop.** Per user review of Writing Intermediate Lesson 3 ("Look at it" makes no sense; students do not work with a physical object). §D8 now says no task requires the belonging to be present: never "look at it," "point to the part," "hold it up," "put it on your desk"; a partner check asks the student to say which part a word describes. §B Foundation Support and §F item 19 reworded to match (matching to a printed picture or category, not pointing at real objects). Style Guide §E logistics row and §I item 16 extended. The same review moved a drafting lesson's Day 2 straight into checking and improving the piece; that is a Writing flow change carried by Lesson prompt v6.5 and packet prompt v3.6, see `writing/academic-writing/Changelog.md`. |
| 2026-09-08 | v2.5 (Style Guide) | **No logistics, word counts, or pacing lines in packets.** Per user review of Writing Intermediate Lesson 3's packet ("Put it on your desk ... borrow a classmate's," "about 40-90 words. It is fine to finish tomorrow"). Three §E rows: object logistics are the teacher's, not printed; students see sentence counts only, never a word range or "count your words"; pacing notes are not printed. §I item 16. Writing's packet prompt v3.5 carries it. |
| 2026-09-08 | v2.4 (Style Guide) | **Self-check lists are lettered Tasks; no speed-gated add-ons.** Per user review of Writing Intermediate Lesson 3's packet, whose "Check Your Own Work" put three and four stars on one line and starred individual items. §F's exception that unlettered content could combine two star tags is deleted: a self-check list is one lettered Task per star level, one star each, shared items repeated, shared instructions as a plain line under the section title. New §F paragraph: a packet never prints "Finished early?", "If you have time," or any add-on gated on speed; a heavier tier is its own starred Task or is left out. §I item 6 updated. Writing's packet prompt v3.4 carries it. |
| 2026-09-08 | v1.2 (Quality Standards) / v2.3 (Style Guide) | **Self-contained across lessons; concrete prompts at lower bands.** Per user review of Writing Intermediate Lesson 3, whose packet opened with "the jacket you planned last time ... look back at the examples from earlier this week and at your own planning notes" and two reflection questions ("What does the example at your star level do that your piece needs to do too?"). New Quality Standards §D9: a lesson's tasks require nothing a student made in an earlier lesson except the carried piece; plans, banks, models, and object choices are rebuilt or reprinted in the lesson's own packet, choices printed as a list to circle. New §E6: at Beginner and Intermediate every student-facing prompt is a concrete action with a visible product, never a process or reflection question; self-checks are yes/no items with marks; staging questions stay in the Markdown. §F items 20-21. Style Guide §E gains the matching translation row and §I item 15. Writing prompts v6.3 / v4.2 / v3.3 carry it; see `writing/academic-writing/Changelog.md`. |
| 2026-09-08 | v1.9 (Program Conventions) | **Practice object vs draft object.** §C's one-Scenario-per-Writing-Set rule now says the Scenario is built on two of the student's own belongings: a practice object (grammar input, frame rounds, Mentor Ladder, Lesson 1 through Lesson 2 Day 1) and a draft object (introduced at Lesson 2 Day 2 prewriting; the only object Lessons 3-4 and a Pair's second Module write about). Per user direction that four lessons on one phone case was too much of one object; the draft becomes the first transfer of the taught grammar to something the student did not drill on, and the ladder stays on the practice object since it models form. CLAUDE.md's Academic Writing bullet updated. Writing's Lesson (v6.2), Module Lesson-Plan (v4.1), and Student Packet (v3.2) prompts carry the rule; see `writing/academic-writing/Changelog.md`. |
| 2026-09-08 | v2.2 (Style Guide) | **One word-bank markup form.** `.wordbank` had a CSS rule (§B) but no markup rule, so each session invented its own inner markup: Passage Reading and Listening/Speaking packets used a bold `Word bank:` label with ` &middot; ` separators, while the four Academic Writing Intermediate Set 1 packets shipped three variants (`<p class="model-step">` per category line; `<br />`-joined lines under a "Your Word Bank" title; `<br />`-joined lines with no title and an instruction line above), all comma-separated. New §F "Word banks" paragraph fixes the form (bold label, ` &middot; ` separators, `<br />` between category lines, no wrapper classes, a `.section-title` reading exactly "Word Bank" only when the bank serves more than one task), a matching §B code comment on `.wordbank`, and §I item 10 checks it. The four Writing packets were normalized in the same pass (markup only, no version bump). |
| 2026-09-08 | v1.1 (Quality Standards) / v2.1 (Style Guide) | **Self-contained lessons: no teacher-prepared media.** New Quality Standards §D8 and self-check item 19, per user direction after the regenerated Writing Intermediate Lesson 1 told students to "look at the phone case picture your teacher shows you": a lesson, homework, or assessment never depends on the teacher preparing, bringing, printing, finding, or showing anything before class beyond its own two files (and, for Listening/Speaking, the cited real source). Picture or object prompts required by the CSV's Level 1-3 objectives are met in priority order by the student's own belonging, then an object in the room, then (only when neither can carry the task) a real image embedded in the packet at generation time; never a teacher-shown picture, never a "[TEACHER: insert photo]" note, never an empty picture box. §B's Foundation Support sentence rewritten to drop teacher-made picture cards. Style Guide: §F's "picture-based items get real labeled placeholder boxes" reversed to "embed the real images, never an empty box"; §I item 8, §H.1's class descriptions, and code comments on `.pic-box` and `.image-placeholder` (retained only for a student-drawn box) updated to match. A repo survey found the dependency in 15 existing lessons and one assessment packet across all three modalities (photo notes, empty `.pic-box` grids, "hold up real objects" hooks, teacher-made card sets); these are logged as Pending work in each modality's `Index.md` and `CLAUDE.md`, to be fixed when next touched, except the Writing Intermediate My Phone Case Set, retrofitted now - see Writing's `Changelog.md`. Prompts repointed in the same pass - see each modality's `Changelog.md`. |
| 2026-09-08 | v1.0 (Quality Standards, new) / v2.0 (Style Guide) / v1.8 (Program Conventions) | **Shared layer for quality rules and output checklists**, closing the gap that let a fix land in one modality while the same bug resurfaced in another (the `.ans-line-sm`/`.blank` conflation was fixed six times across four files; item-distinctness, distractor plausibility, answerable-from-the-centerpiece, and the packet-is-a-regeneration rule each existed in exactly one modality's prompt). New `Generation_Quality_Standards.md` holds every modality-neutral pedagogical rule stated once - task-Level rules (§A), Respectful Tiers and Foundation Support (§B), item quality: distinct, not trivially easy for the band, requires the centerpiece, padded near-miss banks, time-balanced across Levels, counted not estimated, complete inline, traceable (§C), lesson shape: Skill Spotlight, demonstration-only Closing Transfer Check and its per-Set variety, board-dependent moment, teaching-before-practice, adjacency-only rotation, scaffolded oral output (§D), Markdown style constraints (§E), and an 18-item shared self-check (§F) every Lesson/Homework/Assessment prompt runs before its own modality items. Sourced from each rule's best existing wording (L/S Lesson item 17, L/S Assessment A.1/A.4, Reading Assessment §1.2/§3, Writing Lesson items 1/17, Reading Lesson §0.7/§0.8/Relative Multi-Level Strategy). `Student_Packet_Style_Guide.md` v2.0 gains §E universal teacher-to-student translations (the rows all three packet prompts repeated), §F star ratings and lettered Tasks (one star per letter, letters never reused, ascending order, share-instruction-first, no star inside a teaching callout, no coaching note under a stem, every standalone question numbered, bank/frame/starter placed before its items, inline multiple choice), §G the Markdown-is-source-of-truth regeneration rule (from L/S packet Section 4), §H per-modality delta CSS (L/S's 143-line block moved out of its packet prompt verbatim; Writing's `.rule-table`/`.fillblank`/`.model-step` recorded from its packets, previously defined nowhere), and §I a 14-item shared packet self-check; trimmed the HTML skeleton to a head fragment and the masthead history aside. `Program_Conventions.md` §F absorbs the Rotation Log read-before/append-after mechanics and the cross-Set (adjacency), cross-Module/cross-Band (adjacency plus recency flag) rules the three Module Lesson-Plan prompts each restated with cross-references to one another. `CLAUDE.md` gains three process rules: fix modality-neutral bugs in `shared/` first and log them once here; no prose cross-references between modalities' prompts; no version stamps or failure narratives inside prompt bodies. Prompts are unchanged in this entry; each family is converted to point here in its own following pass - see each modality's `Changelog.md`. |
| 2026-09-08 | v1.12 (Style Guide) | Unscoped the inline fill-in-the-blank rule from `.qlist .blank` to plain `.blank` (§B), and added an explicit code-comment rule distinguishing it from `.ans-line`/`.ans-line-sm`: `.blank` is a short blank sitting *inside* a sentence under any list wrapper (`.qlist`, `.fillblank`, or none); `.ans-line`/`.ans-line-sm` is a standalone full-width line by itself *below* a prompt, never mid-sentence. The old scoping only worked under Reading's `.qlist` wrapper - Writing's packets use a different, equally legitimate `<ol class="fillblank">` wrapper with no matching inline-blank rule, so sessions generating a mid-sentence blank there had nothing correct to reach for and reused `.ans-line-sm` instead, which is full-width and swallows the rest of the line. The same conflation had independently drifted into Reading's own `Generate_Assessment_Student_Packet_Prompt` (§2.6), bumped to v1.2 to correct it - see Passage Reading's own `Changelog.md`. Also corrected this file's own stale internal version footer, which still read "v1.8" despite the title already being at v1.11. Retroactive: four Writing packets that had either the bug or an earlier one-off patch of it (`MyPhoneCase_Intermediate_L1`/`L2`, `TwoApartments_Advanced_L1`/`L2`) were normalized onto this single pattern in the same pass - see Writing's own `Changelog.md`. |
| 2026-09-08 | v1.11 (Style Guide) | Retired the `.idiom-list`/`.irow` numbered-table classes added 2026-09-07 (v1.4-v1.6 below): every idiom/slang section now renders as the plain `.idiom-item`/`.spotlight-box` "Phrase Spotlight" treatment regardless of how many idioms it glosses, undoing the "table when there's a list" split. New `.idiom-tag` class - a small uppercase "Slang" label appended after `.idiom-phrase` - distinguishes a transparent (functional) chunk from the unlabeled default (an opaque, figurative idiom), sourced from the transparent/opaque classification each lesson's own Markdown already records (Passage Reading `Generate_Lesson_Prompt` §0.4). Passage Reading's packet prompt bumped to match (v1.11 → v1.12, §2.12 rewritten); Listening/Speaking inherits via its existing pointer, no direct edit needed. Retroactive: every already-generated packet across Reading and Listening/Speaking with an idiom section (14 files total, 13 unique lessons) was hand-corrected in the same pass, including one file (`VinylComeback_Advanced_L3`) found reusing `.vocab-list`/`.vrow` for its idioms instead of either idiom class - see each modality's own `Changelog.md`/`Index.md`. |
| 2026-09-08 | v1.7 (Program Conventions) | New §C addendum: **Module Pair**, an Academic-Writing-only mechanic (Intermediate/Advanced/Proficient; Beginner exempt) pairing two consecutive Modules (1-2, 3-4, 5-6, 7-8) so one Scenario/essay spans both Modules' Sets (8 lessons, not 4), each Module contributing its own Grammar/Essay Focus to the shared piece. Prompted by the user wanting a slower cadence - one finished essay per two Modules instead of per Module - with the essay's stages (grammar input, drafting, revision, publishing) spread across both Modules rather than compressed into the first. Deliberately does not redefine what a Set is or touch §D's folder nesting or §G's version-code shape - each Module keeps its own ordinary Set, scoped and numbered exactly as before; the pairing is a Writing-only continuity layer above Set, not a change to the shared Set/Module mechanics Reading and Listening/Speaking also depend on. Full mechanics (the 8-lesson table, the wrapping Module-Pair Lesson-Plan doc, retrofitting Module 1's existing "Two Apartments"/"My Phone Case" content into Part 1 of Module Pair 1-2) live in `writing/academic-writing/Index.md` and `Changelog.md` - not restated here. |
| 2026-09-08 | v1.10 (Style Guide) | `.vocab-list .word` (§B) changed from `min-width: 130px` to a fixed `width: 165px` + `flex-shrink: 0` - the same `min-width`-is-a-floor flaw already fixed once in the sibling `.idiom-list .irow .idiom-phrase` rule (v1.5 below), never generalized to this class. Surfaced on Listening/Speaking's Great-Grandmother packet (Intermediate Set 1 Lesson 4): "great-grandmother" (17 characters, bold italic Georgia) grew past the 130px floor while shorter words didn't, leaving that row's definition starting at a different x-position. Notably, this specific lesson had already hit this exact overflow once before and been locally widened to 150px, only to be "corrected" back down to the too-narrow shared 130px in a later sync pass (Listening/Speaking `Changelog.md`, 2026-09-07) - reintroducing the bug, which is why the shared default itself (not just one file) needed fixing this time. Going-forward via the shared stylesheet, but also applied retroactively: every already-generated packet across Listening/Speaking and Reading embedding the old `min-width: 130px` (or a locally-widened variant) rule - 20 files total - was hand-corrected to the new fixed-width rule in the same pass (a few of Reading's pre-date this file's formatting conventions and kept their own compact `property:value;` CSS style, per the same precedent as the `.ans-line-sm` sweep below). Neither modality's own `Generate_Student_Packet_Prompt` needs a direct edit - like the `.idiom-list` precedent, both inherit this purely via the shared stylesheet, with no literal width claim of their own. See Listening/Speaking's own `Changelog.md` for the other 3 fixes bundled into the same review pass. |
| 2026-09-08 | v1.6 (Program Conventions) | New §H: naming convention for a lesson folder's raw media assets - `Lesson<N>_<Slug>_Audio.<ext>` (already established by `Lesson1_NewBakery_Audio.mp3`), `Lesson<N>_<Slug>_Img_<Purpose>.<ext>` for images (`.jpeg` normalized to `.jpg`), and `Lesson<N>_<Slug>_SourceVideo.<ext>` for a Mac (`.webloc`)/Windows (`.url`) link-shortcut pair to a lesson's real source video, kept alongside (not replacing) the citation URL already in the lesson `.md`. Assets stay flat in the lesson's own `Lesson_<N>_<Slug>/` folder (§D), no separate `media/` subfolder. Prompted by raw audio/image files dropped into Listening/Speaking's `Lesson_3_Backpack/` and Passage Reading's `Lesson_3_RunningShoes/` folders with non-conforming names (spaces, mixed case) and no existing convention to follow; those files were renamed to match in the same pass - see each modality's own `Index.md`. Embedding these assets into the lesson `.md`/packet `.html` content itself is out of scope for this entry and remains pending. |
| 2026-09-08 | v1.9 (Style Guide) / v1.5 (Program Conventions) | Lesson version-code shape (§G) changed from `S<Set>.<Lesson>.<Iteration>` to `<Module>.<Set>.<Lesson>.<Version>` - four dot-separated numbers, no letter prefix - now that Sets nest inside `Module_<N>/` folders and Set numbering restarts per Module (v1.4 above): a version code needs the Module to stay unambiguous across Modules, the same reason the folder shape needed it. `<Iteration>` renamed `<Version>` (same meaning: starts at 0, +1 per substantive post-generation revision). The TOEFL Track Tier variant-track letter suffix stays, now on the `<Set>` component of the new shape (e.g. `1.1T.1.0`). Retroactive, not going-forward-only: every already-assigned version code across all three modalities was mechanically rewritten (`S<Set>...` -> `1.<Set>...`, since every lesson generated so far is Module 1) in lesson `.md` files, packet `.html` mastheads, and every `Rotation_Log_<Band>.md`'s Version column and narrative mentions - `Changelog.md` entries excluded, left as dated historical record. `Student_Packet_Style_Guide.md` §B's masthead-line placeholder updated to match; its header corrected from a stale "(v1.6)" that had missed the v1.7/v1.8 `.ans-line-sm` bumps logged below. Six lesson-type prompts that construct the version code bumped to match - see each modality's own `Changelog.md`. |
| 2026-09-08 | v1.4 (Program Conventions) | New `Module_<N>/` folder level inserted between `lessons/<band>/` and `Set_<N>/` (§D), since Set numbering is scoped per Module/Band and restarts at 1 for each new Module (§C, now stated explicitly) - without a Module-level folder, a Band's Module 2 Set 1 would collide with Module 1's existing `Set_1/` folder. `Module{N}_{Band}_Lesson_Plan.md` now sits inside its own `Module_<N>/`, above that Module's `Set_<N>/` folders; the variant-track Set fork shape (§D) nests the same way. Migrated now (not grandfathered) across all three modalities' existing Module 1 content - see each modality's own `Changelog.md`. Also corrects a stale header: this file's top line still read "(v1.2)" despite the v1.3 variant-track-fork entry below already being logged; both the missed bump and this change land in the header together. |
| 2026-09-08 | v1.8 (Style Guide) | `.ans-line-sm` (§B) drops its `max-width: 320px` cap and becomes full-width, like `.ans-line` - the two classes are now distinguished only by the shorter `height`/`margin-top` on `.ans-line-sm`, not by width. Surfaced on Passage Reading's Set 1 Lesson 3 (Intermediate) "Running Shoes" packet: Task A's Q1-4 answer lines read as visibly narrower than Q5/Q6/Task C's lines two questions later in the same "Check Your Understanding" section (and the same pattern repeats in that lesson's Unit 3B, where Task A/B use `.ans-line-sm` next to Task C/D's `.ans-line`), with no reason for a narrower line in either case. Passage Reading's `Generate_Student_Packet_Prompt` Section 2.13 and Writing's `Generate_Student_Packet_Prompt` Section 2.12 both described `.ans-line-sm` as "roughly 300px" and are bumped to match (Reading v1.8 -> v1.9, Writing v2.2 -> v2.3) - see each lesson type's own Changelog; Listening/Speaking's prompt makes no literal width claim and inherits via its existing pointer, no direct edit needed. Going-forward only for the CSS class itself; every already-generated packet across all three modalities that embeds `.ans-line-sm` (26 files: Reading, Listening/Speaking, and Writing) was hand-corrected in the same pass since each embeds its own copy of the stylesheet - 4 of Reading's predate this file's formatting conventions and keep their own pre-existing compact `property:value;` CSS style rather than being reformatted as part of this fix. |
| 2026-09-08 | v1.7 (Style Guide) | `.ans-line-sm` changed from `display: inline-block` to `display: block` (§B) - the short answer line was landing on the same visual row as its question whenever the question text left enough room, instead of always starting a fresh line below it. Every existing use of `.ans-line-sm` across Passage Reading's Set 1 packets sits at the end of a finished question (never mid-sentence), so the block-level change fixes every call site without touching each one, while keeping the line's short `max-width: 320px` width. Surfaced during a review of Passage Reading's Set 1 Lesson 3 (Intermediate) "Check Your Understanding" Task A. Passage Reading's `Generate_Student_Packet_Prompt` Section 2.13 bumped to match (v1.7 -> v1.8) - see that lesson type's own Changelog. Going-forward only for the CSS class itself; the 8 already-generated Set 1 packets (both bands, Lessons 1-4) were hand-corrected in the same pass since each embeds its own copy of the stylesheet. |
| 2026-09-07 | v1.3 (Program Conventions) | New optional mechanic, introduced by Listening/Speaking's TOEFL Track Tier (its own `Generate_Lesson_Prompt` Section 0.6, which adds a labeled TOEFL-formatted alternate task to one tier of a Set's lessons rather than a separate document): a lesson type may fork a Set into a variant track without changing the base Set's own files. §D documents the folder shape (`Set_<N><letter>/`, a sibling to `Set_<N>/`, same internal structure); §G documents the matching version-code shape (`<Set>` token gets a single uppercase letter suffix, e.g. `S1T.1.0`, still fitting the existing `S<Set>.<Lesson>.<Iteration>` pattern). Both are mechanics a lesson type may use, not requirements - Passage Reading's own TOEFL extension is a separate companion document, not a variant-track fork, and needs neither. First real example: Listening/Speaking's `lessons/advanced/Set_1_T/Lesson_1_LostKitchen/`, alongside `Set_1/Lesson_1_LostKitchen/`. See Listening/Speaking's own `Changelog.md` for the full TOEFL Track Tier story. |
| 2026-09-07 | v1.6 (Style Guide) | Correction to the same-day masthead-meta version-stack change (below): the spec had the modality tag, Band, and version code as three separate stacked lines - the user's actual intent was two lines, with Band and version combined onto the second (`<Band> S<Set>.<Lesson>.<Iteration>`, e.g. `Advanced S1.1.0`). `Student_Packet_Style_Guide.md` §B rewritten to two stacked `.masthead-tag` spans instead of three; no CSS change needed (`.masthead-meta`'s flex-column/gap rule works the same for two children as three). Each modality's `Generate_Student_Packet_Prompt_*.md` bumped to match - see each lesson type's own Changelog. Surfaced via the one lesson already retrofitted with the three-line version, Listening/Speaking's `LostKitchen_Advanced_L1_Packet.html`, corrected in the same pass. |
| 2026-09-07 | v1.5 (Style Guide) | Follow-up on the `.idiom-list`/`.irow` classes added earlier the same day (below): two things still didn't match "Words to Know." (1) No numbering - idiom rows now carry a plain "N. " prefix inside `.idiom-phrase`, restarting at 1, matching `.vocab-list .word`'s existing convention (documented for the first time, for either list, in Passage Reading's `Generate_Student_Packet_Prompt` v1.6). (2) Columns didn't actually line up - `.idiom-list .irow .idiom-phrase` used `min-width: 160px` with no `nowrap`, a floor rather than a cap, so a phrase wider than 160px (e.g. "keep my feet on the ground") simply grew past the column instead of wrapping, leaving each row's gloss starting at a different x-position. Changed to a true fixed `width: 180px` (still no `nowrap`, so an overlong phrase wraps within that width instead of pushing the gloss sideways). `.vocab-list .word` doesn't hit this because it pairs `min-width` with `nowrap`; idiom phrases are long enough that the gap showed up visibly. Passage Reading's packet prompt bumped to match (v1.5 → v1.6); Listening/Speaking inherits via its existing pointer, no direct edit needed. |
| 2026-09-07 | v1.4 (Style Guide) | New `.idiom-list`/`.irow` classes: when a lesson glosses 2 or more idioms (no single "spotlight" idiom), the list now renders as a two-column table - phrase, then gloss - mirroring `.vocab-list`'s treatment of "Words to Know," instead of stacked plain `.idiom-item` paragraphs. `.idiom-phrase` keeps its existing Georgia italic-bold styling; the new `.idiom-list .irow .idiom-phrase` rule only adds a `min-width` column width, deliberately without `.vocab-list .word`'s `white-space: nowrap`, since idiom phrases run longer than single vocab words and should wrap rather than overflow. `.idiom-item`/`.spotlight-box` are unchanged and still used for a single spotlight idiom. Prompted by a review of Listening/Speaking's LostKitchen packet, which had two idioms rendering as plain paragraphs next to a tabular vocabulary list. Passage Reading's `Generate_Student_Packet_Prompt` Section 2.12 bumped to match (v1.4 → v1.5) - see that lesson type's own Changelog; Listening/Speaking's own prompt inherits the rule via its existing "same rules as Passage Reading 2.12-2.14" pointer, no direct edit needed there. Going-forward only: of the four already-generated packets with 2+ idioms, only LostKitchen was hand-corrected in this pass - `SagradaFamilia_Advanced_L1`, `ForgeAtDawn_Advanced_L2`, and `LivingTextbooks_Advanced_L2` remain pending, noted in each modality's own `Index.md`. |
| 2026-09-07 | v1.3 (Style Guide) / v1.2 (Program Conventions) | New cross-modality **lesson version number** (`Program_Conventions.md` new §G): `S<Set>.<Lesson>.<Iteration>` (global lesson number per §C, iteration starting at 0 and bumped per-lesson on substantive revision) - a distinct axis from this repo's existing "vX.Y" *prompt-file* versions. `Student_Packet_Style_Guide.md` §B's masthead-tag rule changed to match: the single modality-only `.masthead-tag` becomes a three-line `.masthead-meta` stack (Modality, Band, Version), reversing the v1.1 "no Level/Band" restriction now that Band and version are load-bearing identification, not decorative kicker text. Each modality's `Generate_Student_Packet_Prompt_*.md` bumped to match (Reading v1.3 → v1.4, Listening/Speaking v1.1 → v1.2, Writing v1.4 → v1.5) and each `Generate_Lesson_Prompt_*.md` bumped to add the matching `**Band:** ... \| **Version:** ...` metadata line to the `.md` lesson doc (Reading v2.7 → v2.8, Listening/Speaking v1 → v1.1, Writing v3.4 → v3.5) - see each lesson type's own Changelog. Going-forward only: no already-generated packet, lesson doc, or Rotation Log table is retrofitted with a version. |
| 2026-09-06 | v1.2 (Style Guide) | `Student_Packet_Style_Guide.md` §B: `.idiom-item`/`.idiom-phrase` had no font-family or font-size of their own, so an "Idioms to Know" entry fell back to the page's default serif body font at browser-default size, while the neighboring `.vocab-list` "Words to Know" entries were explicitly styled (system-ui sans-serif, 14px, term in Georgia italic-bold) - the two glossary-style sections read as visually mismatched even though they're the same content shape (a term plus a short gloss). Gave `.idiom-item` the same system-ui/14px context as `.vocab-list`, and `.idiom-phrase` the same Georgia serif family as `.vocab-list .word`, so both sections now render in one consistent type system. Found while auditing Listening/Speaking's Set 1 Lesson 1 packets (both bands) against this file. Already-generated packets under `lessons/` are not retroactively updated by this entry alone; the two Set 1 Lesson 1 packets were hand-corrected in the same pass, other already-generated packets remain pending a future regeneration/sync pass. |
| 2026-09-06 | v1.1 (Style Guide) | `Student_Packet_Style_Guide.md` §B: `.masthead` changed from a plain left-aligned block to a flex row (title left, new `.masthead-tag` span top-right), and the old "never define `.kicker`/`.sub`" note replaced with the new rule: the document's opening masthead (only - never a later `.masthead.masthead-later` heading) carries exactly one `.masthead-tag` holding that modality's plain class label (`Reading` / `Listening & Speaking` / `Writing`). This lifts each modality's prior "no kicker/subject line anywhere" ban just enough to allow this one corner label - a Name/Date field, a subtitle line under any heading, and a footer note are all still banned. Driven by a real operational need (a teacher or student identifying which class's packet they're holding at a glance) rather than a subject-line restyle; `.kicker`/`.sub` themselves are not revived. Each modality's own `Generate_Student_Packet_Prompt_*.md` bumped to match (Reading v1.2 → v1.3, Listening/Speaking v1 → v1.1, Writing v1.3 → v1.4) - see each lesson type's own Changelog. Already-generated packets under `lessons/` are not retroactively updated; a future regeneration pass would be needed for those to carry the tag. |
| 2026-09-06 | v1.1 | `Program_Conventions.md` §B: added a star-count-to-band-position table clarifying how the student-packet star rating (★-★★★★, defined identically in each lesson type's `Generate_Student_Packet_Prompt_*.md`) maps onto each band's Task-Levels row. Previously this mapping existed only implicitly — derivable by cross-referencing the star-assignment rule against the numeric table — and the natural generalization from Intermediate/Advanced ("★★/★★★ = the band's two native Levels, ★/★★★★ = one band below/above") doesn't hold for Beginner (no ★★★★; only 3 stars) or Proficient (★ and ★★ are both "1 band below," since it borrows two Levels down from Advanced rather than one). No behavior changed - this documents an existing rule already implemented consistently across all three modalities' packet prompts. |
| 2026-09-06 | v1.0 | `Student_Packet_Style_Guide.md` extracted from Passage Reading's Student Print Formatting Prompt v1.2, Section 3 (previously the only complete copy; Listening/Speaking cross-referenced it by prose, Writing had no styling content at all). While extracting, folded in two fixes Listening/Speaking had been patching on top of its copy after the fact rather than at the source: broadened `.qlist .num` to a plain `.num` rule (dropping the `.qlist` ancestor requirement, so `.qitem` numbers get the same bold weight and fixed width as `.qlist li` numbers), and dropped the separate `.discuss-block .prompt` font-size rule (Discuss It prompts now use `.qitem` and should match every other numbered question's size). Also standardized formatting across the whole stylesheet on one combined convention (space-before-brace and space-after-colon throughout, one declaration per line, multi-value properties kept on one line, double-quoted CSS strings, self-closing void HTML elements, consistent 2-space indentation) - previously Passage Reading's copy used a compact one-line-per-rule style and Listening/Speaking's delta classes used an expanded one-value-per-line style; this file picks the more readable half of each rather than adopting either wholesale. |
| 2026-09-06 | v1.0 | `Program_Conventions.md` extracted: the Level/Band taxonomy and Task-Levels-by-Band table (previously restated in `CLAUDE.md` and all three lesson-generation prompts' Section 0.1, each admitting in prose it was reusing Reading's table wholesale), what a Set is and the Set/Lesson folder-nesting shape (previously restated in `CLAUDE.md`, all three `Index.md` files, and the Reading/Listening-Speaking Module Lesson-Plan prompts), the CBI/TBLT pedagogical-framework paragraph (previously near-identical text in all three lesson-generation prompts' preambles), and generic Rotation Log purpose/mechanics (previously restated in each lesson type's `Rotation_Log.md` header, `Index.md` file-index row, and Module Lesson-Plan prompt Section 0). The "8 lessons/16 days" module-size miscount-correction narrative was deliberately **not** folded in here - it's dated project history, not an ongoing convention - and is left where it already lives (`CLAUDE.md`'s Sets bullet, Reading's `Changelog.md` 2026-09-01 entry, Listening/Speaking's own prompt correction note and `Changelog.md`), with a single pointer sentence in `Program_Conventions.md` §C instead of a fifth retelling. |
